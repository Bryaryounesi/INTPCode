'''
....That's my pandas,numpy and matplotlib.pyplot Cheatsheet...
---------------------------------------------------------
import pandas as pd
شیوه درست ایمپورت کردن پانداس
---------------------------
pd.Series()
برای ساخت سری از لیست

شکل رایج
s= pd.Series(data)

data :
لیستی از مقادیر عددی یا غیر عددی

سایر پارامتر ها 

index =
 لیستی از ایندکس های عددی یا غیر عددی
 
پیش فرض ایندکس عددی و بهتر

dtype
نوع داده هایی که میخاهیم سری داشته باشد
پیشفرض :پانداس خودش داده ها را میشناسد 

name:
نامی برای سری .چندان ضرورتی ندارد

copy
پیشفرض فولز و بهتر 
اگر ترو باشد یک کپی از داده ها ساخته میشود
---------------------------------
pd.DataFrame()
شکل رایج 
df = pd.DataFrame(data)

- ⏩data : منبع داده برای ساخت داتافریم
dict، list ،numpy array ، Series یا  DataFrame دیگر / None پیشفرض 

- index : برچسب (label) ردیف‌ها
list، RangeIndex، یا Index دیگر
پیش‌فرض None(به‌صورت خودکار از 0 شروع می‌شود)
- ⏩columns:  نام ستون‌ها
 پیش‌فرض None / لیستی از نام ستون ها / (در صورت dict، از کلیدها استفاده می‌شود)

- dtype : 
تعیین نوع داده برای تمام ستون‌ها.
 None پیش‌فرض / float, int, str, object

- copy: 
آیا داده کپی شود یا به‌صورت مرجع استفاده شود
پیش‌فرض False / اگر ترو باشد، دادهٔ ورودی کاملاً کپی می‌شود
مثلا
 df2 = pd.DataFrame(df1, copy= True)
----------------------------------
df.select_dtypes(include/exclude)
برای جدا کردن یکجای ستون های دارای ویژگی مشترک
و ساخت داتافریم های جدید 
مثلا
ساخت داتافریم از ستون های عددی
dfnum=df.select_dtypes(include="number")

ساخت داتافریم از ستون های غیر عددی
dfunnum=df.select_dtypes(exclude="number")

برای ساخت لیست ستون های دارای ویژگی مشترک
برای گروهبندی یا برای لوپ فور
numcols=df.select_dtypes(include="number").columns
unumcols= df.select_dtypes(exclude="number").columns
اگر بخواهیم برخی ستون ها را از این مجموعه خارج کنیم
unumcols= df.select_dtypes(exclude="number").drop("name",axis=1).columns

استفاده در لوپ فور
for i in numcols:

استفاده در گروهبندی
df.groupby(by="col1")[numcols].mean().round()
------------------------------------
df.fillna
شکل رایج 
df.fillna(inplace = True)

پرکردن یکجا برای ستون های عددی با توابع آماری
>>> dfm.fillna(dfm.median/mode/mean(numeric_only=True),inplace=True)

پرکردن یکجای ستون های رشته و آپجکت با پرتکرارترین مقدار هر ستون
>>> dfm.fillna(dfm.select_dtypes(include=["string","object"]).mode().iloc[0],inplace=True)

- value: مقدار جایگزین برای NaN
 پیشفرض None / scalar, dict, Series, or DataFrame

- method: روش پر کردن مقادیر خالی
پیشفرض None/ '
ffill', 'bfill', 'pad', 'backfill'

- axis: مشخص می‌کند ستون حذف شود یا ردیف 
پیش فرض 0 / برای ردیف ها 0 و برای ستون ها 1 است


- inplace: انجام عملیات روی دیتافریم اصلی
    False پیشفرض / True 

- limit: 
 حداکثر تعداد پر کردن مقادیر خالی در هر ستون یا ردیف

پیشفرض: None \ عدد دلخواه 
---------------------------------------
df.bfill()
df.ffill()
این دو جایگزین فرمول زیر هستند
df.fillna(method="ffill"/bfill,inplace=True)
 گویا این فرمول در نسخه های آینده پانداس از کار خواهد افتاد
 و دیگر متد به عنوان یک پارامتر در فیلنا کار نخواهد کرد
دو فرمول جایگزین نیازی به پارامتر اینپلیس هم ندارند و خودشان 
تغییرات را بر داتافریم اعمال می کنند.  
--------------------------------------
 df.dropna
شکل رایج 
df.dropna(ignore_index= True, inplace = True) برای حذف ردیف ها 
df["col_name"].dropna(ignore_index= True, بقیه پارامترها اغلب بلااستفاده) برای حذف سلول

ignore_index = برای جلوگیری از به هم ریختن ایندکس ها بعد از حذف
پیشفرض False / بهتر True

- inplace:
 انجام عملیات روی دیتافریم اصلی
   False پیشفرض/ True
   
- axis: جهت حذف
پیشفرض 0/مقدار: 0 (سطرها), 1 (ستون‌ها)

-------کمتر رایج -------
- how   شرط حذف
 all  /پیشفرض 'any' 
   اگر any
   باشد یک مقدار خالی کافیست
   برای حذف ستون یا ردیف 
   ولی در all 
   همه مقادیر باید خالی باشند.

- thresh: 
حداقل تعداد مقادیر غیر خالی در سطر یا ستون ها 
پیشفرض None/عدد 

- subset:
 زیرمجموعه ستون‌ها برای بررسی
پیشفرض None /مقدار list of column names

----------------------------------------
df.drop()

شکل رایج 
df.drop("colomn_name", ignore_index= True,axis = 1 , inplace = True)
value = "colomn_name"/["col1","col2",...]
برای حذف ردیف ها:
ایندکس یک ردیف  یا لیست ایندکس های چند ردیف

ignore_index= True بهتر / False پیشفرض
axis = یک برای حذف ستون / صفر برای حذف ردیف
inplace = True بهتر / False پیشفرض
-------------------------------------------
read_csv()
شکل رایج 
df = pd.read_csv(path)
# هر پارامتری پیش فرض دارد میتواند نوشته نشود
-path = r"مسیر فایل"
- index_col = "column_name"/None پیش فرض 

-----کمتر مورد استفاده---------

-nrows = تعداد ردیف
-use_cols = انتخاب ستون ها ، حالت ها در پایین

۱. با ایندکس حروفی اکسل:
usecols=["A", "C"]
۲. با نام خود ستون‌ها:
usecols=["Name", "Age"]
۳. با ایندکس عددی (شروع از ۰):
usecols=[1, 2]  
# ستون‌های دوم و سوم
۴. با بازه ایندکسی عددی:
usecols=range(1, 4) 
 # ستون‌های دوم، سوم و چهارم
۵. با بازه حروف اکسل:
usecols="A:C" 
 # تمام ستون‌های از A تا C
۶. ترکیب چندین بازه:
usecols="A:C, E:G"  # ستون‌های A-C و E-G
--------------------------------------------------
read_excel()
شکل رایج
dfall = pd.read_excel(path,sheet_name=None)
df2 = pd.read_excel(path,sheet_name=1)

-path = r
"مسیر فایل"
-sheet_name = "names"/بهتر/پیشفرض 0 None

- usecols = حالت ها در بالا توضیح داده شده اند
- nrows = عدد
- index_col = "column_name"/ None پیش فرض 

----------------------------------------
df.reset_index()
برای حذف کردن یا برداشتن ستون ایندکس(تبدیل آن به یک ستون عادی)
 
برای حذف کامل ستون ایندکس
df= df.reset_index(drop=True)

برای تبدیل ستون ایندکس به ستون عادی
df.reset_index(inplace=True)

---------------------------------------
df.sample()
شکل رایج
df= df.sample(frac =1,ignore_index=True,random_state=1)

n = عدد ردیف‌هایی که می‌خواهیم انتخاب شوند 
/پیشفرض None (در این حالت باید frac تعیین شود)

>>> frac = نسبت درصدی نمونه‌گیری (مثلاً 0.2 یعنی ۲۰٪ از داده‌ها) /
پیشفرض None (در این حالت باید ان تعیین شود)
اگر فراک دادیم نیازی به إن نیست و برعکس

random_state = (عددی ثابت برای تکرارپذیری نمونه (بهتر است مقدار ثابت بدهیم
شبیه سید در فیکر 

ignore_index = بهتر True / پیشفرض False (شماره‌گذاری مجدد ایندکس‌ها)

-----کمترکاربردی-----
replace = پیشفرض False (بدون تکرار) / True (با تکرار مجاز)
weights = ستون یا آرایه‌ای برای تعیین احتمال انتخاب هر ردیف /
پیشفرض None (احتمال برابر)
axis = پیشفرض بهتر و برابر 0 (نمونه‌گیری از ردیف‌ها) / 1 برای ستون‌ها

--------------------------------------
sort_values()
شکل رایج 
df.sort_values(by="col_name",ignore_index=True, inplace=True)

by = "col_name"/["col1","col2",...]
ignore_index = بهتر True/ پیشفرض False
inplace = بهتر True/ پیشفرض False
ascending = پیشفرض ترو وبهتر برای صعودی/ فولز برای نزولی/ 
[True,True  یا بالعکس برای حالاتی که بای نیز ستون است]

axis = پیشفرض بهتر و برابر 0/ 1 بد و سبب ارور 

---------------------------
to_excel()
شکل رایج 
df.to_excel(expath,sheetname="sh1",index=False)

-expath
-sheet_name= پیشفرض sheet1 .../"sh1"بهتر/2
-index = True پیشفرض /False بهتر 

-----------------------------------------
pd.excelWriter()
شکل رایج
with pd.ExcelWriter(expath,mode='a',if_sheet_exists= "new") as writer:
    df1.to_excel(writer,sheet_name="sh1",index=False)
  
    df2 .....

expath
mode = "a"
if_sheet_exists= "new"بهتر /"replace"/"error"

-----------------------------------------------
pd.concat
شکل رایج
dfall = pd.concat([df1,df2,...],ignore_index=True)

objs = [,] / (,)
ignore_index = False بهتر/ پیشفرض True 
axis = پیش فرض 0 / 1

----------------------------------
pd.merge()
شکل رایج
dfm = pd.merge(L,R,how = "joinType",on="colname")

L & R = داتافریم های چپ و راست
نوشتن خود این دو حرف لازم نیست فقط نام داتافریم ها را بنویس
how = "outer" بهتر/"inner"/"right"/"left"
on = نام ستون" یا ["ستون های مشترک"] بین دو"داتافریم

left_on= نام متفاوت ستون مشترک در داتافریم چپ
right_on = نام متفاوت ستون مشترک در داتافریم راست 
------------------------------
df.groupby()

🧩 شکل رایج
df.groupby(by="col_name", as_index=False)


⚙️ پارامترها
by = "col_name" / ["col1","col2", ...]
ستونی که بر اساس آن داده‌ها گروه‌بندی می‌شوند (می‌تواند یک یا چند ستون باشد).

as_index = 
بهتر False / پیش‌فرض True
اگر True: ستون گروه‌بندی به اندیس تبدیل می‌شود.
اگر False: ستون گروه‌بندی در خروجی به‌صورت ستون عادی باقی می‌ماند (قابل توصیه‌تر برای کارهای بعدی).

sort = False /  پیش‌فرض True بهتر
اگر True: گروه‌ها بر اساس نامشان مرتب می‌شوند.
اگر False: ترتیب داده‌ها حفظ می‌شود و کمی سریع‌تر اجرا می‌شود.

------کمتر رایج در کار ما --------

dropna = بهتر True / پیش‌فرض True
اگر True: ردیف‌هایی که مقدار گروهشان NaN است حذف می‌شوند.
اگر False: آن گروه‌ها هم نگه داشته می‌شوند.

axis = پیش‌فرض بهتر و برابر 0 / 1 بد و سبب ارور در بیشتر موارد
محور گروه‌بندی؛ معمولاً 0 برای سطرها استفاده می‌شود (درست‌ترین گزینه).


level = None / سطحی از اندیس در DataFrame چندسطحی (MultiIndex)
اگر اندیس چندسطحی داری، تعیین می‌کند در کدام سطح گروه‌بندی انجام شود.

observed = بهتر False / پیش‌فرض False
فقط در داده‌های دسته‌ای (categorical) کاربرد دارد؛ اگر True فقط گروه‌های واقعی را نگه می‌دارد.

group_keys = بهتر True / پیش‌فرض True
مشخص می‌کند نام گروه‌ها در خروجی (مثل هنگام استفاده از apply()) حفظ شوند یا نه.
---------------------------------------
import matplotlib.pyplot as plt
شیوه اینمپورت کردن مت پلات لیب
-------------------------------
🔆 رایج‌ترین توابع
matplotlib.pyplot
برای هوش مصنوعی
# توابع plt

✅figure
برای جدا کردن و ترسیم مجزای هر نمودار از بقیه نمودارها

شکل رایج:
plt.figure(num=)

به تعداد دفعاتِ درج این تابع،صفحه برای رسم نمودار ایجاد میشود
اگر درون پرانتز هر تابع، شماره  بگذاریم
آن عدد تبدیل به نام فیگور میشود
مثلا

plt.figure(3)
نوشته میشود figure 

پارامترها:

figsize = 
 اندازهٔ شکل به اینچ (width, height)

پیش‌فرض: (6.4, 4.8)
مثال: figsize=(8,6)

dpi:
کیفیت و رزولوشن شکل (dots per inch)

پیش‌فرض: 100
مثال: dpi=120


facecolor:

توضیح: رنگ پس‌زمینهٔ شکل
پیش‌فرض: 'white'
مثال: facecolor='lightgray'

edgecolor:
 رنگ حاشیهٔ شکل
پیش‌فرض: 'white'
مثال: edgecolor='blue'

num:
 شماره یا عنوان شکل

پیش‌فرض: None (سیستم خودش شماره اختصاص می‌دهد)
اگر عدد بگذاریم واژه figure حذف نمیشه(میشه figure 5 مثلا)
ولی اگر رشته باشد، فقط رشته به عنوان نام تعریف میشه
مثال: num=1 یا num="My Figure"
------------------------------------
✅subplot
برای آوردن بیش از یک نمودار در یک صفحه

نکته: تمام نمودارهای ساب پلات شده تنها باید با یک 
plt.show()
در آخر همه نمایش داده شوند نه هر یک به صورت جدا دارای این تابع باشد

شکل رایج : 
plt.subplot(nrows,ncols,index)

پارامتر ها: 
nrows →
 تعداد ردیف‌ها (number of rows)

ncols → 
تعداد ستون‌ها (number of columns)

index →
 شماره خانه‌ای که نمودار در آن رسم می‌شود (position of the subplot)

سایر پارامترها : 
hspace →
 فاصله عمودی بین ردیف‌ها (height space)

wspace →
 فاصله افقی بین ستون‌ها (width space)

مثال: 
import matplotlib.pyplot as plt
plt.subplot(2,1,1)
#دو ردیف و یک ستون و دو خانه داریم که این نمودار در خانه اول رسم میشود
plt.hist(A,edgecolor="black",color="green",bins=30)

plt.subplot(2,1,2)
plt.scatter(X,Y,color="black")
plt.show()
--------------------------------
plt.tight.layout()
برای جلوگیری از روی هم افتادن نمودارها و متن های ساب پلات شده

# تابع تای لایوت در پایان و بعد از همه ساب پلات ها درج میشود
# تا مانع تداخل اجزای نمودارها با هم شده و بین آنها فاصله بدهد
--------------------------
✅plot✴️
کاربرد: رسم نمودار خطی ساده یا چند خطی برای نمایش رابطه بین دو سری داده
🧩 شکل رایج

plt.plot(x, y)

⚙️ پارامترها

x: مقادیر محور افقی
پیش‌فرض: ندارد
بهتر: مقادیر واقعی داده‌ها
y: مقادیر محور عمودی
پیش‌فرض: ندارد
بهتر: مقادیر واقعی داده‌ها

: مثلا اگر ایکس و ایگریک ستون داتافریم باشند
x= df["country"] 
y=df["math"]
اگر ایکس یک لیست باشد، خود ایگریک ساخته میشود
plt.plot([list])

color: رنگ خط
پیش‌فرض: 'blue'
بهتر: رنگ دلخواه برای خوانایی

linestyle: نوع خط
پیش‌فرض: '-'
بهتر: '-' یا '--' بسته به نیاز
چند مورد دیگه هم داره 
marker: علامت نقاط داده
پیش‌فرض: None
بهتر: 'o' برای نمایش نقاط
میتوان از * یا هر علامت دلخواه دیگری هم استفاده کرد
label:
 برچسب نمودار برای تابع لجند
پارامتر لیبل در تابع پلات برای نام‌گذاری هر خط در نمودار استفاده می‌شود
تا بعداً در لجند(راهنما) نشان داده شود.
وقتی چند خط روی یک نمودار رسم می‌کنی، 
با لیبل می‌توانی برای هرکدام یک برچسب بگذاری 
در راهنما مشخص شود هر خط مربوط به کدام داده است.

plt.plot(x, y, label="Math Scores")


پیش‌فرض: None
بهتر: برچسب معنی‌دار وقتی چند خط داریم
-----------------------------
✅scatter✴️
کاربرد: رسم نمودار پراکندگی برای نشان دادن توزیع نقاط و همبستگی بین دو متغیر

🧩 شکل رایج
plt.scatter(x, y)

⚙️ پارامترها

x و y: مختصات نقاط
پیش‌فرض: ندارد
بهتر: داده‌های واقعی
با یک لیست واحد به جای ایکس و ایگریک ارور میدهد
باید یا یک آرایه/سری جدا به جای ایکس هم بدهیم یا 
ایکس و ایگریک را از داتا بگیریم
مثلا
plt.scatter(df["col1"],df["col2"])
هر دو پارامتر، داتای واقعی اند
---------------
s=df["col1"]
plt.scatter(s.index,s.values)
ایکس و ایگریک از داتا گرفته شده اند

color: رنگ نقاط
پیش‌فرض: 'blue'
بهتر: رنگ دلخواه

marker: شکل نقاط
پیش‌فرض: 'o'
بهتر: 'o' یا 'x'

s: اندازه نقاط
پیش‌فرض: 20
بهتر: متناسب با نمودار

label: برچسب برای legend
پیش‌فرض: None
بهتر: برچسب معنی‌دار
----------------------
✅bar✴️
کاربرد: رسم نمودار میله‌ای برای مقایسه دسته‌ای داده‌ها

🧩 شکل رایج
plt.bar(x, height,edgecolor="black")

⚙️ پارامترها

x: موقعیت دسته‌ها روی محور افقی
پیش‌فرض: ندارد
بهتر: بر اساس داده‌ها

height: ارتفاع ستون‌ها
# همان محوایگریک ولی با اسمی دیگر

 پیش‌فرض: ندارد
بهتر: مقادیر واقعی

با یک لیست واحد به جای ایکس و ایگریک ارور میدهد
پس ایکس و ایگریک باید هر دو لیست واقعی/سری یا آرایه باشند
نباید ایکس را به صورت ساختگی از ایگریک بسازیم

color: رنگ ستون‌ها
پیش‌فرض: 'blue'
بهتر: رنگ دلخواه

width: پهنای ستون‌ها
پیش‌فرض: 0.8
بهتر: 0.5 تا 0.9

label: برچسب برای legend
پیش‌فرض: None
بهتر: برچسب معنی‌دار
edgecolor= رنگ لبه میله ها 
پیش‌فرض: None
بهتر: 'black'
----------------------------
hist✴️
کاربرد: رسم هیستوگرام برای نمایش توزیع فرکانس داده‌ها

🧩 شکل رایج
plt.hist(data, edgecolor='black')
نکته: داتا به جای محور ایکس قرار میگیرد 
و محور ایگریک فراوانی (frequency)این داتاست
که خود مت پلات لیپ آن را میسازد

⚙️ پارامترها
data: داده‌ها برای هیستوگرام
پیش‌فرض: ندارد
بهتر: آرایه واقعی داده‌ها

bins: تعداد بازه‌ها
پیش‌فرض: 10
بهتر: بسته به پراکندگی داده‌ها

color: رنگ ستون‌ها
پیش‌فرض: 'blue'
بهتر: رنگ دلخواه

edgecolor: رنگ مرز ستون‌ها
پیش‌فرض: None
بهتر: 'black'

alpha: شفافیت نمودار
پیش‌فرض: 1.0
بهتر: 0.5 تا 0.8 برای نمودارهای چندگانه
---------------------------
xlabel / ylabel✴️
کاربرد: تعیین عنوان محور افقی و محور عمودی نمودار

🧩 شکل رایج

✅plt.xlabel("X-axis label")
✅plt.ylabel("Y-axis label")

⚙️ پارامترها
xlabel / ylabel: عنوان محور
پیش‌فرض: ''
بهتر: متن مناسب داده‌ها
---------------------------
✅title✴️
کاربرد: تعیین عنوان کلی نمودار
🧩 شکل رایج

plt.title("Chart Title")

⚙️ پارامترها
متن عنوان
پیش‌فرض: ''
بهتر: عنوان معنی‌دار
--------------------------
legend✴️
کاربرد: نمایش راهنمای نمودارها (label های مختلف)

🧩 شکل رایج
plt.legend(loc='best')

⚙️ پارامترها

loc: محل نمایش legend
پیش‌فرض: 'best'
بهتر: 'upper right' یا 'lower left' بسته به نمودار
------------------------------
show✴️
کاربرد: نمایش نمودار نهایی
🧩 شکل رایج

plt.show()
⚙️ پارامترها
ندارد
---------------
savefig()
تابعی برای ذخیره نمودار در فایل های تصویری

شکل رایج:
plt.savefig(fname="chartname.png",dpi=300)

نکته1 : نباید قبل از این تابع از plt.show استفاده شود
چون فقط صفحه خالی ذخیره میشه

نکته2: اگر چند figure داشته باشیم با plt.savefig تنها یکی از آنها ذخیره میشه
راه حل اینه که در زمان ساخت figure ها برای هر
figureیک شی تعریف کنیم مثلا fig1=plt.figure(1)
سپس در زمان ذخیره با savefig بنویسیم fig1.savefig("fig_name")

پارامترها:
dpi:
بیانگر وضوح تصویر
300 بهتر

fname: ""
نام فایل همراه با پسوند فرمت درون دبل کوت
مثلا name.png 

------------------------------
plt.imshow
برای نمایش تصویر — بسیار مهم در بینایی ماشین (دیدن ورودی‌ها، 
فیلترها، heatmapها و …).
------------------------------
plt.boxplot
برای بررسی پراکندگی و تشخیص outlierها — کاربرد در پردازش داده و 
پاک‌سازی دیتاست.
------------------------------
 plt.grid
برای نمایش شبکهٔ پس‌زمینه — خوانایی را بالا می‌برد.
------------------------------
 plt.colorbar
برای نمودارهای heatmap یا خروجی‌های مدل‌های CNN.
--------------------------------------------------
--------------------------------------------------
⚜️ نیاز به ایکس در انواع نمودار مت‌ پلات‌ لیب ⚜️

#انواع نمودار matplotlib 
#ایکس در نمودار ها 

❌ = نیاز ندارد
⭕ = اختیاری
✔️ = نیاز دارد
-----------------------
hist
❌ نیاز ندارد
فقط ایگریک می‌گیرد
محور ایکس به‌صورت خودکار بازه‌ها (bins) است
plt.hist(y)
-------------------------------------
boxplot
❌ نیاز ندارد
ایکس  صرفاً جایگاه رسم است
ایکس معنا ندارد و توسط خود نمودار ساخته می‌شود
plt.boxplot(y)
-----------------------------------
plot (line plot)

⭕ اختیاری
حالت‌ها:
1- بدون درج x
ایکس به‌صورت خودکار، ساخته می‌شود
plt.plot(y)

2- وقتی y یک Series است ولی ایندکس معنا ندارد و فقط ترتیب است
در این حالت x از روی y ساخته می‌شود با فرمول زیر 
plt.plot(range(len(y),y)

3- وقتی هر دو محور معنا دارند.

plt.plot(df["col1"], df["col2"])

4- وقتی y سری حاصل از دسته بندی value_countd  است و ایندکس آن معنا دارد 
در این حالت x از ایندکس سری و y از والیوی سری بدست می آید . 
s= df["col_name"].value_counts()

plot.plot(s.index,s.values)
-------------------------------
scatter
✔️ نیاز دارد
هر دو محور باید دادهٔ واقعی باشند
 ⏪ایکس ساختگی معمولاً تحلیل را بی‌معنا می‌کند

plt.scatter(x, y)
پس x میتوان از ستون داتافریم، آرایه یا ایندکس سری بیاید ولی ایکس ساختگی نداریم. 
---------------------------------
bar
✔️ نیاز دارد
ایکس  باید دسته، نام، یا مفهوم قابل مقایسه باشد.
ایگریک،  مقدار هر دسته است
plt.bar(x, y)
پس x میتوان  از ستون داتافریم، آرایه یا ایندکس سری بیاید. ولی x ساختگی نداریم. 
--------------------------------------
value_counts bar
✔️ نیاز دارد

ایکس  از index سری می‌آید (دسته‌ها)
ایگریک از values می‌آید (تعداد)

s = df["col"].value_counts()
plt.bar(s.index, s.values)
------------------------------
جمع‌بندی نهایی (قانون ذهنی)
🔰نمودارهای توزیعی و خلاصه → x لازم نیست
🔰نمودارهای رابطه‌ای و مقایسه‌ای → x باید معنا داشته باشد
🔰اگر x در داده وجود ندارد → فقط در plot می‌توان آن را مصنوعی و ساختگی  ساخت. 
 -------------------------------------
⚜️چه نوع نموداری را رسم کنیم؟ نمودار ما نیاز به ایکس دارد یا نه ؟ ⚜️

#تشخیص نوع نمودار درست 
#نوع درست نمودار
#نیاز به x 

الگوریتم تشخیص ایکس (سه سؤال طلایی)
سؤال ۱-🟣این نمودار فقط برای دیدنِ توزیع است؟

مثال
پخش نمره‌ها
پراکندگی داده
میانه و چارک‌ها

اگر بله ⟶
❌ x نساز
❌ x از داده نگیر

نمودار درست:
plt.hist(y)
plt.boxplot(y)
----------------
سؤال ۲) اگر ایکس را حذف کنم، نمودار هنوز معنی دارد؟

اگر بله ⟶
⭕ x معنا ندارد
✔️ می‌توانی x را بسازی

نمودار درست:

plt.plot(y)
# یا
x = range(len(y))
plt.plot(x, y)
------------------------------
سؤال ۳)آیا چیزی در مسئله هست که «روی محور افقی» باید دیده شود؟

مثل:
زمان
نام افراد
دسته‌ها
نمره‌ها
گروه‌ها

اگر بله ⟶
✔️ x باید از داده بیاید
❌ x نساز

نمودار درست:
plt.bar(df["student"], df["math"])
plt.scatter(df["age"], df["score"])
plt.bar(s.index, s.values)
---
نسخهٔ فوق‌فشرده (حفظی)

توزیع؟        → x نمی‌خواهم
روند؟         → x می‌تواند مصنوعی باشد
مقایسه/رابطه؟ → x باید واقعی باشد
---

تست نهایی خودکنترلی
قبل از نوشتن کد، این جمله را کامل کن:
> «محور افقی این نمودار نشان‌دهندهٔ ____ است»
اگر نتوانستی چیزی معنادار بگویی → x نساز
اگر فقط گفتی «ردیف‌ها» → x بساز
اگر گفتی «زمان / شخص / دسته / مقدار واقعی» → x را از داده بگیر
----------------------------------
جمع‌بندی ذهنی نهایی

x یا:

وجود ندارد (توزیع)
ساختگی است (ترتیب)
واقعی است (مفهوم)
------------------------------------
----------------------------------
⚜️رسم نمودار مستقیما از پانداس ⚜️

🔰رسم نمودار از پانداس شبیه رسم از مت پلات لیب است ولی کمی کار را ساده تر میکند و کد نویسی را کمتر.
🔰 در رسم از پانداس،  فقط یک تابع داریم که  plot() است و نوع نمودار به صورت پارامتر kind به این تابع داده میشوند. 
🔰 همچنان نیاز به ایمپورت متپلات لیب داریم چون مراحل نمایش و ذخیره نمودار مثل قبل است و تنها ساخت نمودارها از پانداس است. 

🧠 Pandas Plot – Cheat Sheet

✅ plot✴️ (Line)
نمودار خطی
کاربرد: نمایش روند و تغییرات داده
df.plot()

پارامترهای مهم:
kind برای نمودار خطی لازم نیست 
x → محور افقی (پیش‌فرض: index)
y → ستون یا ستون‌ها
color → رنگ
linestyle → '-' '--' ':'
marker → 'o' '*' 'x' '+'
linewidth → ضخامت خط
ضخامت خط : پیشفرض 1 
title → عنوان
grid → True / False default
نمودار را درون یک شبکه رسم میکند
figsize → (w, h)
اندازه نمودار به اینچ ( عرض, ارتفاع)
پیشفرض figsize=(6.4,4.8)

legend → True / False default 

نمونه:

df.plot(x="year", y="sales", marker="o", grid=True)
plt.show()
----------------------------------------
📊 bar✴️ (ستونی)

کاربرد: مقایسه مقادیر گسسته
df.plot(kind="bar")
پارامترهای مهم:

x
y
color
width
title
grid
نمونه:
df.plot(x="city", y="population", kind="bar")
plt.show()
---------------------------------------
📉 hist✴️ (هیستوگرام)

کاربرد: بررسی توزیع داده
df["col"].plot(kind="hist")

پارامترهای مهم:
bins → تعداد بازه‌ها
color
alpha → شفافیت
grid
edgecolor
نمونه:

df["age"].plot(kind="hist", bins=20, alpha=0.7)
plt.show()
-------------------------------
📦 box✴️ (جعبه‌ای)
کاربرد: تشخیص پراکندگی و داده پرت

df.plot(kind="box")
پارامترهای مهم:

vert → True / False
grid
title

نمونه:
df[["height", "weight"]].plot(kind="box")
plt.show()
---------------------------------------
🔘 scatter✴️ (پراکندگی)
کاربرد: بررسی رابطه دو متغیر
df.plot(kind="scatter", x="xcol", y="ycol")

پارامترهای مهم:

x
y
color
s → اندازه نقاط
alpha
نمونه:
df.plot(kind="scatter", x="age", y="salary", alpha=0.6)
plt.show()
--------------------------------
🥧 pie✴️ (دایره‌ای)
کاربرد: نمایش نسبت‌ها

df["col"].plot(kind="pie")
پارامترهای مهم:

autopct
startangle
ylabel → '' (برای حذف)

نمونه:
df["gender"].plot(kind="pie", autopct="%1.1f%%", ylabel="")
plt.show()
--------------------------
💾 ذخیره نمودار (همه حالت‌ها)

plt.savefig("plot.png")
plt.show()

بدون نمایش:
plt.savefig("plot.png")
plt.close()
---------------------------------
🧩 الگوی حفظی نهایی (VS Code)

import matplotlib.pyplot as plt

df.plot(...)
plt.title(...)
plt.savefig("file.png")
plt.show()
-------------------------------------
import numpy as np
شیوه درست ایمپورت کردن نامپی 
-------------------------
فرمول ساخت آرایه 
a= np.array(data)
data = 
اغلب لیست،گاهی تاپل
در ساخت ماتریس، لیستی از لیست ها

دیگر پارامتر ها :
dtype=
پیشفرض مطابق با لیست اولیه/int,float
گاهی str یا bool
-----------------------
.tolist()
برای تبدیل مجدد آرایه(بردار/ماتریس) به لیست

مثلا
new = a.tolist()
-------------------
v= np.array()
فرمول ساخت بردار

شکل رایج 
v= np.array(data)

data = 
لیستی از مقادیر عددی مثبت یا منفی 
تعداد پارامترها بدون محدودیت
--------------------------
length= np.linalg.norm(v)
فرمول محاسبه طول بردار

v= 
برداری که از فرمول بردار محاسبه شده
--------------------------------
m= np.array([[list1],[list2],[list3]])
فرمول ماتریس (آرایه ای با لیستی از لیست ها)
-------------------------
⚜️توابع آماری روی بردار‌ها ⚜️

np.size(v)  تعداد 
برابر با len(v)
np.sum(v)  مجموع 
np.mean(v)  میانگین
np.std(v)  انحراف معیار 
np.var(v)  واریانس
np.max(v)  بیشینه
np.min(v)  کمینه
np.median(v)  میانه 
p(np.mean(v)) 
نامپی تابع mode ندارد
---------------------------------------
⚜️ توابع آماری روی ماتریس ⚜️

# مجموع
# ------------------------------
np.sum(m)        
    # مجموع کل ماتریس
np.sum(m, axis=0)  
  # مجموع ستون‌ها
np.sum(m, axis=1)  
  # مجموع ردیف‌ها

# میانگین
# ------------------------------
np.mean(m)        
    # میانگین کل ماتریس
np.mean(m, axis=0)  
  # میانگین ستون‌ها
np.mean(m, axis=1)  
  # میانگین ردیف‌ها

نکته: میانگین وزندار با np.average() بدست می آید
به دلیل داشتن چند پارامتر،
به صورت جدا ولی در همین صفحه توضیح داده شده
# ------------------------------
# انحراف معیار
# ------------------------------
np.std(m)        
     # انحراف معیار کل ماتریس
np.std(m, axis=0) 
    # انحراف معیار ستون‌ها
np.std(m, axis=1)  
   # انحراف معیار ردیف‌ها
# ------------------------------
# واریانس
# ------------------------------
np.var(m)     
        # واریانس کل ماتریس
np.var(m, axis=0) 
    # واریانس ستون‌ها
np.var(m, axis=1)
     # واریانس ردیف‌ها
# ------------------------------
# بیشینه
# ------------------------------
np.max(m)     
        # بیشینه کل ماتریس
np.max(m, axis=0)  
   # بیشینه ستون‌ها
np.max(m, axis=1)  
   # بیشینه ردیف‌ها
# ------------------------------
# کمینه
# ------------------------------
np.min(m)          
   # کمینه کل ماتریس
np.min(m, axis=0)  
   # کمینه ستون‌ها
np.min(m, axis=1)  
   # کمینه ردیف‌ها
# ------------------------------
# میانه
# ------------------------------
np.median(m)      
    # میانه کل ماتریس
np.median(m, axis=0) 
 # میانه ستون‌ها
np.median(m, axis=1)
  # میانه ردیف‌ها
---------------------------------
تعداد عناصر ماتریس 
m.size             # تعداد کل عناصر
m.shape[0]         # تعداد ردیف‌ها
m.shape[1]         # تعداد ستون‌ها

---------------------------------
🔆شرط تحقق ضرب ماتریسی به زبان پایتون 🔆
if A.shape[1] == B.shape[0]:
    C = A @ B
    print(C)
else:
    print("ابعاد سازگار نیستند، ضرب انجام نمی‌شود")

--------------------------------------
c= A @ B یا c=np.dot(A, B)
فرمول ضرب ماتریسی
--------------------------------------
⚜️توابع نامپی برای ساخت ماتریس و بردار با ابعاد دلخواه⚜️
🟣 توابع کاربردی تر از نظر شخصی
 
برای ماتریس:
np.zeros((m,n)) 
→ ماتریس m×n پر از صفر
مثال: A = np.zeros((3,4))

برای بردار:
np.zeros(n) 
بردار n خانه‌ای پر از صفر
مثال: v = np.zeros(5)
-----------------------------
برای ماتریس:
np.ones((m,n)) 
 ماتریس m×n پر از یک
مثال: A = np.ones((2,5))

برای بردار :
np.ones(n) 
بردار n خانه‌ای پر از یک
مثال: v = np.ones(4)
----------------------------------------------
برای ماتریس:
np.eye(n)
 ماتریس واحد n×n
یک ماتریس مربعی با والیوی غالبا صفر میسازد ولی والیوهایی از یک هم دارد  که به صورت قطری در ماتریس امتداد دارند. 
مثال: I = np.eye(3)

برای بردار :
برای بردار قابل استفاده نیست
-----------------------------------------
برای ماتریس:
np.full((m,n), value) 
 ماتریس m×n پر از یک عدد دلخواه
این عدد دلخواه جای value قرار میگیرد. 
مثال: A = np.full((2,3), 7)

برای بردار:
🟣np.full(n, value) 
 بردار n خانه‌ای پر از عدد دلخواه
تعداد پارامتر = n 
عدد دلخواە = value
مثال: v = np.full(6, 7)
-------------------------------
برای ماتریس:
np.arange(total_elements).reshape(m,n) 
 ماتریس با اعداد ترتیبی
مثال: A = np.arange(12).reshape(3,4)
باید حاصلضرب اعداد درون پرانتز reshape برابر با عدد arange باشد در غیر این صورت خطا می‌دهد. اگر این قانون رو رعایت کنیم ماتریسی میسازد با والیوهایی  از عدد ۱ تا عدد درون arange

برای بردار:
🟣np.arange(n) 
 بردار با اعداد ترتیبی از یک تا n 
مثال:
v1 = np.arange(5)   بازه از صفر تا 4
v2=np.arange(2,10)   بازه از 2 تا 9
v3=np.arange(3,10,step=3)  بازه از 3 تا 9 با گام حرکتی سه تایی
---------------------------------------
برای ماتریس
A= np.linspace(start,stop,num).reshape(m,n)
تولید آرایه در یک رنج دلخواه ولی با یک گام حرکتی مشخص نه تصادفی

پارامتر ها:
start
شروع بازه
stop:
پایان بازه
num
تعداد اعداد تولید شونده
پیشفرض 50/ بهتر عدد دلخواه 

m , n  ابعاد ماتریس که حاصلضرب آنها باید برابر num باشد
-----------------
سایر پارامترها
dtype=
پیشفرض float
/int,bool,str
اغلب نتایج تابع به صورت اعشاری میشوند 
پس با این پارامتر و انتخاب int میتوان اعداد صحیح بدست آورد 
-----------
مثال : 
A= np.linspace(0,10,6,dtype=int).reshape(2,3)


برای بردار: 
v= np.linspace(start,stop,num)
مثال :
v= np.linspace(0,8,5,dtype=int)
حتما باید سه عدد آغار و پایان بازه و num درج شوند
------------------------------

برای ماتریس:
🟣 np.random.rand(m,n) 
 ماتریس m×n با اعداد تصادفی و اعشاری  بین 0 و 1 
مثال:
 A = np.random.rand(2,3).round()
البته اعداد اعشاری طولانی به ما میدهد که بهتر است آنها را با تابع round() کرد کنیم.‌

برای بردار:
🟣np.random.rand(n) 
 بردار n خانه‌ای با اعداد تصادفی بین 0 و 1
تعداد اعداد بین صفر و یک = n 
مثال: v = np.random.rand(4)
------------------------------------
برای ماتریس:
🟣 np.random.randint(min,max,size=(m,n))
ماتریس با اعداد تصادفی در رنج دلخواه 
min و max
دو آستانە  رنج دلخواه ما هستند 
چهار چوب ماتریس را از نظر تعداد سطر و ستون مشخص می‌کند
تعداد سطر = m 
تعداد ستون = n 

مثال: 
 A= np.random.randint(5, 20, size=(2,5))
print(A)

برای بردار:
v= np.random.randint(5, 20, size=n)
مثال:
v= np.random.randint(5,20,size=8)
-------------------------------------
 ⚜️توابع کمکی نامپی برای تبدیل شکل بردار⚜️

reshape(n,1) 
 تبدیل بردار به ستون برای ضرب ماتریسی
مثال: v_col = np.arange(3).reshape(3,1)
---------------------
reshape(1,n) 
 تبدیل بردار به سطر برای ضرب ماتریسی
مثال: v_row = np.arange(3).reshape(1,3)
---------------------------------------
ساخت بردار در رنج دلخواه با اعداد تصادفی و تابع
random.choices 
# خروجی این تابع، یک لیست است که باید این لیست را در فرمول آرایه نامپی قرار دهیم تا آرایه ساخته شود
import numpy as np
import random
random.seed(1)
L= random.choices(population=range(10,50), k= 8)
# لیست ساخته شد
v= np.array(L)

تابع random.choices
به صورت مفصل تر و به همراه پارامترها در همین متن توضیح داده شده
--------------------------
💠توابع تبدیل داده ها در نامپی 💠

✴️astype 
تبدیل نوع داده
وقتی می‌خواهیم نوع داده‌ها را عوض کنیم (مثلاً int → float)، از astype استفاده می‌کنیم.

شکل رایج:
new_arary= array.astype(array_type)

array_type = int/float or ...

مثال

import numpy as np
data = np.array([1, 2, 3])
result = data.astype(float)
print(result)
--------------------
✴️reshape 
— تغییر شکل آرایه
برای تغییر تعداد سطر و ستون بدون تغییر مقدارها استفاده می‌شود.
این کار در هوش مصنوعی بسیار رایج است.
⏪برای تبدیل بردار به ماتریس قابل استفاده است.

شکل رایج :
new_arary= array.reshape(n,m) 

n و m :
ابعاد ماتریس هستند

مثال
import numpy as np
data = np.array([1, 2, 3, 4, 5, 6])
result = data.reshape(2, 3)
print(result)
----------------------------------
✴️ flatten 

— تبدیل آرایه چندبعدی به یک‌بعدی

برای زمانی که مدل یا تابع فقط داده یک‌بعدی قبول می‌کند. 
⏪مثلا برای تبدیل ماتریس به بردار 

شکل رایج :
new_arary = array.flatten()

مثال
import numpy as np
data = np.array([[1, 2], [3, 4]])
result = data.flatten()
print(result)
----------------------------------
✴️ np.concatenate(())
— چسباندن آرایه‌ها

⏪دقت شود که دو پرانتر دارد

برای کنار هم قرار دادن دو یا چند آرایه استفاده می‌شود.
🔰 آرایه ها باید ابعاد یکسانی داشته باشند.
مثلا هر دو بردار یا هر دو ماتریس دو بعدی باشند. 

شکل رایج :
new_arary = np.concatenate((array1, array2),axis=0/1)

axis 
پیشفرض 0
برای بردارها لازم نیست 
برای ماتریس ها باید به آن دقت شود
axis=1 برای چسباندن افقی 
axis =0 یا عدم درج برای چسباندن عمودی 

مثال
import numpy as np
a = np.array([1, 2])
b = np.array([3, 4])
result = np.concatenate((a, b))
print(result)

مثال دوم برای چسباندن دو ماتریس از طرف ستون ها به هم 
import numpy as np
p= print
a= np.arange(1,13).reshape(3,4)
b=np.linspace(-9,15,12,dtype=int).reshape(3,4)
c=np.concatenate((a,b))
p(c)
---------------------------
✴️ np.split()
تقسیم آرایه

وقتی می‌خواهیم آرایه را به بخش‌های مساوی یا چند تکه تقسیم کنیم.
🔰مشکل این تابع اینه که وقتی آرابه ای رو با اون تکه تکه میکنی، یک لیست میسازد با چندین  عبارت array() که با کاما از هم جدا شدن و درون پرانتز هر یک، یک تکه از آرایه اولیه قرار دارد. 
مثلا [...array(), array(), array(), arr]

شکل رایج :
new_arary = np.split(array,parts,axis=0/1)

axis=
پیشفرض صفر
برای بردارها لازم نیست 
برای ماتریس ها باید به آن دقت شود
axis=1 برای برش افقی ردیف به ردیف 
axis =0 یا عدم درج برای برش عمودی ستون ها 

مثال

import numpy as np
data = np.array([10, 20, 30, 40, 50, 60])
result = np.split(data, 3)
print(result)
---------------
✴️ np.sort() 
مرتب‌سازی داده
برای مرتب نمایش دادن داده‌ها از کوچک به بزرگ.


شکل رایج:
new_arary = np.sort(array)

مثال
import numpy as np
data = np.array([5, 2, 8, 1])
result = np.sort(data)
print(result)
------------------------------
فرمول دترمینان ماتریس 
m_det = np.linalg.det(m)
------------------------------
فرمول معکوس ماتریس 
m_inv = np.linalg.inv(m)
----------------------------
شرط  معکوس داشتن ماتریس (صفر نبودن دترمینان) 

if m1_det!=0:
    m1_inv=np.linalg.inv(m1)
    p("m1 invers:",m1_inv)
else:
    p("matrix has no invers!")
---------------------------- 
 ✴️چهار ابزار پایه‌ایِ جبرخطی موردنیاز در هوش مصنوعی برای حل دستگاه های معادلات ✴️

این چهار ابزار solve, inv, istsq و pinv هستن . 
هر کدام برای چه حالاتی مناسب و برای چه حالاتی مناسب نیستند: 

🟢 solve
تعداد معادلات = تعداد مجهول‌ها (دستگاه های مربعی)✔
ماتریس قابل معکوس ✔
ماتریس منفرد (بدون معکوس و با دترمینان صفر)✖
تعداد معادلات > تعداد مجهول‌ها(دستگاه های بیش تعداد) ✖
تعداد معادلات < تعداد مجهول‌ها(دستگاه های کم تعداد) ✖
------------------
فرمول solve :
x= np.linalg.solve(A, b)

مثال : 

مثال
می‌خواهیم دستگاه زیر را حل کنیم:
2x + y = 5
x - y = 1

import numpy as np
# ماتریس ضرایب
A = np.array([[2, 1],
              [1, -1]])

# بردار سمت راست معادلات
b = np.array([5, 1])

# حل دستگاه
x = np.linalg.solve(A, b)
print(x)
-------------------------
🟢inv
معکوس صریح ماتریس مربعی ✔
ماتریس نزدیک به منفرد و منفرد ✖
دستگاه بیش‌تعداد ✖
دستگاه کم‌تعداد ✖
-----------
فرمول inv :

A_inv= np.linalg.inv(A)

مثال : 
import numpy as np

# ماتریس مربعی
A = np.array([[2, 1],
              [1, -1]])

# بردار سمت راست
b = np.array([5, 1])

# محاسبه معکوس ماتریس
A_inv = np.linalg.inv(A)

# ضرب معکوس در بردار سمت راست
x = A_inv @ b
print(x)
-----------------------
🟢lstsq
ال-اِس-تی-اِس-کیو
 
تعداد معادلات > تعداد مجهول‌ها ✔
داده‌های نویزی ✔
دستگاه مربعی سالم ✖
ماتریس منفرد ✖
------------
فرمول lstsq : 
x = np.linalg.lstsq(A, b, rcond=None)[0]

مثال :

import numpy as np

# ماتریس ضرایب
A = np.array([[1, 1],
              [2, 1],
              [3, 1]])
# بردار سمت راست معادلات
b = np.array([2, 3, 4])

# حل با روش کمترین مربعات
x = np.linalg.lstsq(A, b, rcond=None)[0]
print(x)
------------------------
🟢pinv
شبه‌معکوس مور-پنروز
اسم دیگر pseudo-inverse

تعداد معادلات < تعداد مجهول‌ها ✔
ماتریس منفرد ✔
دستگاه غیرمربعی ✔
دستگاه مربعی سالم ✖
-------------
فرمول pinv :
x = np.linalg.pinv(A) @ b

مثال :
import numpy as np
# ماتریس ضرایب
A = np.array([[1, 1, 1],
              [2, 1, 1]])

# بردار سمت راست
b = np.array([3, 4])

# حل با شبه‌معکوس
x = np.linalg.pinv(A) @ b
print(x) 
-------------------------------------------------
⚜️لیست جامع توابع ریاضی NumPy برای شروع هوش مصنوعی⚜️

🟢۱. توابع پایه‌ای و عددی
np.sqrt(array)
  ریشه دوم یا همان جذر

درهمه توابع این لیست آرایه درون پرانتز تابع قرار میگیرد
فقط در تابع اول برای نمونه این را نوشتیم
دربقیه حتی پرانتز نیز نوشته نشده

np.cbrt 
— ریشه سوم
np.exp 
تابع نمایی (e^x)

np.exp2 
— تابع نمایی به مبنای 2

np.log 
— لگاریتم طبیعی (ln(x))

np.log2 
— لگاریتم به مبنای 2

np.log10 
— لگاریتم به مبنای 10

np.log1p 
— لگاریتم طبیعی (1 + x)

np.abs 
 مقدار قدرمطلق 

np.power 
— توان

np.mod 
— باقی‌مانده تقسیم

🟢۲. توابع مثلثاتی
np.sin — سینوس
np.cos — کسینوس
np.tan — تانژانت
np.arcsin — معکوس سینوس
np.arccos — معکوس کسینوس
np.sinh — هیپربولیک سینوس
np.cosh — هیپربولیک کسینوس
np.tanh — هیپربولیک تانژانت

🟢 ۳. توابع آماری و تجمعی

np.mean — میانگین
np.std — انحراف معیار
np.var — واریانس
np.min — حداقل
np.max — حداکثر
np.median — میانه
np.sum — جمع عناصر
np.prod — حاصل‌ضرب عناصر
np.cumsum — جمع تجمعی
np.cumprod — حاصل‌ضرب تجمعی
np.percentile — درصدک‌ها
np.corrcoef — ضریب همبستگی

🟢 ۴. توابع جبر خطی و برداری

np.dot — ضرب داخلی بردارها یا ماتریس‌ها
np.cross — ضرب متقاطع بردارها
np.linalg.norm — نُرم (طول بردار یا ماتریس)
np.linalg.inv — معکوس ماتریس
np.linalg.det — دترمینان ماتریس
np.linalg.eig — مقادیر و بردارهای ویژه
np.linalg.solve — حل معادله خطی Ax = b

🟢۵. توابع کاربردی دیگر

np.clip — محدود کردن مقادیر در بازه مشخص
np.sign — علامت هر عنصر
np.round — گرد کردن مقادیر
np.floor / np.ceil — پایین/بالا گرد کردن
np.reshape — تغییر شکل آرایه
np.transpose — ترانهادهٔ ماتریس
np.flatten — صاف کردن آرایه چندبعدی 
-----------------------------------
💠نحوه دسترسی به ردیف‌ها و ستون‌های ماتریس 💠
#دسترسی به ردیف ماتریس
#دسترسی به ستون ماتریس 
دسترسی به عنصر ماتریس 

چه برای ردیف ها و ستون ها از ایندکس استفاده میکنیم 
و ایندکس ها از صفر شروع شده و بیشتر میشوند. طبق الگوی های زیر : 
برای انتخاب یک ردیف واحد 
matrix_name[row_index]
برای انتخاب رنجی از ردیف ها 
matrix_name[start:end]
برای انتخاب یک ستون 
matrix_name[ : ، col_index]
برای انتخاب رنجی از ستون ها 
matrix_name[ : ، start : end]
برای انتخاب رنجی از ستون ها و ردیف‌ها

matrix_name[ start: end ، start : end]
برای انتخاب یک عنصر از ماتریس
matrix_name[row_index ,col_index]

مثال ها : 
m[1]
دسترسی به ردیف دوم
m[: , 1]
دسترسی به ستون دوم
m[:-5, :4]
دسترسی به پنج ردیف آخر از پنج ستون اول 
m[2,5]
دسترسی به یک عنصر از ردیف سوم، ستون ششم 
------------------------------------------
💠دسترسی‌ها با iloc در پانداس 💠
#دسترسی به ستون داتافریم پانداس 
#دسترسی به ردیف داتافریم پانداس 
#دسترسی به عنصر داتافریم پانداس 

برای انتخاب یک ردیف واحد از داتافریم 
df.iloc[row_index]

برای انتخاب رنجی از ردیف‌های داتافریم 
df.iloc[start:end]

انتخاب از ردیف start تا end (غیرشامل end).
------------------
برای انتخاب یک ستون داتافریم 
df.iloc[:, col_index]

برای انتخاب رنجی از ستون‌های داتافریم
df.iloc[:, start:end]

انتخاب ستون‌های start تا end (غیرشامل end).
------------------------------
برای انتخاب رنجی از ردیف‌ها و ستون‌های داتافریم 
df.iloc[start:end, start2:end2]

انتخاب هم‌زمان ردیف‌ها و ستون‌ها.
------------------------------
برای انتخاب یک عنصر از دیتافریم
df.iloc[row_index, col_index]
---------------------------------
💠فرمول میانگین وزندار در نامپی

np.average()

شکل رایج 
avg= np.average(m,weights=w)

پارامترها: 
m= 
آرایه یا لیست مقادیر عددی که باید میانگین از آن‌ها گرفته شود.
ستون داتافریم، ستون ماتریس نیز قابل استفاده هستند

weights= 
آرایه‌ای با طول برابر a برای تعیین وزن هر مقدار.
اگر خالی باشد یا این پارامتر نوشته نشود → میانگین ساده حساب میشود.
اگر وزن را اعمال کنیم حتما باید نام پارامتر وزن یعنی = weights  در فرمول درج شود. 
ستون داتافریم، ستون ماتریس نیز قابل استفاده هستند

returned= 
پیش‌فرض False.
اگر مقدار True بگیرد، علاوه‌بر میانگین، مجموع وزن‌ها نیز برمی‌گردد.
به این صورت:
(array[averages],array[weights sumations]) 

axis = 
محور محاسبه میانگین در آرایه‌های چندبعدی.

پیش‌فرض: محاسبه میانگین روی کل آرایه.
برای محاسبه میانگین تک تک ستون هاaxis =0 
برای محاسبه میانگین تک تک ردیف ها axis= 1 

مثال: 
import numpy as np
import pandas as pd
import random
np.random.seed(1)
p=print
m=np.random.randint(-30,50,size=(10,10))

# مثال برای حالتی که داتا و وزن در میانگین وزن دار، ستون های ماتریس باشند
avg=np.average(m[:,2],weights=m[:,3])

#  مثال برای حالتی که داتا و وزن در میانگین وزن دار، ستون های داتافریم باشند
df=pd.DataFrame(m)
avg2=np.average(df.iloc[:,1],weights= df.iloc[:,2])
p(avg2)
-----------------------------------
⚜️اطلاعات جامع انواع توزیع های مورد نیاز در هوش مصنوعی ⚜️
# انواع توزیع داده ها در نامپی
🟣 = موارد مهمتر

۱) توزیع یکنواخت Uniform Distribution🟣

تعریف ساده
در این توزیع، همه‌ی مقادیر در یک بازه شانس برابر دارند.

شکل رایج

data = np.random.uniform(low, high, size)

توضیح پارامترها: 
low = کمترین مقدار ممکن
high = بیشترین مقدار ممکن
size = تعداد داده‌ها

مثال
import numpy as np
np.random.seed(1)
data = np.random.uniform(-20, 40, 100).astype(int)
print(data)

مناسب برای: 
✅مقداردهی اولیه وزن‌ها در شبکه‌های عصبی ساده
✅شبیه‌سازی داده بدون سوگیری
✅تست الگوریتم‌ها

مناسب نیست برای
❌مدل‌سازی داده‌های واقعی طبیعی
❌داده‌هایی که تمرکز حول یک مقدار دارند
----------------------------------
۲) توزیع نرمال Normal (Gaussian) Distribution🟣

تعریف ساده
اکثر داده‌ها نزدیک میانگین هستند و هرچه دورتر شویم، احتمال کمتر می‌شود.

شکل رایج
data = np.random.normal(mean, std, size)

توضیح پارامترها: 
mean = میانگین
std = انحراف معیار
size = تعداد داده‌ها

مثال
import numpy as np
np.random.seed(1)
data = np.random.normal(0, 1, 100)
print(data)

مناسب برای
✅مدل‌سازی نویز
✅داده‌های طبیعی و انسانی
✅فرض‌های آماری در یادگیری ماشین


مناسب نیست برای:
❌داده‌های کاملاً نامتقارن
❌داده‌های گسسته
-------------------------------------------
۳) توزیع دوجمله‌ای Binomial Distribution

تعریف ساده:
تعداد موفقیت‌ها در چند آزمایش با احتمال ثابت.

شکل رایج:
data = np.random.binomial(n, p, size)

توضیح پارامترها: 

n = تعداد آزمایش‌ها
p = احتمال موفقیت
size = تعداد داده‌ها

مثال

import numpy as np
np.random.seed(1)
data = np.random.binomial(10, 0.3, 100)
print(data)

مناسب برای
✅مسائل طبقه‌بندی
✅مدل‌سازی بله/خیر
✅شبیه‌سازی آزمایش‌های احتمالی

مناسب نیست برای
❌داده‌های پیوسته
❌مقادیر بزرگ با توزیع پیچیده
---------------------------------------
۴) توزیع برنولی Bernoulli Distribution

تعریف ساده: 
هر آزمایش فقط دو حالت دارد: صفر یا یک.

شکل رایج: 
data = np.random.binomial(1, p, size)

توضیح پارامترها: 
p = احتمال رخداد
size = تعداد داده‌ها

مثال
import numpy as np
np.random.seed(1)
data = np.random.binomial(1, 0.6, 100)
print(data)

مناسب برای:
✅برچسب‌های دودویی
✅خروجی مدل‌های باینری

مناسب نیست برای
❌مسائل چندکلاسه
❌داده‌های پیوسته
----------------------------------------
۵) توزیع پواسون Poisson Distribution

تعریف ساده:
تعداد رخدادها در یک بازه‌ی زمانی یا مکانی مشخص.

شکل رایج: 
data = np.random.poisson(lam, size)

توضیح پارامترها: 
lam = میانگین رخداد
size = تعداد داده‌ها

مثال: 
import numpy as np
np.random.seed(1)
data = np.random.poisson(5, 100)
print(data)

مناسب برای: 
✅مدل‌سازی رخدادهای نادر
✅ترافیک، خطا، درخواست‌ها

مناسب نیست برای: 
❌داده‌های پیوسته
❌داده‌هایی با وابستگی شدید
-----------------------------------------
۶) توزیع نمایی Exponential Distribution

تعریف ساده:
مدت زمان انتظار تا رخ دادن یک اتفاق.

شکل رایج: 
data = np.random.exponential(scale, size)

توضیح پارامترها:
scale = میانگین زمان انتظار
size = تعداد داده‌ها

مثال:
import numpy as np
np.random.seed(1)
data = np.random.exponential(2, 100)
print(data)

مناسب برای: 
✅زمان خرابی
✅فاصله بین رویدادها

مناسب نیست برای: 

❌داده‌های متقارن
❌داده‌های محدود بالا و پایین
--------------------------------------------
۷) توزیع لاگ‌نرمال Log-Normal Distribution

تعریف ساده :
وقتی لگاریتم داده نرمال باشد، خود داده لاگ‌نرمال است.

شکل رایج:
data = np.random.lognormal(mean, sigma, size)

توضیح پارامترها:
mean = میانگین لگاریتمی
sigma = انحراف معیار
size = تعداد داده‌ها

مثال:
import numpy as np
np.random.seed(1)
data = np.random.lognormal(0, 1, 100)
print(data)

مناسب برای:
✅درآمد
✅قیمت‌ها
✅رشد نمایی

مناسب نیست برای:
❌داده‌های منفی
❌داده‌های متقارن
-------------------------------------------
۸) توزیع گاما Gamma Distribution

تعریف ساده: 
مدل‌سازی زمان تا چندین رخداد پشت سر هم.

شکل رایج
data = np.random.gamma(shape, scale, size)

توضیح پارامترها
shape = شکل توزیع
scale = مقیاس
size = تعداد داده‌ها

مثال:
import numpy as np
np.random.seed(1)
data = np.random.gamma(2, 2, 100)
print(data)

مناسب برای: 
✅زمان پردازش
✅مدل‌های احتمالی پیشرفته

مناسب نیست برای:
❌داده‌های گسسته
❌داده‌های منفی
--------------------------------------------
۹) توزیع چندجمله‌ای Multinomial Distribution

تعریف ساده: 
تعمیم توزیع دوجمله‌ای برای چند کلاس.

شکل رایج
data = np.random.multinomial(n, pvals, size)

توضیح پارامترها
n = تعداد آزمایش‌ها
pvals = احتمال هر کلاس
size = تعداد نمونه‌ها

مثال
import numpy as np
np.random.seed(1)
data = np.random.multinomial(10, [0.2, 0.5, 0.3], 5)
print(data)

مناسب برای: 
✅مسائل چندکلاسه
✅توزیع برچسب‌ها

مناسب نیست برای:
❌داده‌های پیوسته
❌مسائل رگرسیونی
-------------------------------
⚜️فرمول شمارش دسته ای ستون های پانداس ⚜️
()df[col_name].value_counts 

شکل رایج : 
df["column_name"].value_count()

پارامتر ها : 
پارامتر normalize:
پیشفرض False / بهتر False
اگر True شود، به‌جای تعداد، فراوانی نسبی (درصدی) برمی‌گرداند.
---
پارامتر sort: 
پیشفرض True / بهتر True
اگر False شود، ترتیب مقادیر حفظ می‌شود و مرتب‌سازی انجام نمی‌شود.
---
پارامتر ascending
پیشفرض False / بهتر False
اگر True شود، شمارش‌ها از کم به زیاد مرتب می‌شوند.
---

پارامتر dropna
پیشفرض True / بهتر True
اگر فولز شود، مقادیر خالی هم در شمارش لحاظ می‌شوند.
-------------------------
نکات کلیدی: 
🔰این تابع inplace ندارد پس خودِ DataFrame یا ستون اصلی را تغییر نمی‌دهد و نتیجهٔ محاسبه را در یک شیء جدید برمی‌گرداند.
🔰روی یک ستون (Series) اجرا می‌شود نه روی کل DataFrame.
🔰خروجی به‌صورت Series است و می‌توان آن را در متغیر ذخیره کرد یا به ستون جدید تبدیل کرد.

------------------------------------
random.choices()
برای ساخت لیست از یک بازه یا لیست مرجع دلخواه 
از این لیست میتوان بعدها در ساخت آرایه استفاده کرد

شکل رایج
random.choices(population, k=1)

پارامترها:

🔹 population → 
مجموعه‌ای که از آن انتخاب تصادفی انجام می‌شود. می‌تواند لیست، تاپل یا رنج باشد.
مثال:
gender = ["M", "F", None]
range(-20, 40)

🔹 k → تعداد نمونه‌هایی که می‌خواهیم انتخاب کنیم (تعداد ردیف‌ها).

🔹 weights → وزن احتمال هر عنصر برای انتخاب (اختیاری). مثلا بعضی آیتم‌ها شانس بیشتری برای انتخاب داشته باشند.
--------------------
سایر پارامترها:

🔹 cum_weights → 
وزن تجمعی عناصر، 
جایگزین weights می‌شود (اختیاری).


مثال:
import random
random.seed(2)  # برای ثابت ماندن خروجی
gender = random.choices(population=["M","L",None], weights=[0.8,0.1,0.1], k=80)

مثال بعدی 
import numpy as np
import random
random.seed(1)
L= random.choices(range(-10,50), k= 8)
# لیست ساخته شد
v= np.array(L)
# آرایه ساخته شد

نکات مهم:
- خروجی این تابع همیشه لیست است و ممکن است حاوی مقادیر تکراری باشد؛ یونیک بودن تضمین نمی‌شود.
- این تابع متعلق به کتابخانه‌ی استاندارد راندوم پایتون است.
- نیازی به کتابخانه دیگری مانند نامپی یا پانداس ندارد.
----------------------------------------
random.sample()
مشابه تابع بالا ولی برای ساخت لیست های یونیک

random.sample(population, k=1)

پارامترها:

🔹 population → 
مجموعه‌ای که از آن انتخاب تصادفی انجام می‌شود. می‌تواند لیست، تاپل یا رنج باشد.
مثال:
gender = ["M", "F", None]
range(-20, 40)

🔹 k → تعداد نمونه‌هایی که می‌خواهیم انتخاب کنیم (تعداد ردیف‌ها).
مثلا
k=عدد دلخواه
k=len(df) برای تولید لیست به اندازه طول ستون داتافریم

مثال:
import random
random.seed(2)  # برای ثابت ماندن خروجی
L = random.sample(population=range(-10,50) k=30)

نکات مهم :
خروجی این تابع همیشه یونیک است؛ هیچ عدد یا مقدار تکراری نخواهد بود.
- این تابع متعلق به کتابخانه‌ی استاندارد random پایتون است.
- نمی‌توان از weights یا cum_weights استفاده کرد.
- نیازی به کتابخانه دیگری مانند NumPy یا pandas ندارد.
----------------------------
np.random.choice()
تابع مشابه دوتای بالایی این بار از کتابخانه نامپی 
برای تولید آرایه

شکل رایج:
np.random.choice(a, size=, replace=False)

پارامترها:
🔹 a → آرایه یا مجموعه‌ای که از آن انتخاب تصادفی انجام می‌شود. می‌تواند NumPy array، لیست یا range باشد.
مثال:
arr = np.array([10, 20, 30, 40, 50])
range(10000, 100000)

🔹 size → تعداد نمونه‌هایی که می‌خواهیم انتخاب کنیم (تعداد ردیف‌ها).

🔹 replace → پیشفرض True. اگر False شود، نمونه‌ها بدون تکرار انتخاب می‌شوند (یونیک خواهند بود).

🔹 p → آرایه‌ای از احتمال‌های انتخاب هر عنصر (اختیاری). مجموع آن باید 1 باشد.

مثال عملی:
import numpy as np
import pandas as pd
np.random.seed(2)  # برای ثابت ماندن خروجی
id = np.random.choice(range(10000,100000), size=len(df), replace=False)
print(id)
# یک ستون از آیدی 5 رقمی یونیک برای داتافریم پانداس ساختیم

نکات مهم:
- با replace=False → خروجی یونیک است، بدون تکرار.
- با replace=True → خروجی ممکن است تکراری باشد.
- این تابع متعلق به کتابخانه‌ی نامپی است و برای
تولید نمونه‌های تصادفی سریع و حرفه‌ای مناسب است.
-----------------------------------------
🔶مراحل بررسی عددی توزیع داده‌ها🔶

1️⃣ گام ۱: خودش چند بخش دارد 

⏪الف ) شناخت نوع ستون ها(عددی (int, float) یا  دسته ای (object))
 df.info()
df.select_dtypes(include="object")
⏪ب)تایید ستون های دسته ای از روی  تعداد مقادیر یکتا 
df[ستون احتمالا دسته ای].nunique()
🔰ستون های float و int عددی هستند و ستون های object اغلب دسته ای هستند(شرط اول دسته ای بودن = object بودن)
🔰ستون های object هر چه غیر یونیک تر باشند(تکراری تر) دسته ای ترند پس باید nunique آنها نسبت به تعداد ردیف ها کم باشد(شرط دوم دسته ای بودن= تکرار بالا)
⏪ج) پی بردن به مقادیر خالی در کل داتافریم 
df.isna().sum()
--------------------------------
2️⃣ گام ۲: بررسی توزیع ستون‌های دسته‌ای

⏪شمارش فراوانی ستون های دسته ای
df["gender"].value_counts()

می‌فهمی:
هر مقدار چند بار تکرار شده
آیا یک دسته غالب است یا نه
---
⏪بررسی نسبت‌ها (خیلی مهم)ستون های دسته ای 
df["gender"].value_counts(normalize=True)

می‌فهمی:
هر دسته چند درصد از کل داده را تشکیل می‌دهد
-----------------------------------
3️⃣ گام ۳: بررسی توزیع ستون‌های عددی
با ()describe() ,  median و skew()

numcols = df.select_dtypes(include="number")
⏩p(df.describe())
⏩p(numcols.median())

بررسی چولگی ساده (بدون نمودار)
چولگی (Skewness) یعنی عدم تقارن توزیع داده‌ها حول مرکز آن‌ها (میانه یا میانگین).
⏩df["age"].skew()

نزدیک ۰ → نسبتاً متقارن
مثبت → دم راست بلند
منفی → دم چپ بلند
'''
