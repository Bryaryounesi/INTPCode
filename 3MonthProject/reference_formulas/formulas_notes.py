'''
import pandas as pd
شیوه درست ایمپورت کردن پانداس
---------------------------
pd.Series()
برای ساخت سری از لیست

شکل رایج
s= pd.Series(data)

data :
لیستی از مقادیر عددی یا غیر عددی

سایر پارامتر ها 

index =
 لیستی از ایندکس های عددی یا غیر عددی
 
پیش فرض ایندکس عددی و بهتر

dtype
نوع داده هایی که میخاهیم سری داشته باشد
پیشفرض :پانداس خودش داده ها را میشناسد 

name:
نامی برای سری .چندان ضرورتی ندارد

copy
پیشفرض فولز و بهتر 
اگر ترو باشد یک کپی از داده ها ساخته میشود
---------------------------------
pd.DataFrame()
شکل رایج 
df = pd.DataFrame(data)

- ⏩data : منبع داده برای ساخت داتافریم
dict، list ،numpy array ، Series یا  DataFrame دیگر / None پیشفرض 

- index : برچسب (label) ردیف‌ها
list، RangeIndex، یا Index دیگر
پیش‌فرض None(به‌صورت خودکار از 0 شروع می‌شود)
- ⏩columns:  نام ستون‌ها
 پیش‌فرض None / لیستی از نام ستون ها / (در صورت dict، از کلیدها استفاده می‌شود)

- dtype : 
تعیین نوع داده برای تمام ستون‌ها.
 None پیش‌فرض / float, int, str, object

- copy: 
آیا داده کپی شود یا به‌صورت مرجع استفاده شود
پیش‌فرض False / اگر ترو باشد، دادهٔ ورودی کاملاً کپی می‌شود
مثلا
 df2 = pd.DataFrame(df1, copy= True)
----------------------------------
df.select_dtypes(include/exclude)
برای جدا کردن یکجای ستون های دارای ویژگی مشترک
و ساخت داتافریم های جدید 
مثلا
ساخت داتافریم از ستون های عددی
dfnum=df.select_dtypes(include="number")

ساخت داتافریم از ستون های غیر عددی
dfunnum=df.select_dtypes(exclude="number")

برای ساخت لیست ستون های دارای ویژگی مشترک
برای گروهبندی یا برای لوپ فور
numcols=df.select_dtypes(include="number").columns
unumcols= df.select_dtypes(exclude="number").columns
اگر بخواهیم برخی ستون ها را از این مجموعه خارج کنیم
unumcols= df.select_dtypes(exclude="number").drop("name",axis=1).columns

استفاده در لوپ فور
for i in numcols:

استفاده در گروهبندی
df.groupby(by="col1")[numcols].mean().round()
------------------------------------
df.fillna
شکل رایج 
df.fillna(inplace = True)

پرکردن یکجا برای ستون های عددی با توابع آماری
>>> dfm.fillna(dfm.median/mode/mean(numeric_only=True),inplace=True)

پرکردن یکجای ستون های رشته و آپجکت با پرتکرارترین مقدار هر ستون
>>> dfm.fillna(dfm.select_dtypes(include=["string","object"]).mode().iloc[0],inplace=True)

- value: مقدار جایگزین برای NaN
 پیشفرض None / scalar, dict, Series, or DataFrame

- method: روش پر کردن مقادیر خالی
پیشفرض None/ '
ffill', 'bfill', 'pad', 'backfill'

- axis: مشخص می‌کند ستون حذف شود یا ردیف 
پیش فرض 0 / برای ردیف ها 0 و برای ستون ها 1 است


- inplace: انجام عملیات روی دیتافریم اصلی
    False پیشفرض / True 

- limit: 
 حداکثر تعداد پر کردن مقادیر خالی در هر ستون یا ردیف

پیشفرض: None \ عدد دلخواه 
---------------------------------------
df.bfill()
df.ffill()
این دو جایگزین فرمول زیر هستند
df.fillna(method="ffill"/bfill,inplace=True)
 گویا این فرمول در نسخه های آینده پانداس از کار خواهد افتاد
 و دیگر متد به عنوان یک پارامتر در فیلنا کار نخواهد کرد
دو فرمول جایگزین نیازی به پارامتر اینپلیس هم ندارند و خودشان 
تغییرات را بر داتافریم اعمال می کنند.  
--------------------------------------
 df.dropna
شکل رایج 
df.dropna(ignore_index= True, inplace = True) برای حذف ردیف ها 
df["col_name"].dropna(ignore_index= True, بقیه پارامترها اغلب بلااستفاده) برای حذف سلول

ignore_index = برای جلوگیری از به هم ریختن ایندکس ها بعد از حذف
پیشفرض False / بهتر True

- inplace:
 انجام عملیات روی دیتافریم اصلی
   False پیشفرض/ True
   
- axis: جهت حذف
پیشفرض 0/مقدار: 0 (سطرها), 1 (ستون‌ها)

-------کمتر رایج -------
- how   شرط حذف
 all  /پیشفرض 'any' 
   اگر any
   باشد یک مقدار خالی کافیست
   برای حذف ستون یا ردیف 
   ولی در all 
   همه مقادیر باید خالی باشند.

- thresh: 
حداقل تعداد مقادیر غیر خالی در سطر یا ستون ها 
پیشفرض None/عدد 

- subset:
 زیرمجموعه ستون‌ها برای بررسی
پیشفرض None /مقدار list of column names

----------------------------------------
df.drop()

شکل رایج 
df.drop("colomn_name", ignore_index= True,axis = 1 , inplace = True)
value = "colomn_name"/["col1","col2",...]
برای حذف ردیف ها:
ایندکس یک ردیف  یا لیست ایندکس های چند ردیف

ignore_index= True بهتر / False پیشفرض
axis = یک برای حذف ستون / صفر برای حذف ردیف
inplace = True بهتر / False پیشفرض
-------------------------------------------
read_csv()
شکل رایج 
df = pd.read_csv(path)
# هر پارامتری پیش فرض دارد میتواند نوشته نشود
-path = r"مسیر فایل"
- index_col = "column_name"/None پیش فرض 

-----کمتر مورد استفاده---------

-nrows = تعداد ردیف
-use_cols = انتخاب ستون ها ، حالت ها در پایین

۱. با ایندکس حروفی اکسل:
usecols=["A", "C"]
۲. با نام خود ستون‌ها:
usecols=["Name", "Age"]
۳. با ایندکس عددی (شروع از ۰):
usecols=[1, 2]  
# ستون‌های دوم و سوم
۴. با بازه ایندکسی عددی:
usecols=range(1, 4) 
 # ستون‌های دوم، سوم و چهارم
۵. با بازه حروف اکسل:
usecols="A:C" 
 # تمام ستون‌های از A تا C
۶. ترکیب چندین بازه:
usecols="A:C, E:G"  # ستون‌های A-C و E-G
--------------------------------------------------
read_excel()
شکل رایج
dfall = pd.read_excel(path,sheet_name=None)
df2 = pd.read_excel(path,sheet_name=1)

-path = r
"مسیر فایل"
-sheet_name = "names"/بهتر/پیشفرض 0 None

- usecols = حالت ها در بالا توضیح داده شده اند
- nrows = عدد
- index_col = "column_name"/ None پیش فرض 

----------------------------------------
df.reset_index()
برای حذف کردن یا برداشتن ستون ایندکس(تبدیل آن به یک ستون عادی)
 
برای حذف کامل ستون ایندکس
df= df.reset_index(drop=True)

برای تبدیل ستون ایندکس به ستون عادی
df.reset_index(inplace=True)

---------------------------------------
df.sample()
شکل رایج
df= df.sample(frac =1,ignore_index=True,random_state=1)

n = عدد ردیف‌هایی که می‌خواهیم انتخاب شوند 
/پیشفرض None (در این حالت باید frac تعیین شود)

>>> frac = نسبت درصدی نمونه‌گیری (مثلاً 0.2 یعنی ۲۰٪ از داده‌ها) /
پیشفرض None (در این حالت باید ان تعیین شود)
اگر فراک دادیم نیازی به إن نیست و برعکس

random_state = (عددی ثابت برای تکرارپذیری نمونه (بهتر است مقدار ثابت بدهیم
شبیه سید در فیکر 

ignore_index = بهتر True / پیشفرض False (شماره‌گذاری مجدد ایندکس‌ها)

-----کمترکاربردی-----
replace = پیشفرض False (بدون تکرار) / True (با تکرار مجاز)
weights = ستون یا آرایه‌ای برای تعیین احتمال انتخاب هر ردیف /
پیشفرض None (احتمال برابر)
axis = پیشفرض بهتر و برابر 0 (نمونه‌گیری از ردیف‌ها) / 1 برای ستون‌ها

--------------------------------------
sort_values()
شکل رایج 
df.sort_values(by="col_name",ignore_index=True, inplace=True)

by = "col_name"/["col1","col2",...]
ignore_index = بهتر True/ پیشفرض False
inplace = بهتر True/ پیشفرض False
ascending = پیشفرض ترو وبهتر برای صعودی/ فولز برای نزولی/ 
[True,True  یا بالعکس برای حالاتی که بای نیز ستون است]

axis = پیشفرض بهتر و برابر 0/ 1 بد و سبب ارور 

---------------------------
to_excel()
شکل رایج 
df.to_excel(expath,sheetname="sh1",index=False)

-expath
-sheet_name= پیشفرض sheet1 .../"sh1"بهتر/2
-index = True پیشفرض /False بهتر 

-----------------------------------------
pd.excelWriter()
شکل رایج
with pd.ExcelWriter(expath,mode='a',if_sheet_exists= "new") as writer:
    df1.to_excel(writer,sheet_name="sh1",index=False)
  
    df2 .....

expath
mode = "a"
if_sheet_exists= "new"بهتر /"replace"/"error"

-----------------------------------------------
pd.concat
شکل رایج
dfall = pd.concat([df1,df2,...],ignore_index=True)

objs = [,] / (,)
ignore_index = False بهتر/ پیشفرض True 
axis = پیش فرض 0 / 1

----------------------------------
pd.merge()
شکل رایج
dfm = pd.merge(L,R,how = "joinType",on="colname")

L & R = داتافریم های چپ و راست
نوشتن خود این دو حرف لازم نیست فقط نام داتافریم ها را بنویس
how = "outer" بهتر/"inner"/"right"/"left"
on = نام ستون" یا ["ستون های مشترک"] بین دو"داتافریم

left_on= نام متفاوت ستون مشترک در داتافریم چپ
right_on = نام متفاوت ستون مشترک در داتافریم راست 
------------------------------
df.groupby()

🧩 شکل رایج
df.groupby(by="col_name", as_index=False)


⚙️ پارامترها
by = "col_name" / ["col1","col2", ...]
ستونی که بر اساس آن داده‌ها گروه‌بندی می‌شوند (می‌تواند یک یا چند ستون باشد).

as_index = 
بهتر False / پیش‌فرض True
اگر True: ستون گروه‌بندی به اندیس تبدیل می‌شود.
اگر False: ستون گروه‌بندی در خروجی به‌صورت ستون عادی باقی می‌ماند (قابل توصیه‌تر برای کارهای بعدی).

sort = False /  پیش‌فرض True بهتر
اگر True: گروه‌ها بر اساس نامشان مرتب می‌شوند.
اگر False: ترتیب داده‌ها حفظ می‌شود و کمی سریع‌تر اجرا می‌شود.

------کمتر رایج در کار ما --------

dropna = بهتر True / پیش‌فرض True
اگر True: ردیف‌هایی که مقدار گروهشان NaN است حذف می‌شوند.
اگر False: آن گروه‌ها هم نگه داشته می‌شوند.

axis = پیش‌فرض بهتر و برابر 0 / 1 بد و سبب ارور در بیشتر موارد
محور گروه‌بندی؛ معمولاً 0 برای سطرها استفاده می‌شود (درست‌ترین گزینه).


level = None / سطحی از اندیس در DataFrame چندسطحی (MultiIndex)
اگر اندیس چندسطحی داری، تعیین می‌کند در کدام سطح گروه‌بندی انجام شود.

observed = بهتر False / پیش‌فرض False
فقط در داده‌های دسته‌ای (categorical) کاربرد دارد؛ اگر True فقط گروه‌های واقعی را نگه می‌دارد.

group_keys = بهتر True / پیش‌فرض True
مشخص می‌کند نام گروه‌ها در خروجی (مثل هنگام استفاده از apply()) حفظ شوند یا نه.
---------------------------------------
import matplotlib.pyplot as plt
شیوه اینمپورت کردن مت پلات لیب
-------------------------------
🔆 رایج‌ترین توابع
matplotlib.pyplot

# توابع plt

✅plot✴️
کاربرد: رسم نمودار خطی ساده یا چند خطی برای نمایش رابطه بین دو سری داده
🧩 شکل رایج

plt.plot(x, y)

⚙️ پارامترها

x: مقادیر محور افقی
پیش‌فرض: ندارد
بهتر: مقادیر واقعی داده‌ها
y: مقادیر محور عمودی
پیش‌فرض: ندارد
بهتر: مقادیر واقعی داده‌ها

: مثلا اگر ایکس و ایگریک ستون داتافریم باشند
x= df["country"] 
y=df["math"]
اگر ایکس یک لیست باشد، خود ایگریک ساخته میشود
plt.plot([list])

color: رنگ خط
پیش‌فرض: 'blue'
بهتر: رنگ دلخواه برای خوانایی

linestyle: نوع خط
پیش‌فرض: '-'
بهتر: '-' یا '--' بسته به نیاز
چند مورد دیگه هم داره 
marker: علامت نقاط داده
پیش‌فرض: None
بهتر: 'o' برای نمایش نقاط
میتوان از * یا هر علامت دلخواه دیگری هم استفاده کرد
label:
 برچسب نمودار برای تابع لجند
پارامتر لیبل در تابع پلات برای نام‌گذاری هر خط در نمودار استفاده می‌شود
تا بعداً در لجند(راهنما) نشان داده شود.
وقتی چند خط روی یک نمودار رسم می‌کنی، 
با لیبل می‌توانی برای هرکدام یک برچسب بگذاری 
در راهنما مشخص شود هر خط مربوط به کدام داده است.

plt.plot(x, y, label="Math Scores")


پیش‌فرض: None
بهتر: برچسب معنی‌دار وقتی چند خط داریم
-----------------------------
✅scatter✴️
کاربرد: رسم نمودار پراکندگی برای نشان دادن توزیع نقاط و همبستگی بین دو متغیر

🧩 شکل رایج
plt.scatter(x, y)

⚙️ پارامترها

x و y: مختصات نقاط
پیش‌فرض: ندارد
بهتر: داده‌های واقعی
با یک لیست واحد به جای x و y ارور میدهد
باید لیست را به جای y بدهیم 
و یک x متناسب با آن طبق فرمول زیر بسازیم

plt.scatter(range(len(y)), y)

color: رنگ نقاط
پیش‌فرض: 'blue'
بهتر: رنگ دلخواه

marker: شکل نقاط
پیش‌فرض: 'o'
بهتر: 'o' یا 'x'

s: اندازه نقاط
پیش‌فرض: 20
بهتر: متناسب با نمودار

label: برچسب برای legend
پیش‌فرض: None
بهتر: برچسب معنی‌دار
----------------------
✅bar✴️
کاربرد: رسم نمودار میله‌ای برای مقایسه دسته‌ای داده‌ها

🧩 شکل رایج
plt.bar(x, height,edgecolor="black")

⚙️ پارامترها

x: موقعیت دسته‌ها روی محور افقی
پیش‌فرض: ندارد
بهتر: بر اساس داده‌ها

height: ارتفاع ستون‌ها
# همان محوایگریک ولی با اسمی دیگر

 پیش‌فرض: ندارد
بهتر: مقادیر واقعی

با یک لیست واحد به جای x و y ارور میدهد
باید لیست را به جای y بدهیم 
و یک x متناسب با آن طبق فرمول زیر بسازیم


color: رنگ ستون‌ها
پیش‌فرض: 'blue'
بهتر: رنگ دلخواه

width: پهنای ستون‌ها
پیش‌فرض: 0.8
بهتر: 0.5 تا 0.9

label: برچسب برای legend
پیش‌فرض: None
بهتر: برچسب معنی‌دار
edgecolor= رنگ لبه میله ها 
پیش‌فرض: None
بهتر: 'black'
----------------------------
hist✴️
کاربرد: رسم هیستوگرام برای نمایش توزیع فرکانس داده‌ها

🧩 شکل رایج
plt.hist(data, edgecolor='black')

⚙️ پارامترها

data: داده‌ها برای هیستوگرام
پیش‌فرض: ندارد
بهتر: آرایه واقعی داده‌ها

bins: تعداد بازه‌ها
پیش‌فرض: 10
بهتر: بسته به پراکندگی داده‌ها

color: رنگ ستون‌ها
پیش‌فرض: 'blue'
بهتر: رنگ دلخواه

edgecolor: رنگ مرز ستون‌ها
پیش‌فرض: None
بهتر: 'black'

alpha: شفافیت نمودار
پیش‌فرض: 1.0
بهتر: 0.5 تا 0.8 برای نمودارهای چندگانه
---------------------------
xlabel / ylabel✴️
کاربرد: تعیین عنوان محور افقی و محور عمودی نمودار

🧩 شکل رایج

✅plt.xlabel("X-axis label")
✅plt.ylabel("Y-axis label")

⚙️ پارامترها
xlabel / ylabel: عنوان محور
پیش‌فرض: ''
بهتر: متن مناسب داده‌ها
---------------------------
✅title✴️
کاربرد: تعیین عنوان کلی نمودار
🧩 شکل رایج

plt.title("Chart Title")

⚙️ پارامترها
متن عنوان
پیش‌فرض: ''
بهتر: عنوان معنی‌دار

legend✴️
کاربرد: نمایش راهنمای نمودارها (label های مختلف)

🧩 شکل رایج
plt.legend(loc='best')

⚙️ پارامترها

loc: محل نمایش legend
پیش‌فرض: 'best'
بهتر: 'upper right' یا 'lower left' بسته به نمودار
------------------------------
show✴️
کاربرد: نمایش نمودار نهایی
🧩 شکل رایج

plt.show()
⚙️ پارامترها
ندارد
---------------
savefig()
تابعی برای ذخیره نمودار در فایل های تصویری

شکل رایج:
plt.savefig(fname="chartname.png",dpi=300)

پارامترها:
dpi:
بیانگر وضوح تصویر
300 بهتر

fname: ""
نام فایل همراه با پسوند فرمت درون دبل کوت
مثلا name.png 
-----------------------------
import numpy as np
شیوه درست ایمپورت کردن نامپی 
-------------------------
فرمول ساخت آرایه 
a= np.array(data)
data = 
اغلب لیست،گاهی تاپل
در ساخت ماتریس، لیستی از لیست ها

دیگر پارامتر ها :
dtype=
پیشفرض مطابق با لیست اولیه/int,float
گاهی str یا bool
-----------------------
.tolist()
برای تبدیل مجدد آرایه(بردار/ماتریس) به لیست

مثلا
new = a.tolist()
-------------------
v= np.array()
فرمول ساخت بردار

شکل رایج 
v= np.array(data)

data = 
لیستی از مقادیر عددی مثبت یا منفی 
تعداد پارامترها بدون محدودیت
--------------------------
length= np.linalg.norm(v)
فرمول محاسبه طول بردار

v= 
برداری که از فرمول بردار محاسبه شده
--------------------------------
m= np.array([[list1],[list2],[list3]])
فرمول ماتریس (آرایه ای با لیستی از لیست ها)
-------------------------
⚜️توابع آماری روی بردار‌ها ⚜️

np.size(v)  تعداد 
برابر با len(v)
np.sum(v)  مجموع 
np.mean(v)  میانگین
np.std(v)  انحراف معیار 
np.var(v)  واریانس
np.max(v)  بیشینه
np.min(v)  کمینه
np.median(v)  میانه 
p(np.mean(v)) 
نامپی تابع mode ندارد
---------------------------------------
⚜️ توابع آماری روی ماتریس ⚜️

# مجموع
# ------------------------------
np.sum(m)        
    # مجموع کل ماتریس
np.sum(m, axis=0)  
  # مجموع ستون‌ها
np.sum(m, axis=1)  
  # مجموع ردیف‌ها

# میانگین
# ------------------------------
np.mean(m)        
    # میانگین کل ماتریس
np.mean(m, axis=0)  
  # میانگین ستون‌ها
np.mean(m, axis=1)  
  # میانگین ردیف‌ها

# ------------------------------
# انحراف معیار
# ------------------------------
np.std(m)        
     # انحراف معیار کل ماتریس
np.std(m, axis=0) 
    # انحراف معیار ستون‌ها
np.std(m, axis=1)  
   # انحراف معیار ردیف‌ها
# ------------------------------
# واریانس
# ------------------------------
np.var(m)     
        # واریانس کل ماتریس
np.var(m, axis=0) 
    # واریانس ستون‌ها
np.var(m, axis=1)
     # واریانس ردیف‌ها
# ------------------------------
# بیشینه
# ------------------------------
np.max(m)     
        # بیشینه کل ماتریس
np.max(m, axis=0)  
   # بیشینه ستون‌ها
np.max(m, axis=1)  
   # بیشینه ردیف‌ها
# ------------------------------
# کمینه
# ------------------------------
np.min(m)          
   # کمینه کل ماتریس
np.min(m, axis=0)  
   # کمینه ستون‌ها
np.min(m, axis=1)  
   # کمینه ردیف‌ها
# ------------------------------
# میانه
# ------------------------------
np.median(m)      
    # میانه کل ماتریس
np.median(m, axis=0) 
 # میانه ستون‌ها
np.median(m, axis=1)
  # میانه ردیف‌ها
---------------------------------
تعداد عناصر ماتریس 
m.size             # تعداد کل عناصر
m.shape[0]         # تعداد ردیف‌ها
m.shape[1]         # تعداد ستون‌ها

---------------------------------
🔆شرط تحقق ضرب ماتریسی به زبان پایتون 🔆
if A.shape[1] == B.shape[0]:
    C = A @ B
    print(C)
else:
    print("ابعاد سازگار نیستند، ضرب انجام نمی‌شود")

--------------------------------------
c= A @ B یا c=np.dot(A, B)
فرمول ضرب ماتریسی
--------------------------------------
⚜️توابع نامپی برای ساخت ماتریس و بردار با ابعاد دلخواه⚜️
🟣 توابع کاربردی تر از نظر شخصی
 
برای ماتریس:
np.zeros((m,n)) 
→ ماتریس m×n پر از صفر
مثال: A = np.zeros((3,4))

برای بردار:
np.zeros(n) 
بردار n خانه‌ای پر از صفر
مثال: v = np.zeros(5)
-----------------------------
برای ماتریس:
np.ones((m,n)) 
 ماتریس m×n پر از یک
مثال: A = np.ones((2,5))

برای بردار :
np.ones(n) 
بردار n خانه‌ای پر از یک
مثال: v = np.ones(4)
----------------------------------------------
برای ماتریس:
np.eye(n)
 ماتریس واحد n×n
یک ماتریس مربعی با والیوی غالبا صفر میسازد ولی والیوهایی از یک هم دارد  که به صورت قطری در ماتریس امتداد دارند. 
مثال: I = np.eye(3)

برای بردار :
برای بردار قابل استفاده نیست
-----------------------------------------
برای ماتریس:
np.full((m,n), value) 
 ماتریس m×n پر از یک عدد دلخواه
این عدد دلخواه جای value قرار میگیرد. 
مثال: A = np.full((2,3), 7)

برای بردار:
🟣np.full(n, value) 
 بردار n خانه‌ای پر از عدد دلخواه
تعداد پارامتر = n 
عدد دلخواە = value
مثال: v = np.full(6, 7)
-------------------------------
برای ماتریس:
np.arange(total_elements).reshape(m,n) 
 ماتریس با اعداد ترتیبی
مثال: A = np.arange(12).reshape(3,4)
باید حاصلضرب اعداد درون پرانتز reshape برابر با عدد arange باشد در غیر این صورت خطا می‌دهد. اگر این قانون رو رعایت کنیم ماتریسی میسازد با والیوهایی  از عدد ۱ تا عدد درون arange

برای بردار:
🟣np.arange(n) 
 بردار با اعداد ترتیبی از یک تا n 
مثال:
v1 = np.arange(5)   بازه از صفر تا 4
v2=np.arange(2,10)   بازه از 2 تا 9
v3=np.arange(3,10,step=3)  بازه از 3 تا 9 با گام حرکتی سه تایی
---------------------------------------
برای ماتریس
A= np.linspace(start,stop,num).reshape(m,n)
تولید آرایه در یک رنج دلخواه ولی با یک گام حرکتی مشخص نه تصادفی

پارامتر ها:
start
شروع بازه
stop:
پایان بازه
num
تعداد اعداد تولید شونده
پیشفرض 50/ بهتر عدد دلخواه 

m , n  ابعاد ماتریس که حاصلضرب آنها باید برابر num باشد
-----------------
سایر پارامترها
dtype=
پیشفرض float
/int,bool,str
اغلب نتایج تابع به صورت اعشاری میشوند 
پس با این پارامتر و انتخاب int میتوان اعداد صحیح بدست آورد 
-----------
مثال : 
A= np.linspace(0,10,6,dtype=int).reshape(2,3)


برای بردار: 
v= np.linspace(start,stop,num)
مثال :
v= np.linspace(0,8,5,dtype=int)
حتما باید سه عدد آغار و پایان بازه و num درج شوند
------------------------------

برای ماتریس:
🟣 np.random.rand(m,n) 
 ماتریس m×n با اعداد تصادفی و اعشاری  بین 0 و 1 
مثال:
 A = np.random.rand(2,3).round()
البته اعداد اعشاری طولانی به ما میدهد که بهتر است آنها را با تابع round() کرد کنیم.‌

برای بردار:
🟣np.random.rand(n) 
 بردار n خانه‌ای با اعداد تصادفی بین 0 و 1
تعداد اعداد بین صفر و یک = n 
مثال: v = np.random.rand(4)
------------------------------------
برای ماتریس:
🟣 np.random.randint(min,max,size=(m,n))
ماتریس با اعداد تصادفی در رنج دلخواه 
min و max
دو آستانە  رنج دلخواه ما هستند 
چهار چوب ماتریس را از نظر تعداد سطر و ستون مشخص می‌کند
تعداد سطر = m 
تعداد ستون = n 

مثال: 
 A= np.random.randint(5, 20, size=(2,5))
print(A)

برای بردار:
v= np.random.randint(5, 20, size=n)
مثال:
v= np.random.randint(5,20,size=8)
-------------------------------------
 ⚜️توابع کمکی نامپی برای تبدیل شکل بردار⚜️

reshape(n,1) 
 تبدیل بردار به ستون برای ضرب ماتریسی
مثال: v_col = np.arange(3).reshape(3,1)
---------------------
reshape(1,n) 
 تبدیل بردار به سطر برای ضرب ماتریسی
مثال: v_row = np.arange(3).reshape(1,3)
---------------------------------------
ساخت بردار در رنج دلخواه با اعداد تصادفی و تابع
random.choices 

import numpy as np
import random
random.seed(1)
L= random.choices(population=range(10,50), k= 8)
v= np.array(L)

------------------------------
فرمول دترمینان ماتریس 
m_det = np.linalg.det(m)

------------------------------
فرمول معکوس ماتریس 
m_inv = np.linalg.inv(m)

----------------------------
شرط  معکوس داشتن ماتریس (صفر نبودن دترمینان) 

if m1_det!=0:
    m1_inv=np.linalg.inv(m1)
    p("m1 invers:",m1_inv)
else:
    p("matrix has no invers!")
---------------------------- 
 ✴️چهار ابزار پایه‌ایِ جبرخطی موردنیاز در هوش مصنوعی برای حل دستگاه های معادلات ✴️

این چهار ابزار solve, inv, istsq و pinv هستن . 
هر کدام برای چه حالاتی مناسب و برای چه حالاتی مناسب نیستند: 

🟢 solve
تعداد معادلات = تعداد مجهول‌ها (دستگاه های مربعی)✔
ماتریس قابل معکوس ✔
ماتریس منفرد (بدون معکوس و با دترمینان صفر)✖
تعداد معادلات > تعداد مجهول‌ها(دستگاه های بیش تعداد) ✖
تعداد معادلات < تعداد مجهول‌ها(دستگاه های کم تعداد) ✖
------------------
فرمول solve :
x= np.linalg.solve(A, b)

مثال : 

مثال
می‌خواهیم دستگاه زیر را حل کنیم:
2x + y = 5
x - y = 1

import numpy as np
# ماتریس ضرایب
A = np.array([[2, 1],
              [1, -1]])

# بردار سمت راست معادلات
b = np.array([5, 1])

# حل دستگاه
x = np.linalg.solve(A, b)
print(x)
-------------------------
🟢inv
معکوس صریح ماتریس مربعی ✔
ماتریس نزدیک به منفرد و منفرد ✖
دستگاه بیش‌تعداد ✖
دستگاه کم‌تعداد ✖
-----------
فرمول inv :

A_inv= np.linalg.inv(A)

مثال : 
import numpy as np

# ماتریس مربعی
A = np.array([[2, 1],
              [1, -1]])

# بردار سمت راست
b = np.array([5, 1])

# محاسبه معکوس ماتریس
A_inv = np.linalg.inv(A)

# ضرب معکوس در بردار سمت راست
x = A_inv @ b
print(x)
-----------------------
🟢lstsq
ال-اِس-تی-اِس-کیو
 
تعداد معادلات > تعداد مجهول‌ها ✔
داده‌های نویزی ✔
دستگاه مربعی سالم ✖
ماتریس منفرد ✖
------------
فرمول lstsq : 
x = np.linalg.lstsq(A, b, rcond=None)[0]

مثال :

import numpy as np

# ماتریس ضرایب
A = np.array([[1, 1],
              [2, 1],
              [3, 1]])
# بردار سمت راست معادلات
b = np.array([2, 3, 4])

# حل با روش کمترین مربعات
x = np.linalg.lstsq(A, b, rcond=None)[0]
print(x)
------------------------
🟢pinv
شبه‌معکوس مور-پنروز
اسم دیگر pseudo-inverse

تعداد معادلات < تعداد مجهول‌ها ✔
ماتریس منفرد ✔
دستگاه غیرمربعی ✔
دستگاه مربعی سالم ✖
-------------
فرمول pinv :
x = np.linalg.pinv(A) @ b

مثال :
import numpy as np
# ماتریس ضرایب
A = np.array([[1, 1, 1],
              [2, 1, 1]])

# بردار سمت راست
b = np.array([3, 4])

# حل با شبه‌معکوس
x = np.linalg.pinv(A) @ b
print(x) 
 
'''
