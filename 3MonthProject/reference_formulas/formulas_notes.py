'''
....That's my pandas,numpy and matplotlib.pyplot Cheatsheet...
---------------------------------------------------------
import pandas as pd
شیوه درست ایمپورت کردن پانداس
---------------------------
pd.Series()
برای ساخت سری از لیست

شکل رایج
s= pd.Series(data)

data :
لیستی از مقادیر عددی یا غیر عددی
------------------------
p(Serie_name.value)
با پرینت عبارت بالا میتوان داتای سازنده سری را دید
مثلا:
p(s.values)   به صورت آرایه
p(s.values.tolist()) به صورت لیست

--------------------------
سایر پارامتر ها 

index =
 لیستی از ایندکس های عددی یا غیر عددی
-----------------------
p(serie_name.index)
با پرینت عبارت بالا می توان ایندکس های سازنده سری را دید
----------------------- 
پیش فرض ایندکس عددی و بهتر

dtype
نوع داده هایی که میخاهیم سری داشته باشد
پیشفرض :پانداس خودش داده ها را میشناسد 

name:
نامی برای سری .چندان ضرورتی ندارد

copy
پیشفرض فولز و بهتر 
اگر ترو باشد یک کپی از داده ها ساخته میشود
---------------------------------
pd.DataFrame()
شکل رایج 
df = pd.DataFrame(data)

- ⏩data : منبع داده برای ساخت داتافریم
dict، list ،numpy array ، Series یا  DataFrame دیگر / None پیشفرض 

- index : برچسب (label) ردیف‌ها
list، RangeIndex، یا Index دیگر
پیش‌فرض None(به‌صورت خودکار از 0 شروع می‌شود)
- ⏩columns:  نام ستون‌ها
 پیش‌فرض None / لیستی از نام ستون ها / (در صورت dict، از کلیدها استفاده می‌شود)

- dtype : 
تعیین نوع داده برای تمام ستون‌ها.
 None پیش‌فرض / float, int, str, object

- copy: 
آیا داده کپی شود یا به‌صورت مرجع استفاده شود
پیش‌فرض False / اگر ترو باشد، دادهٔ ورودی کاملاً کپی می‌شود
مثلا
 df2 = pd.DataFrame(df1, copy= True)
----------------------------------
..............دسترسی به محتوای داتافریم..............
p(df.columns.tolist())    مشاهده نام ستون های داتافریم
p(df[col_name])       مشاهده محتوای یک ستون خاص از داتافریم به صورت سری
p(df[col_name].tolist())  مشاهده محتوای یک ستون خاص از داتافریم به صورت لیست
--------------------------------
df.select_dtypes(include/exclude)
برای جدا کردن یکجای ستون های دارای ویژگی مشترک
و ساخت داتافریم های جدید 
مثلا
ساخت داتافریم از ستون های عددی
dfnum=df.select_dtypes(include="number")

ساخت داتافریم از ستون های غیر عددی
dfunnum=df.select_dtypes(exclude="number")

برای ساخت لیست ستون های دارای ویژگی مشترک
برای گروهبندی یا برای لوپ فور
numcols=df.select_dtypes(include="number").columns
unumcols= df.select_dtypes(exclude="number").columns
اگر بخواهیم برخی ستون ها را از این مجموعه خارج کنیم
unumcols= df.select_dtypes(exclude="number").drop("name",axis=1).columns

استفاده در لوپ فور
for i in numcols:

استفاده در گروهبندی
df.groupby(by="col1")[numcols].mean().round()
------------------------------------
df.fillna
شکل رایج 
df.fillna(n یا df.median()یا ... ,inplace = True)
n =  والیوی دلخواه به جای مقادیر خالی 

پرکردن یکجا برای ستون های عددی با توابع آماری
>>> dfm.fillna(dfm.median/mode/mean(numeric_only=True),inplace=True)

پرکردن یکجای ستون های رشته و آپجکت با پرتکرارترین مقدار هر ستون
>>> dfm.fillna(dfm.select_dtypes(include=["string","object"]).mode().iloc[0],inplace=True)

- value: مقدار جایگزین برای NaN
 پیشفرض None / scalar, dict, Series, or DataFrame

- method: روش پر کردن مقادیر خالی
پیشفرض None/ '
ffill', 'bfill', 'pad', 'backfill'

- axis: مشخص می‌کند ستون حذف شود یا ردیف 
پیش فرض 0 / برای ردیف ها 0 و برای ستون ها 1 است


- inplace: انجام عملیات روی دیتافریم اصلی
    False پیشفرض / True 

- limit: 
 حداکثر تعداد پر کردن مقادیر خالی در هر ستون یا ردیف

پیشفرض: None \ عدد دلخواه 
---------------------------------------
df.bfill()
df.ffill()
این دو جایگزین فرمول زیر هستند
df.fillna(method="ffill"/bfill,inplace=True)
 گویا این فرمول در نسخه های آینده پانداس از کار خواهد افتاد
 و دیگر متد به عنوان یک پارامتر در فیلنا کار نخواهد کرد
دو فرمول جایگزین نیازی به پارامتر اینپلیس هم ندارند و خودشان 
تغییرات را بر داتافریم اعمال می کنند.  
--------------------------------------
 df.dropna
شکل رایج 
df.dropna(ignore_index= True, inplace = True) برای حذف ردیف ها 
df["col_name"].dropna(ignore_index= True, بقیه پارامترها اغلب بلااستفاده) برای حذف سلول

ignore_index = برای جلوگیری از به هم ریختن ایندکس ها بعد از حذف
پیشفرض False / بهتر True

- inplace:
 انجام عملیات روی دیتافریم اصلی
   False پیشفرض/ True
   
- axis: جهت حذف
پیشفرض 0/مقدار: 0 (سطرها), 1 (ستون‌ها)

-------کمتر رایج -------
- how   شرط حذف
 all  /پیشفرض 'any' 
   اگر any
   باشد یک مقدار خالی کافیست
   برای حذف ستون یا ردیف 
   ولی در all 
   همه مقادیر باید خالی باشند.

- thresh: 
حداقل تعداد مقادیر غیر خالی در سطر یا ستون ها 
پیشفرض None/عدد 

- subset:
 زیرمجموعه ستون‌ها برای بررسی
پیشفرض None /مقدار list of column names

----------------------------------------
df.drop()

شکل رایج 
df.drop("colomn_name", ignore_index= True,axis = 1 , inplace = True)
value = "colomn_name"/["col1","col2",...]
برای حذف ردیف ها:
ایندکس یک ردیف  یا لیست ایندکس های چند ردیف

ignore_index= True بهتر / False پیشفرض
axis = یک برای حذف ستون / صفر برای حذف ردیف
inplace = True بهتر / False پیشفرض
-------------------------------------------
read_csv()
شکل رایج 
df = pd.read_csv(path)
# هر پارامتری پیش فرض دارد میتواند نوشته نشود
-path = r"مسیر فایل"
- index_col = "column_name"/None پیش فرض 

-----کمتر مورد استفاده---------

usecols       → انتخاب ستون‌های خاص

nrows         → تعداد ردیف محدود

dtype         → تعیین نوع داده ستون‌ها

skiprows      → رد کردن n ردیف اول

index_col =    → ستون ایندکس

parse_dates =["Date"]   → تبدیل خودکار به datetime

na_values =True    → مقادیر غیر خالی و سفارشی معادل NaN
مثلا 0 
na_filter =True
تبدیل همه انواع مقادیر خالی (Na,N/A,none,None و " ", ...) به NaN
تا دراپنا آنها را تشخصیص دهد
دراپنا تنها None و NaN رو تشخیص میده
converters    → اعمال تابع روی ستون هنگام خواندن
--------------------------------------------------
ساخت شیء از فایل اکسل(قبل از تبدیل آن به داتافریم) 
برای پی بردن به اسامی شیت ها
excel_file = pd.ExcelFile(path2)    #شی اکسل ساخته شد
p(excel_file.sheet_names)          #پی بردن به اسامی شیت ها
p(len(excel_file.sheet_names))          #پی بردن به تعداد شیت ها
---------------------------------------------
read_excel()    ساخت داتافریم از اکسل
شکل رایج
dfall = pd.read_excel(path,sheet_name=None)
این الگو داتافریم را در یک دیکشنری بزرگ میریزد یعنی 
اگر بنویسیم type(dfall) میگوید دیکشنری
پس بهتر است از آن استفاده نشود 
و مستقما بدانیم از چه شیتی از اکسل داتافریم میسازیم. 
طبق الگوی زیر:

df2 = pd.read_excel(path,sheet_name=1)

-path = r
"مسیر فایل"
-sheet_name = "names"/بهتر/پیشفرض 0 None

- usecols = حالت ها در بالا توضیح داده شده اند
- nrows = عدد
- index_col = "column_name"/ None پیش فرض 
-------------------------
پی بردن به تعداد شیت ها و تعداد ستون ها

dfall = pd.read_excel(path,sheet_name=None)
p(dfall.keys())   نام شیت ها پرینت میشوند
favorite_sheet = dfall["sheet_name"] خارج سازی هر شیت خاص به صورت داتافریم از دیکشنری کلی 
نام شیت به صورت کلید بعد از نام داتافریم کلی(دیکشنری) درج شده و والیوی هر شیت که یک داتافریم است در دسترس قرار میگیرد
p(dfn.keys())   نام ستون های یک داتافریم خاص پرینت میشوند
----------------------------------------
df.reset_index()
برای حذف کردن یا برداشتن ستون ایندکس(تبدیل آن به یک ستون عادی)
 
برای حذف کامل ستون ایندکس
df= df.reset_index(drop=True)

برای تبدیل ستون ایندکس به ستون عادی
df.reset_index(inplace=True)

---------------------------------------
df.sample()
شکل رایج
df= df.sample(frac =1,ignore_index=True,random_state=1)

n = عدد ردیف‌هایی که می‌خواهیم انتخاب شوند 
/پیشفرض None (در این حالت باید frac تعیین شود)

>>> frac = نسبت درصدی نمونه‌گیری (مثلاً 0.2 یعنی ۲۰٪ از داده‌ها) /
پیشفرض None (در این حالت باید ان تعیین شود)
اگر فراک دادیم نیازی به إن نیست و برعکس

random_state = (عددی ثابت برای تکرارپذیری نمونه (بهتر است مقدار ثابت بدهیم
شبیه سید در فیکر 

ignore_index = بهتر True / پیشفرض False (شماره‌گذاری مجدد ایندکس‌ها)

-----کمترکاربردی-----
replace = پیشفرض False (بدون تکرار) / True (با تکرار مجاز)
weights = ستون یا آرایه‌ای برای تعیین احتمال انتخاب هر ردیف /
پیشفرض None (احتمال برابر)
axis = پیشفرض بهتر و برابر 0 (نمونه‌گیری از ردیف‌ها) / 1 برای ستون‌ها

--------------------------------------
sort_values()
شکل رایج 
df.sort_values(by="col_name",ignore_index=True, inplace=True)

by = "col_name"/["col1","col2",...]
ignore_index = بهتر True/ پیشفرض False
inplace = بهتر True/ پیشفرض False
ascending = پیشفرض ترو وبهتر برای صعودی/ فولز برای نزولی/ 
[True,True  یا بالعکس برای حالاتی که بای نیز ستون است]

axis = پیشفرض بهتر و برابر 0/ 1 بد و سبب ارور 

---------------------------
to_excel()
شکل رایج 
df.to_excel(expath,sheetname="sh1",index=False)

-expath
-sheet_name= پیشفرض sheet1 .../"sh1"بهتر/2
-index = True پیشفرض /False بهتر 

-----------------------------------------
pd.excelWriter()
شکل رایج
with pd.ExcelWriter(expath,mode='a',if_sheet_exists= "new") as writer:
    df1.to_excel(writer,sheet_name="sh1",index=False)
  
    df2 .....

expath
mode = "a"
if_sheet_exists= "new"بهتر /"replace"/"error"

-----------------------------------------------
pd.concat
شکل رایج
dfall = pd.concat([df1,df2,...],ignore_index=True) برای اتصال عمودی
dfall = pd.concat([df1,df2,...],axis = 1) برای اتصال افقی

objs = [,] / (,) or dictionary لارم نیست نام این پارامتر درج شود تنها والیوی آن کافیست
ignore_index = False بهتر/ پیشفرض True 
اگر axis = 0(کونکت عمودی) ignore_index =True لازم  
در غیر این صورت(کونکت افقی) لازم نیست چون نام ستون ها رو تبدیل به عدد میکند

axis = پیش فرض 0 / 1

----------------------------------
pd.merge()
شکل رایج
dfm = pd.merge(L,R,how = "joinType",on="colname")

L & R = داتافریم های چپ و راست
نوشتن خود این دو حرف لازم نیست فقط نام داتافریم ها را بنویس
how = "outer" بهتر/"inner"/"right"/"left"
on = نام ستون" یا ["ستون های مشترک"] بین دو"داتافریم

left_on= نام متفاوت ستون مشترک در داتافریم چپ
right_on = نام متفاوت ستون مشترک در داتافریم راست 
------------------------------
df.groupby()

🧩 شکل رایج
df.groupby(by="col_name", as_index=False)  # شی  گروهبندی ساخته میشود

برای انجام گروهبندی، باید بعد از این شی، نام ستون هایی که قرار است گروهبندی شوند و سپس نام توابع آماری که روی این ستون ها اعمال میشود بیایند
طبق الگوی زیر 
df.groupby(by="col1")[numcols].mean()

شکل هایی دیگر از الگوی گروهبندی:
df.groupby(by="col1",as_index=False)[numcols].agg(["mean","max",...]) 
df.groupby(by="col1",as_index=False).agg({"col2":["mean","max",...], "col2":["min","std"]}) 
ستون ها به صورت کلید در تابع ای جی جی استفاده شده اند
-----------------
⚙️ پارامترها
by = "col_name" / ["col1","col2", ...]
ستونی که بر اساس آن داده‌ها گروه‌بندی می‌شوند (می‌تواند یک یا چند ستون باشد).

as_index = 
بهتر False / پیش‌فرض True
اگر True: ستون گروه‌بندی به اندیس تبدیل می‌شود.
اگر False: ستون گروه‌بندی در خروجی به‌صورت ستون عادی باقی می‌ماند (قابل توصیه‌تر برای کارهای بعدی).

sort = False /  پیش‌فرض True بهتر
اگر True: گروه‌ها بر اساس نامشان مرتب می‌شوند.
اگر False: ترتیب داده‌ها حفظ می‌شود و کمی سریع‌تر اجرا می‌شود.

------کمتر رایج در کار ما --------

dropna = بهتر True / پیش‌فرض True
اگر True: ردیف‌هایی که مقدار گروهشان NaN است حذف می‌شوند.
اگر False: آن گروه‌ها هم نگه داشته می‌شوند.

axis = پیش‌فرض بهتر و برابر 0 / 1 بد و سبب ارور در بیشتر موارد
محور گروه‌بندی؛ معمولاً 0 برای سطرها استفاده می‌شود (درست‌ترین گزینه).


level = None / سطحی از اندیس در DataFrame چندسطحی (MultiIndex)
اگر اندیس چندسطحی داری، تعیین می‌کند در کدام سطح گروه‌بندی انجام شود.

observed = بهتر False / پیش‌فرض False
فقط در داده‌های دسته‌ای (categorical) کاربرد دارد؛ اگر True فقط گروه‌های واقعی را نگه می‌دارد.

group_keys = بهتر True / پیش‌فرض True
مشخص می‌کند نام گروه‌ها در خروجی (مثل هنگام استفاده از apply()) حفظ شوند یا نه.
# ---------------------------------------------------
#  تابع شمارش دسته‌ای در Pandas
# ----------------------------------------------------
df[col_name].value_counts()

# پارامترها:
# normalize = پیش‌فرض False، اگر True شود فراوانی نسبی برمی‌گرداند
# sort = پیش‌فرض True
# ascending = پیش‌فرض False
# dropna = پیش‌فرض True

# نکات کلیدی:
# - این تابع inplace ندارد
# - روی یک ستون (Series) اجرا می‌شود
# - خروجی به صورت Series است
# ---------------------------------------------
# ۱۵. دسترسی با iloc در Pandas
# ------------------------------------------
# انتخاب یک ردیف
df.iloc[row_index]

# انتخاب رنجی از ردیف‌ها
df.iloc[start:end]

# انتخاب یک ستون
df.iloc[:, col_index]

# انتخاب رنجی از ستون‌ها
df.iloc[:, start:end]

# انتخاب رنجی از ردیف‌ها و ستون‌ها
df.iloc[start:end, start2:end2]

# انتخاب یک عنصر
df.iloc[row_index, col_index]
# ============================================
# جزوه مت پلات لیب 
# ============================================
import matplotlib.pyplot as plt
شیوه اینمپورت کردن مت پلات لیب
-----------------------------------------------------------
🔆 رایج‌ترین توابع
matplotlib.pyplot
برای هوش مصنوعی
# توابع plt
----------------------------------------------------------------
توابع زیر بعد از مشخص کردن نوع نمودار می آیند یعنی بعد از 
plt.plot(),plt.scatter(),plt.bar(),plt.hist() بعدا و در ادامه هر یک از این نمودار ها توضیح داده میشن 
ولی فعلا توابع تنظیمی نمودارها را بدانید

توابع تنظیمی مت پلات لیب خالص(اگر نمودار ها را از پانداس رسم کنیم نیازی به این توابع تنظیمی نیست)

# 1
xlabel / ylabel✴️
کاربرد: تعیین عنوان محور افقی و محور عمودی نمودار

🧩 شکل رایج

✅plt.xlabel("X-axis label")
✅plt.ylabel("Y-axis label")

⚙️ پارامترها
xlabel / ylabel: عنوان محور
پیش‌فرض: ''
بهتر: متن مناسب داده‌ها
---------------------------
# 2
✅title✴️
کاربرد: تعیین عنوان کلی نمودار
🧩 شکل رایج

plt.title("Chart Title")

⚙️ پارامترها
متن عنوان
پیش‌فرض: ''
بهتر: عنوان معنی‌دار
--------------------------
# 3
legend✴️
کاربرد: نمایش راهنمای نمودارها (label های مختلف)

🧩 شکل رایج
plt.legend(loc='best')

⚙️ پارامترها

loc: محل نمایش legend
پیش‌فرض: 'best'
بهتر: 'upper right' یا 'lower left' بسته به نمودار
------------------------------
# 4
show✴️
کاربرد: نمایش نمودار نهایی
🧩 شکل رایج

plt.show()
⚙️ پارامترها
ندارد
---------------
# 4

savefig()
تابعی برای ذخیره نمودار در فایل های تصویری

شکل رایج:
plt.savefig(fname="chartname.png",dpi=300)

نکته1 : نباید قبل از این تابع از plt.show استفاده شود
چون فقط صفحه خالی ذخیره میشه

نکته2: اگر چند figure داشته باشیم با plt.savefig تنها یکی از آنها ذخیره میشه
راه حل اینه که در زمان ساخت figure ها برای هر
figureیک شی تعریف کنیم مثلا fig1=plt.figure(1)
سپس در زمان ذخیره با savefig بنویسیم fig1.savefig("fig_name")

پارامترها:
dpi:
بیانگر وضوح تصویر
300 بهتر

fname: ""
نام فایل همراه با پسوند فرمت درون دبل کوت
مثلا name.png 
------------------------------
# 5

plt.imshow
برای نمایش تصویر — بسیار مهم در بینایی ماشین (دیدن ورودی‌ها، 
فیلترها، heatmapها و …).
------------------------------
# 6

plt.boxplot
برای بررسی پراکندگی و تشخیص outlierها — کاربرد در پردازش داده و 
پاک‌سازی دیتاست.
------------------------------
# 7

 plt.grid
برای نمایش شبکهٔ پس‌زمینه — خوانایی را بالا می‌برد.
------------------------------
# 8

 plt.colorbar
برای نمودارهای heatmap یا خروجی‌های مدل‌های CNN.
--------------------------------------------------
# 9

✅figure
برای جدا کردن و ترسیم مجزای هر نمودار از بقیه نمودارها

شکل رایج:
plt.figure(num=)

به تعداد دفعاتِ درج این تابع،صفحه برای رسم نمودار ایجاد میشود
اگر درون پرانتز هر تابع، شماره  بگذاریم
آن عدد تبدیل به نام فیگور میشود
مثلا

plt.figure(3)
نوشته میشود figure 

پارامترها:

figsize = 
 اندازهٔ شکل به اینچ (width, height)

پیش‌فرض: (6.4, 4.8)
مثال: figsize=(8,6)

dpi:
کیفیت و رزولوشن شکل (dots per inch)

پیش‌فرض: 100
مثال: dpi=120


facecolor:

توضیح: رنگ پس‌زمینهٔ شکل
پیش‌فرض: 'white'
مثال: facecolor='lightgray'

edgecolor:
 رنگ حاشیهٔ شکل
پیش‌فرض: 'white'
مثال: edgecolor='blue'

num:
 شماره یا عنوان شکل

پیش‌فرض: None (سیستم خودش شماره اختصاص می‌دهد)
اگر عدد بگذاریم واژه figure حذف نمیشه(میشه figure 5 مثلا)
ولی اگر رشته باشد، فقط رشته به عنوان نام تعریف میشه
مثال: num=1 یا num="My Figure"
------------------------------------
✅subplot   این تابع قبل از رسم نمودار می آید و تنظیمی نیست
برای آوردن بیش از یک نمودار در یک صفحه

نکته: تمام نمودارهای ساب پلات شده تنها باید با یک 
plt.show()
در آخر همه نمایش داده شوند نه هر یک به صورت جدا دارای این تابع باشد

شکل رایج : 
plt.subplot(nrows,ncols,index)

پارامتر ها: 
nrows →
 تعداد ردیف‌ها (number of rows)

ncols → 
تعداد ستون‌ها (number of columns)

index →
 شماره خانه‌ای که نمودار در آن رسم می‌شود (position of the subplot)

سایر پارامترها : 
hspace →
 فاصله عمودی بین ردیف‌ها (height space)

wspace →
 فاصله افقی بین ستون‌ها (width space)

مثال: 
import matplotlib.pyplot as plt
plt.subplot(2,1,1)
#دو ردیف و یک ستون و دو خانه داریم که این نمودار در خانه اول رسم میشود
plt.hist(A,edgecolor="black",color="green",bins=30)

plt.subplot(2,1,2)
plt.scatter(X,Y,color="black")
plt.show()
--------------------------------
# 10
plt.tight.layout()
برای جلوگیری از روی هم افتادن نمودارها و متن های ساب پلات شده

# تابع تای لایوت در پایان و بعد از همه ساب پلات ها درج میشود
# تا مانع تداخل اجزای نمودارها با هم شده و بین آنها فاصله بدهد

--------------------------------------------------------------------
تابع سابپلات متپلات لیب خالص

شکل رایج :

✅figX, axsX =plt.subplots(nrows,ncols, figsize=(width,height),constrained_layout=True)
X = شماره دلخواه که شاید اصلا لازم نباشد

جایگزین سه تابع 
❌plt.figure()
❌plt.subplot()
❌plt.tight_layout()
تابع سابپلات میتواند به جای سه تابع بالا استفاده شود و کار هر سه را یکجا و مدرن تر انجام میدهد.

پارامتر ها : 
figsize = اندازه کل صفحه 
پیشفرض 4  × 6  اینچ
عدد بزرگتر → عرض (width)
عدد کوچکتر → ارتفاع (height)

برای یک نمودار واحد(حتی برای دوتاهم) figsize لازم نیست.‌
برای 4 نمودار: 10×12  
--------------
nrows و ncols 
شماره ردیف و ستون جدول اکس ها هستند. برای یک نمودار واحد لازم نیستند ولی برای بیش از یک نمودار در یک فیگور باید درج شوند ولی فقط اعداد آنها درون فرمول تابع. 
-------------
constrained_layout
پیشفرض False/ بهتر True

جایگزینی بهتر برای تابع plt.tight_layout()
در شرایطی تعدد سابپلات هاست 
.اگر فقط یک نمودار واحد داشتیم نیازی به این پارامتر نیست
------------
 نحوه اعمال سابپلات در نمودارهای پانداس
 
# 1)
اعمال سابپلات با تابع سابپلات متپلات خالص
(ترکیب متپلات لیب و پانداس ، کنترل بیشتر بر سابپلات ها)


در این روش بعد از درج تابع، در هر سری پلات یا دی اف پلات تنها یک پارامتر جدید به نام اکس اضافه میکنیم و موقعیت آن نمودار را در والیوی این پارامتر درج میکنیم 

import matplotlib.pyplot as plt
import pandas as pd

fig,axs=plt.subplots(2,1,figsize=None)  
# --------------------------
# نمودار اول با اکس صفر:

df["gender"].value_counts().plot(kind="bar",ax=axs[0],title=" Students Gender Bar Chart",color="green",edgecolor="black")
# دو ردیف و یک ستون داریم
# پس اکس(آرایه جدول نمودارهای ما) یک بعدی است و باید به صورت یک عدد درون کروشه بیآید 
# هرگاه یکی از ردیف ها یا ستون ها (فرقی نمیکند) یک باشد آرایه یک بعدی است
و نیازی به نوشتن ایندکسِ شماره آن ستون یا ردیف در  کروشه آکس نیست
# مثلا
# ax=axs[1] یا ax=axs[0] 
# ax=axs[1,0] اشتباه است برای آرایه یک بعدی
# -----------------
# نمودار دوم با اکس یک:
df["class"].value_counts().plot(kind="bar",ax=axs[1],title="Students Class Bar Chart",color="grey",edgecolor="black")
# ------------------
# قابلیت اعمال تنظیمات بیشتر بر هر اکس و به کمک توابع تنظیمی متپلات لیب مثلا:
axes[0].set_xlim(0,20)   تعیین محدوده محور ایکس برای نمودار اکس صفر

# -------------
fig1.savefig(fname="Value_counts bar Cahrt.png",dpi=300) 
صرف نظر از تعداد ابعاد اکس و نمودارها ، تابع ذخیره همیشه به شکل بالاست
همیشه باید تابع ذخیره قبل از تابع نمایش بیاید وگرنه چیزی ذخیره نمیشود
plt.show()   #تابع نمایش 
----------------------------------------------------
# 2
روش دوم برای اعمال سابپلات بر توابع پانداس(با پارامتر های خود پانداس برای سابپلات)
روشی آسانتر ولی با کنترل کمتر از جمله اینکه اسامی متفاوت محور نداریم
-----------------------------------------------------
# در این روش به جای تابع سابپلات از پارامتر های زیر استفاده میشود :
subplots = True   #اعمال سابپلات بر نمودارها و عدم رسم آنها روی هم
layout = (nrow, ncols)     #تعیین چیدمان سابپلات ها 
figsize = (8,10)       #تعیین اندازه شکل اگر لازم بود

مثال :

# رسم سه هیستوگرام در سه ساب‌پلات جداگانه، فقط با پانداس
df[["math", "biology", "ecology"]].plot(
    kind="hist",
    subplots=True,
    layout=(3, 1),
    figsize=(8, 10),
    bins=10,
    title="Distribution of Scores"
)
plt.tight_layout()
plt.show()

مثال دوم :
df[["math","biology"]].plot.line(subplots=True,layout=(2,1),xlabel = "indexes",ylabel = "score")
plt.xticks(df.index)
plt.tight_layout()
plt.show()
------------------------------------------
اعمال سابپلات برای نمودارهای متپلات لیب خالص

با تابع سابپلات و متد های شیء اکس 
---------------------------------------
# مثال :

import numpy as np
np.random.seed(2)
p=print

a1=np.random.choice(range(-10,40),size=100)
a2=np.random.uniform(-10,45,100).astype(int)
# p(a2)

import matplotlib.pyplot as plt
fig,axs=plt.subplots(2,1,figsize=None,constrained_layout=True)  #تابع سابپلات
axs[0].hist(a1,color="green",edgecolor="black")           #کد نمودار اکس اول
# اگر فقط یک نمودار داشته باشیم نیاز به براکت عدد دار نیست
# یعنی مثلا باید بنویسیم  
# axs.hist()
# ---------------
axs[0].set_title("A1 Hist")    # توابع شیء اکس برای تنظیمات بیشتر بر اکس اول
axs[0].set_xlabel("digits")      #"
axs[0].set_ylabel("Frequency")
# برای گذاشتن عنوان و برچسبها باید از متدهای اکس استفاده کنیم
# نه از پارامترها یا توابع پی ال تی 

# ----------------------
axs[1].hist(a2,color="grey",edgecolor="black")  کد نمودار اکس دوم
axs[1].set_title("A2 Hist")
fig.savefige("my_plots.png",dpi = 300) 
plt.show()
---------------
مثال برای رسم یک نمودار واحد از مت پلاتلیب خالص

import matplotlib.pyplot as plt
fig1,axs1 = plt.subplots()
axs1.plot(new["math"],marker = "*",color = "black",linestyle = "--",label = "math score")
axs1.set_title("math line chart")
axs1.set_xlabel("Frequency")
axs1.set_ylabel("math scores")
axs1.legend()      #تابع لجند، ست ندارد
fig1.savefige("my_plots.png",dpi = 300)
plt.show()
------------------------------
... توضیح انواع نمودارها با رسم مستقیم از متپلات لیب خالص و بدون سابپلات....

✅plot✴️  
کاربرد: رسم نمودار خطی ساده یا چند خطی برای نمایش رابطه بین دو سری داده
🧩 شکل رایج

plt.plot(x, y)
نکته مهم) 
تقریبا همیشه رسم نمودارهای خطی از پانداس(به صورت سری یا دی اف پلات)
بهتر از رسم آنها از متپلاتلیب خالص است 
تنها اثتثنا : مگر داتا از داتافریمی واحد نباشد یا چند منبع داشته باشد 

--------------------
نکته : 
دستور plt.plot()
به طور کلی دو آرگومان اصلی می‌گیرد:ایکس و ایگریک· 
اگر فقط یک آرگومان به آن بدهی مثلا 
plt.plot(df["math"])
آن را به عنوان محور ایگریک در نظر می‌گیرد و خودش برای محور ایکس از اعداد ۰, ۱, ۲, ... استفاده می‌کند. اگر داتا مثلا یک سری باشد ایندکس آن به جای ایکس قرار میگیرد
· اگر دو آرگومان به آن بدهی 
(مثلاً plt.plot(X, Y))،
اولی را محور ایکس و دومی را محور ایگریک در نظر می‌گیرد.
--------------------
⚙️ پارامترها

x: مقادیر محور افقی
پیش‌فرض: ندارد
بهتر: مقادیر واقعی داده‌ها
y: مقادیر محور عمودی
پیش‌فرض: ندارد
بهتر: مقادیر واقعی داده‌ها

: مثلا اگر ایکس و ایگریک ستون داتافریم باشند
x= df["country"] 
y=df["math"]
---------------------------
color: رنگ خط
پیش‌فرض: 'blue'
بهتر: رنگ دلخواه برای خوانایی

linestyle: نوع خط
پیش‌فرض: '-'
بهتر: '-' یا '--' بسته به نیاز
چند مورد دیگه هم داره 
marker: علامت نقاط داده
پیش‌فرض: None
بهتر: 'o' برای نمایش نقاط
میتوان از * یا هر علامت دلخواه دیگری هم استفاده کرد
label:
 برچسب نمودار برای تابع لجند
پارامتر لیبل در تابع پلات برای نام‌گذاری هر خط در نمودار استفاده می‌شود
تا بعداً در لجند(راهنما) نشان داده شود.
وقتی چند خط روی یک نمودار رسم می‌کنی، 
با لیبل می‌توانی برای هرکدام یک برچسب بگذاری 
در راهنما مشخص شود هر خط مربوط به کدام داده است.

plt.plot(x, y, label="Math Scores")


پیش‌فرض: None
بهتر: برچسب معنی‌دار وقتی چند خط داریم
-----------------
مثال برای نمودار خطی (بدون سابپلات و رسم از متپلاتلیب خالص)
import matplotlib.pyplot as plt

plt.plot(df1["math"],marker = "*",color = "black",linestyle = "--",label = "math score")
plt.title("math line chart")  #توابع تنطیمات
plt.xlabel("Frequency")    #"
plt.ylabel("math scores")    #"
plt.legend()     #نمایش دهنده لیبل
# fig1.savefige("my_plots.png",dpi = 300)
plt.show()
---------------------------------------------------
نحوه استفاده درست از نمودار خطی (plot)
----------------------------------------------------
فرضا یک داتافریم از نمرات دانش آموزان داریم با این ستون ها :
age,country,math,biology,chemistry

۱. رسم روند یک درس برای همه دانش‌آموزان
SORTING: Not Required (چون ایکس همان ایندکس است و ترتیب ردیف‌ها را نشان می‌دهد.)
اگر دیتافریم سورت والیوز شده باشد حتماً باید با ایگنور ایندکس ترو باشد، وگرنه نمودارهای خطی تبدیل به کلاف سردرگم می‌شوند.

روش پانداس (توصیه‌شده):

import pandas as pd
import matplotlib.pyplot as plt

این یک سری پلات است اگر دو کروشه کنیم میشه دی اف پلات و خودش اتوماتیک لیبل نمودار رو هم درج میکند
df["math"].plot(
    kind="line",                             #میتوان این پارامتر را ننویسیم و به جای آن بنوسیم
    title="Math Scores Trend",          #df.plot.line()
    xlabel="Student Index",
    ylabel="Math Score",
    label = "math",
    color = "green",
    linestyle = "--"
    marker = "*"
    #ax = axs[nrow,ncol] 
    # اگر نمودار اکس داشت تنها با درج این پارامتر درون پلات،  اکس اعمال میشود
    البته باید تابع سابپلات(دقیقا به همان شکلی که در متپلاتلیب خالص هست)قبل از پلات درج شود
)

plt.legend()
plt.show()
اینجا X ایندکس پیش‌فرض (0, 1, 2,...) است. نمودار نشان می‌دهد که نمرات ریاضی از دانش‌آموز اول تا آخر چه روندی داشته است. عنوان و لیبل‌ها مستقیماً داخل  پلات پانداس تنظیم شده‌اند.
----------------------------------------
۲. رسم چند درس برای مقایسه
SORTING: Not Required (باز هم X ایندکس است.)

روش پانداس (توصیه‌شده):

یک دی اف پلات داریم
df[["math", "biology", "chemistry"]].plot(
    kind="line",
    title="Scores Trend Comparison",
    xlabel="Student Index",
    ylabel="Score"
)
plt.show()
پانداس خودکار برای هر ستون یک خط با رنگ متفاوت می‌کشد و لجند را هم خودکار می‌سازد.

اگر با متپلات‌لیب بخواهی (روش جایگزین):

plt.plot(df["math"], label="Math", marker="*")
plt.plot(df["biology"], label="Biology", linestyle="--")
plt.plot(df["chemistry"], label="chemistry")

plt.title("Scores Trend Comparison")
plt.xlabel("Student Index")
plt.ylabel("Score")
plt.legend()
plt.show()
اینجا سه خط مجزا داریم که روند نمرات سه درس را در بین دانش‌آموزان مقایسه می‌کند. در روش متپلات‌لیب باید دستی label، legend و استایل هر خط را تنظیم کنی.
-----------------------------------------------
۳. رسم رابطه بین دو درس (روشی که یاد گرفتی)
SORTING: Not Required (چون هر دو متغیر هستند و ترتیبشان باید با هم جفت بماند.)

روش پانداس (توصیه‌شده):

df.plot(
    kind="line",
    x="math",
    y="biology",
    title="Relationship between Math and Biology",
    xlabel="Math Score",
    ylabel="Biology Score"
)
plt.show()
X: نمره ریاضی هر دانش‌آموز

Y: نمره زیست همان دانش‌آموز

هدف: دیدن اینکه آیا دانش‌آموزانی که ریاضی قوی‌تری دارند، زیستشان هم قوی‌تر است؟

هشدار: اگر نمرات ریاضی به هم ریخته باشند (مثلاً ۲۰, ۱۲, ۱۹, ۱۵)، خطوط نمودار درهم می‌روند. برای این نوع تحلیل، معمولاً نمودار پراکندگی اسکتر مناسب‌تر است، اما نمودار خطی هم قابل قبول است.

اگر با متپلات‌لیب بخواهی (روش جایگزین):

plt.plot(df["math"], df["biology"], "o-")
plt.title("Relationship between Math and Biology")
plt.xlabel("Math Score")
plt.ylabel("Biology Score")
plt.show()
-----------------------
۴. رسم تغییرات نمره بر اساس سن (Age)
SORTING: Required (چون سن باید روی محور ایکس مرتب باشد، وگرنه خطوط درهم می‌شوند.)

روش پانداس (توصیه‌شده):

# Step 1: Sort the DataFrame by the column you want on the X-axis
df_sorted = df.sort_values(by="age", ignore_index=True)

# Step 2: Plot using the sorted data
df_sorted.plot(
    kind="line",
    x="age",
    y="math",
    title="Math Score as a Function of Age",
    xlabel="Age",
    ylabel="Math Score"
)
میتوان دو مرحله بالا را با هم نوشت مثلا :
# df.sort_values(by="age",ignore_index=True).plot.line(x="age",y="math")
plt.show()
اگر اینجا sort_values را فراموش کنی، سن‌ها به ترتیب اصلی‌شان روی محور X می‌آیند (مثلاً ۱۵, ۱۲, ۱۸, ۱۴) و نمودار خطی به شدت گمراه‌کننده و اشتباه خواهد بود.
------------------------------------------------
۵. رسم میانگین نمرات به تفکیک کشور (Country)
SORTING: Optional but Recommended  #اختیاری ولی توصیه شده

روش پانداس (توصیه‌شده):

# Step 1: Calculate mean scores per country
# اگر در گروپبای پارامتر اس ایندکس رو فولز  تعریف کنیم،
# بعداً دیتافریم گروپبای شده را اگر بخواهیم نمودار خطی کنیم ارور می‌دهد
یعنی گروه ها باید ایندکس باشند تا در دی اف پلات به صورت اتوماتیک روی محور ایکس ترسیم شوند

grouped = df.groupby(by="country")[["math","biology","chemistry"]].mean().round().astype(int)
grouped.plot(
    kind="line",
    title="Average Math Score by Country", 
    ylabel="Average Math Score"  #نیازی به لیبل محور ایکس نیست چون خودش میزنه در داتافریم های گروهبندی شده
)
همان طور که میبینید در داتافریم های گروهبندی شده نیازی به درج ایکس و ایگریک در پلات نیست
کل ستون ها را رسم میکند و ستون ایندکس را در محور ایکس میگذارد

plt.show()
نکته مهم) 
پانداس با داده های گروهبندی شده مثل ایندکس صرف برخورد نمیکند و تعداد گروه ها رو هوشمندانه کم می کند تا در محور ایکس همه چیز قاطی نشود. ولی متپلات لیب همه رو رسم میکند
---------------------------
اگر با متپلات‌لیب بخواهی (روش جایگزین):

grouped = df.groupby("country")["math"].mean()
در این مثال به ما یک سری میدهد چون تعداد ستون های بعد از شی گروهبندی تنها یکی است
اگر ستون ریاضی رو درون دو پرانتز بنویسیم به ما یک داتافریم میده
اهمیت این موضوع اینه که اگر تنها بنویسیم plt.(df["math"])
نمودار در مت پلات لیب خالص نصب میشود ولی اگر یک سری داشته باشیم 
باید مثل پایین ایندکس و والیوی آن را خودمان دستی در محل ایکس و ایگریک نمودار درج کنیم.
plt.plot(grouped.index, grouped.values)   #این حالت درج جدای والیوو ایندکس تنها در داتای سری هستش
plt.title("Average Math Score by Country")
plt.xlabel("Country")
plt.ylabel("Average Math Score")
plt.show()
----------------
۶. رسم کارنامه یک دانش‌آموز (نمرات یک ردیف در دروس مختلف)
SORTING: Not Required (چون محور ایکس نام دروس است و ترتیبشان را ما مشخص می‌کنیم.)

روش پانداس (توصیه‌شده):


student_scores = df.loc[0, ["math", "biology", "chemistry"]]
# student_scores یک ردیف از دیتافریم است که در واقع یک Series است.

student_scores.plot(
    kind="line",
    title="Report Card for Student 0",
    xlabel="Subject",
    ylabel="Score"
)
plt.show()
اینجا محور ایکس نام دروس است. برای هر دانش‌آموز یک نمودار جداگانه می‌کشیم که نمراتش را در درس‌های مختلف نشان می‌دهد.

---------------------
اگر دو یا چند دانش آموز باشند
st5_scores = df.loc[5,["math","biology","chemistry"]]
st4_scores = df.loc[4,["math","biology","chemistry"]]
st4_scores.plot.line()
st5_scores.plot.line()
plt.plot()
----------------------------
اگر با متپلات‌لیب بخواهی (روش جایگزین):

student_scores = df.loc[0, ["math", "biology", "chemistry"]]

plt.plot(student_scores.index, student_scores.values)
plt.title("Report Card for Student 0")
plt.xlabel("Subject")
plt.ylabel("Score")
plt.show()
------------------
اگر دو یا چند دانش آموز باشند
st5_scores = df.loc[5,["math","biology","chemistry"]]
st4_scores = df.loc[4,["math","biology","chemistry"]]

plt.plot(st4_scores.index,st4_scores.values)
plt.plot(st5_scores.index,st5_scores.values)
plt.show()
-------------------
✅scatter✴️
رسم اسکتر همیشه از متپلات لیب خالص بهتر از پانداس است

کاربرد: رسم نمودار پراکندگی برای نشان دادن توزیع نقاط و همبستگی بین دو متغیر

🧩 شکل رایج
plt.scatter(x, y)

⚙️ پارامترها

x و y: مختصات نقاط
پیش‌فرض: ندارد
بهتر: داده‌های واقعی
با یک لیست واحد به جای ایکس و ایگریک ارور میدهد
باید یا یک آرایه/سری جدا به جای ایکس هم بدهیم یا 
ایکس و ایگریک را از داتا بگیریم
مثلا
plt.scatter(df["col1"],df["col2"])
هر دو پارامتر، داتای واقعی اند
---------------
s=df["col1"]
plt.scatter(s.index,s.values)
ایکس و ایگریک از داتا گرفته شده اند

color: رنگ نقاط
پیش‌فرض: 'blue'
بهتر: رنگ دلخواه

marker: شکل نقاط
پیش‌فرض: 'o'
بهتر: 'o' یا 'x'

s: اندازه نقاط
پیش‌فرض: 20
بهتر: متناسب با نمودار

label: برچسب برای legend
پیش‌فرض: None
بهتر: برچسب معنی‌دار
------------
مثال برای رسم اسکتر از متپلات لیب خالص و بدون سابپلات
import matplotlib.pyplot as plt
plt.scatter(df1["math"].index,df1["math"].values ,marker = "o", color = "black", label = "math score", s=20)
plt.title("math line chart")
plt.xlabel("Frequency")
plt.ylabel("math scores")
plt.legend()
# fig1.savefige("my_plots.png",dpi = 300)
plt.show()
----------------------
نحوه استفاده درست از نمودار اسکتر(scatter)

اسکتر همیشه به دو آرگومان اصلی و اجباری نیاز دارد: ایکس و ایگریک
برای رسم اسکتر در هیچ حالتی نیاز به سورتینگ داتا نیست
------------
۱. رابطه بین دو درس (کلاسیک‌ترین کاربرد)

روش پانداس (توصیه‌شده):

df.plot(
    kind="scatter",
    x="math",
    y="biology",
    title="Relationship between Math and Biology Scores",
    xlabel="Math Score",  #خودش لیبل ها رو میزاره مگر بخواهیم لیبلی بهتر بگذاریم
    ylabel="Biology Score"
)
plt.show()
X: نمره ریاضی

Y: نمره زیست

هدف: دیدن همبستگی. هر نقطه یک دانش‌آموز است.

اگر با متپلات‌لیب بخواهی (جایگزین):

plt.scatter(df["math"], df["biology"])
plt.title("Relationship between Math and Biology Scores")
plt.xlabel("Math Score")
plt.ylabel("Biology Score")
plt.show()

۲. نمرات یک درس در برابر سن
روش پانداس (توصیه‌شده):


df.plot(
    kind="scatter",
    x="age",
    y="chemistry",
    title="chemistry Scores vs. Age",
    xlabel="Age",
    ylabel="chemistry Score"
)
plt.show()
X: سن (متغیر عددی پیوسته)

Y: نمره بوم‌شناسی

هدف: دیدن اینکه آیا الگوی خاصی بین سن و عملکرد در این درس وجود دارد.
--------------------
۳. پراکندگی یک درس برای همه دانش‌آموزان
روش پانداس (توصیه‌شده):
ارور میده مگر قبل از هر چیز ستون ایندکس را به یک ستون عادی تبدیل کنیم
یا از متپلات لیب خالص استفاده کنیم
یعنی اسکتر در پانداس با سری پلات رسم نمیشه 

df = df.reset_index()
df.plot(
    kind="scatter",
    x="index",      #ستون ایندکس به یک ستون عادی تبدیل شده پس مانند یک ستون عادی نامش را مینویسیم
    y="math",
    title="Distribution of Math Scores",
    xlabel="Student Index",
    ylabel="Math Score"
)
plt.show()
X: ایندکس (شماره ردیف)

Y: نمره ریاضی

هدف: دیدن محدوده و پراکندگی نمرات. این کار با اسکتر بسیار تمیزتر از نمودار خطی است.
-------------------------
۴. اضافه کردن بُعد سوم با رنگ (c)
df.plot.scatter() is BETTER for simple cases, but here Matplotlib wins.

روش متپلات‌لیب (الزامی — پانداس از پارامتر c پشتیبانی نمی‌کند):


plt.scatter(df["math"], df["biology"], c=df["age"])
plt.colorbar(label="Age")
plt.title("Math vs. Biology Colored by Age")
plt.xlabel("Math Score")
plt.ylabel("Biology Score")
plt.show()
X: نمره ریاضی

Y: نمره زیست

c (رنگ): سن

هدف: دیدن اینکه آیا سن روی رابطه این دو درس تأثیر می‌گذارد. پانداس در رسم scatter از رنگ‌گذاری بر اساس ستون سوم پشتیبانی نمی‌کند.
---------------------
۵. تغییر اندازه نقاط با یک متغیر دیگر (s)

روش متپلات‌لیب (الزامی — پانداس از پارامتر s پشتیبانی نمی‌کند):

plt.scatter(df["math"], df["biology"], s=df["chemistry"]*50)
plt.title("Math vs. Biology, Size by chemistry Score")
plt.xlabel("Math Score")
plt.ylabel("Biology Score")
plt.show()
X: نمره ریاضی

Y: نمره زیست

s (اندازه): نمره بوم‌شناسی

هدف: اضافه کردن بُعد چهارم اطلاعات. پانداس از تغییر اندازه نقاط بر اساس ستون سوم پشتیبانی نمی‌کند.
# --------------------------
۶. اضافه کردن شفافیت (alpha)

روش متپلات‌لیب (الزامی — پانداس از پارامتر alpha در scatter پشتیبانی نمی‌کند):

plt.scatter(df["math"], df["biology"], alpha=0.5)
plt.title("Math vs. Biology with Transparency")
plt.xlabel("Math Score")
plt.ylabel("Biology Score")
plt.show()
هدف: وقتی نقاط زیادی داری و روی هم می‌افتند، شفافیت کمک می‌کند تراکم نقاط را بهتر ببینی. پانداس این پارامتر را در scatter ندارد.

۷. رسم میانگین نمرات به تفکیک کشور (پس از groupby)
SORTING: Optional but Recommended

روش پانداس (توصیه‌شده):

country_means = df.groupby("country")["math"].mean().reset_index()
در اسکتر حتما باید ایکس و ایگریک ستون واقعی باشند پس باید با ریست ایندکس
ستون گروه ها رو که ایندکس هم هست به ستونی معمولی تبدیل کنیم
سپس ایکس و ایگریک را در پلات درج کنیم وگرنه ارور میده
country_means.plot(
    kind="scatter",
    x="country",
    y="math"
    title="Average Math Score by Country",
    xlabel="Country",
    ylabel="Average Math Score"
)
plt.xticks(rotation=45,ha="right") #کد چرخواندن گروه ها در نمودار 
# برخلاف پی ال تی لاین اینجا کل گروه ها میان روی ستون ایکس و قاطی میشن
# ولی با چرخاندن آنها با کد بالا مشکل حل میشه
plt.tight_layout()
plt.show()
X: نام کشور (Categorical که به عنوان برچسب استفاده می‌شود)

Y: میانگین نمره ریاضی

هدف: مقایسه بصری میانگین‌ها.
------------------------------------
✅bar✴️
کاربرد: رسم نمودار میله‌ای برای مقایسه دسته‌ای داده‌ها
تقریبا همیشه بهتر است با پانداس رسم شود مگر نیاز به تزئینات خیلی خاص (مثل متن یا عدد روی میله ها)
باشد در این صورت باید از ترکیب پانداس و متپلاتلیب استفاده شود

🧩 شکل رایج
plt.bar(x, height,edgecolor="black")

⚙️ پارامترها

x: موقعیت دسته‌ها روی محور افقی
پیش‌فرض: ندارد
بهتر: بر اساس داده‌ها

height: ارتفاع ستون‌ها
# همان محوایگریک ولی با اسمی دیگر

 پیش‌فرض: ندارد
بهتر: مقادیر واقعی

در متپلات لیب خالص : با یک لیست واحد به جای ایکس و ایگریک ارور میدهد
پس ایکس و ایگریک باید هر دو لیست واقعی/سری یا آرایه باشند
نباید ایکس را به صورت ساختگی از ایگریک بسازیم

در پانداس صرفا با درج یک داتا به جای ایگریک نمودار را رسم میکند

color: رنگ ستون‌ها
پیش‌فرض: 'blue'
بهتر: رنگ دلخواه

width: پهنای ستون‌ها
پیش‌فرض: 0.8
بهتر: 0.5 تا 0.9

label: برچسب برای legend
پیش‌فرض: None
بهتر: برچسب معنی‌دار
edgecolor= رنگ لبه میله ها 
پیش‌فرض: None
بهتر: 'black'
--------------------------------------------------------
نحوه استفاده درست از نمودار میله‌ای (kind="bar")
--------------------------------------------------------
۱. رسم نمرات یک دانش‌آموز خاص در تمام دروس (کارنامه)
SORTING: Not Required (ترتیب رسم، همان ترتیب ستون‌ها در دیتافریم است.)

روش پانداس (توصیه‌شده):

# انتخاب نمرات اولین دانش‌آموز (یک Series که ایندکسش نام درس‌هاست)
student_scores = df.loc[0, ["math", "biology", "chemistry"]]

student_scores.plot(
    kind="bar",
    title="Report Card for Student 0",
    xlabel="Subject",
    ylabel="Score",
    rot=0  # چرخش برچسب‌های محور X برای خوانایی
)
plt.show()
هر میله نماینده یک درس است.

ارتفاع میله نشان‌دهنده نمره در آن درس است.
--------------------------------------------
۲. مقایسه یک درس خاص بین چند دانش‌آموز
SORTING: Not Required (محور X همان ایندکس دانش‌آموزان خواهد بود.)

روش پانداس (توصیه‌شده):

# مقایسه نمره ریاضی ۵ دانش‌آموز اول
df["math"].head(5).plot(
    kind="bar",
    title="Math Scores of First 5 Students",
    xlabel="Student Index",
    ylabel="Math Score",
    rot=0
)
plt.show()
------------------------------------------
۳. رسم میانگین نمرات به تفکیک کشور (Grouped Bar)
SORTING: Optional but Recommended

روش پانداس (توصیه‌شده):

# محاسبه میانگین ریاضی برای هر کشور
country_means = df.groupby("country")["math"].mean().sort_values()

country_means.plot(
    kind="bar",
    title="Average Math Score by Country",
    xlabel="Country",
    ylabel="Average Math Score",
    rot=45  # اگر اسم کشورها طولانی است، آنها را بچرخان
)
plt.tight_layout()  # برای اینکه برچسب‌ها از کادر بیرون نزنند
plt.show()
-----------------------------------
۴. مقایسه چند درس در یک نمودار میله‌ای (Grouped Bar Chart)
SORTING: Not Required

روش پانداس (توصیه‌شده):

# میانگین تمام دروس برای هر کشور
country_all_means = df.groupby("country")[["math", "biology", "chemistry"]].mean()
نکته : اگر یک سورت والیوز بعد از تابع مین بزنیم ستون ها را به ترتیب افزایشی یا کاهشی مرتب میکند

country_all_means.plot(
    kind="bar",
    title="Average Scores by Country and Subject",
    xlabel="Country",
    ylabel="Average Score",
    rot=45
)
plt.legend(title="Subject")
plt.tight_layout()
plt.show()
اینجا برای هر کشور، چندین میله کنار هم داریم (یکی برای ریاضی، یکی برای زیست، یکی برای بوم‌شناسی).
------------------------
مثال دوم رسم دروس بر حسب سن

sorted_df = df.sort_values(by="age")
sorted_df[["math","biology","chemistry","age"]].plot(kind="bar",x="age",edgecolor = "black",ylabel="scores",title = "students score by age chart")
# همچنین میتوان ستون ها رو در ابتدا نداد و دی اف پلاتی 
# با ایکس برابر سن و ایگریک برابر با لیستی از ستون های نمرات ساخت
plt.show()
-----------------------------------
۵. رسم میله‌ای افقی (kind="barh")
SORTING: Not Required


روش پانداس (توصیه‌شده):

country_means = df.groupby("country")["math"].mean().sort_values(ascending=False)

country_means.plot(
    kind="barh",        #وقتی یک اچ به بار اضافه میکنیم نوع نمودار عوض شده و افقی میشه
    title="Average Math Score by Country",
    xlabel="Average Math Score",
    ylabel="Country"
)
plt.show()
اینجا محور ایکس نمره است و محور ایگریک کشورها. وقتی نام کشورها طولانی است یا تعدادشان زیاد است، این نمودار از bar عمودی بهتر است، چون اسم‌ها افقی نوشته می‌شوند و خواناترند.
# ----------------------------------------------------
کی از متپلات‌لیب خالص برای نمودار میله‌ای استفاده کنیم؟

مثال: اضافه کردن عدد دقیق بالای هر میله

پانداس این کار را به صورت خودکار انجام نمی‌دهد. برای اضافه کردن برچسب مقدار روی هر میله، به متپلات‌لیب نیاز داری:

country_means = df.groupby("country")["math"].mean().sort_values()

# رسم با پانداس
ax = country_means.plot(kind="bar", title="Average Math Score by Country", xlabel="Country", ylabel="Average Math Score", rot=45)

# اضافه کردن متن بالای هر میله با متپلات‌لیب
for i, value in enumerate(country_means.values):
    ax.text(i, value + 0.5, str(round(value, 1)), ha="center", va="bottom", fontsize=9)

plt.tight_layout()
plt.show()
----------------------------
hist✴️
کاربرد: رسم هیستوگرام برای نمایش توزیع فرکانس داده‌ها

🧩 شکل رایج
plt.hist(data, edgecolor='black')
نکته: داتا به جای محور ایکس قرار میگیرد (البته نه به صورت خام بلکه به صورت بازه ای)
و محور ایگریک فراوانی (frequency)این داتاست
که خود مت پلات لیپ آن را میسازد

⚙️ پارامترها
data: داده‌ها برای هیستوگرام
پیش‌فرض: ندارد
بهتر: آرایه واقعی داده‌ها

bins: تعداد بازه‌ها
پیش‌فرض: 10
بهتر: بسته به پراکندگی داده‌ها

color: رنگ ستون‌ها
پیش‌فرض: 'blue'
بهتر: رنگ دلخواه

edgecolor: رنگ مرز ستون‌ها
پیش‌فرض: None
بهتر: 'black'

alpha: شفافیت نمودار
پیش‌فرض: 1.0
بهتر: 0.5 تا 0.8 برای نمودارهای چندگانه
------------------------------------------------------
کاربرد درست هیستوگرام (Histogram)
----------------------------------------------------------
نمودار هیستوگرام برای نمایش توزیع فراوانی یک متغیر عددی استفاده می‌شود. برخلاف نمودار میله‌ای که مقادیر را در دسته‌های مشخص مقایسه می‌کند، هیستوگرام داده‌ها را به بازه‌های مساوی (bins) تقسیم می‌کند و نشان می‌دهد که چند داده در هر بازه قرار دارد.

SORTING: Not Required برای هیستوگرام. ترتیب داده‌ها در رسم هیستوگرام هیچ تأثیری ندارد.

۱. هیستوگرام یک درس (توزیع نمرات)
هدف: ببینیم نمرات ریاضی چطور پخش شده‌اند. چند نفر نمره ۰ تا ۵ گرفته‌اند؟ چند نفر ۵ تا ۱۰؟ و الی آخر.

روش پانداس (توصیه‌شده):

df["math"].plot(
    kind="hist",
    bins=10,
    title="Distribution of Math Scores",
    xlabel="Math Score",
    ylabel="Frequency"
)
plt.show()
محور X: بازه‌های نمرات (مثلاً ۰-۲، ۲-۴، ۴-۶، ...)

محور Y (ارتفاع میله‌ها): تعداد دانش‌آموزانی که نمره‌شان در آن بازه قرار دارد.

bins: تعداد بازه‌ها. هرچه بیشتر باشد، نمودار دانه‌دانه‌تر و جزئی‌تر می‌شود. هرچه کمتر باشد، کلی‌تر.

نکته مهم:
بازه هایی که خود هیستوگرام برای داده ها تعریف میکند دقیقا با داده ها منطبق نیستند
این مشکل برای داده هایی مثل نمرات، بزرگ تر است یعنی 
با تماشای هیستوگرام دقیقا نمیفهمیم فراوانی هر بازه عددی چقدر است 
پس بهتر است خودمان بازه هایی برای هر نمره تعریف کنیم با نامپی مثلا 
bins= np.arange((df["math"].min()-0.5),(df["math"].max()+0.5),1)
plt.hist(df["math"],bins= np.arange((df["math"].min()-0.5),(df["math"].max()+0.5),1),edgecolor = "black")
درنتیجه عدد هربازه دقیقا در وسط ستون فراوانی مربوط به آن بازه قرار میگردو به آسانی قابل تفسیر است
-------------------------------------------------------------
۲. هیستوگرام چند درس با هم (مقایسه توزیع‌ها)
هدف: مقایسه توزیع نمرات ریاضی، زیست و بوم‌شناسی. آیا زیست نمراتش جمع‌تر است یا ریاضی؟

روش پانداس (توصیه‌شده):

df[["math", "biology", "ecology"]].plot(
    kind="hist",
    bins=10,
    alpha=0.5,  # شفافیت برای دیدن همپوشانی
    title="Distribution of Scores by Subject",
    xlabel="Score",
    ylabel="Frequency"
)
# plt.legend(["Math", "Biology", "Ecology"]) 
alpha=0.5 باعث می‌شود میله‌های روی هم افتاده دیده شوند.

هر درس با یک رنگ متفاوت رسم می‌شود.

پانداس خودکار legend را می‌سازد (ولی بهتر است دستی هم تنظیمش کنی).
# -------------------------------------------------
۳. هیستوگرام با تنظیم دقیق بازه‌ها
هدف: تعیین دستی محدوده و پهنای بازه‌ها.

روش پانداس (توصیه‌شده):

python
df["math"].plot(
    kind="hist",
    bins=[0, 5, 10, 12, 14, 16, 18, 20],  # بازه‌های سفارشی
    title="Math Scores with Custom Bins",
    xlabel="Math Score",
    ylabel="Frequency",
    edgecolor="black"  # مرز مشکی دور میله‌ها برای وضوح
)
plt.show()
bins می‌تواند یک لیست از مرزهای بازه‌ها باشد: [0, 5, 10, 12, 14, 16, 18, 20]

بازه اول: ۰ تا ۵

بازه دوم: ۵ تا ۱۰

بازه سوم: ۱۰ تا ۱۲

و الی آخر

edgecolor="black" دور هر میله را خط می‌اندازد تا از هم جدا دیده شوند.
# -----------------------------------------
۴. هیستوگرام نرمال‌شده (چگالی احتمال)
تنها یک پارامتر جدید به نمودار اضافه میکنیم که عبارت است از 
density =True
و نام محور ایگریک رو هم میگذاریم دنسیتی ولی مفهوم نموداری که رسم میشود را فعلا درک نکرده ایم

هدف: به جای فراوانی مطلق، چگالی احتمال را نشان دهیم (مساحت کل نمودار = ۱).

روش پانداس (توصیه‌شده):

df["math"].plot(
    kind="hist",
    bins=10,
    density=True,  # نرمال‌سازی
    title="Probability Density of Math Scores",
    xlabel="Math Score",
    ylabel="Density",
    edgecolor="black"
)
plt.show()
با density=True، ارتفاع میله‌ها طوری تنظیم می‌شود که مساحت کل هیستوگرام برابر با ۱ شود.

این برای مقایسه توزیع‌ها با اندازه‌های نمونه متفاوت بسیار مفید است.
# -------------------------------------------------------
۵. هیستوگرام تجمعی (Cumulative)
یک نمودار پلکانی ساخته میشود با مفهومی که فعلا نمیدانم چیست
این نمودار نیز مثل نمودار مثال چهار تنها با افزودن یک پارامتر جدید و تغییر نام محور ایگریک ساخته میشود

هدف: دیدن اینکه چند درصد دانش‌آموزان نمره‌ای کمتر از یک مقدار خاص گرفته‌اند.

روش پانداس (توصیه‌شده):

df["math"].plot(
    kind="hist",
    bins=10,
    cumulative=True,  # تجمعی
    title="Cumulative Distribution of Math Scores",
    xlabel="Math Score",
    ylabel="Cumulative Frequency",
    edgecolor="black"
)
plt.show()
هر میله، مجموع فراوانی خودش و تمام میله‌های قبلی را نشان می‌دهد.

میله آخر همیشه برابر با تعداد کل دانش‌آموزان است (یا ۱ اگر density=True باشد).
# --------------------------------------------------
۶. کی از متپلات‌لیب خالص برای هیستوگرام استفاده کنیم؟
...درج این مثال برای بررسی و درک بعدی ...

مثال: رسم همزمان هیستوگرام و منحنی چگالی (KDE)

پانداس نمی‌تواند همزمان هیستوگرام و یک منحنی نرم روی آن رسم کند. برای این کار به متپلات‌لیب و SciPy نیاز داری:

from scipy.stats import gaussian_kde
import numpy as np

# داده‌ها
data = df["math"].dropna()

# رسم هیستوگرام
plt.hist(data, bins=10, density=True, alpha=0.6, color="blue", edgecolor="black")

# محاسبه و رسم منحنی چگالی
kde = gaussian_kde(data)
x_range = np.linspace(data.min(), data.max(), 100)
plt.plot(x_range, kde(x_range), "r-", linewidth=2, label="Density Curve")

plt.title("Histogram with Density Curve")
plt.xlabel("Math Score")
plt.ylabel("Density")
plt.legend()
plt.show()
# ----------------------------------------
۷. ساب‌پلات برای هیستوگرام چند درس
هدف: دیدن توزیع هر درس در یک نمودار مجزا، ولی کنار هم.

روش پانداس (توصیه‌شده):

df[["math", "biology", "ecology"]].plot(
    kind="hist",
    subplots=True,
    layout=(3, 1),
    figsize=(8, 10),
    bins=10,
    title="Distribution of Scores by Subject",
    xlabel="Score",
    ylabel="Frequency",
    edgecolor="black"
)
plt.tight_layout()
plt.show()
subplots=True: هر ستون در یک ساب‌پلات جداگانه رسم می‌شود.

layout=(3, 1): سه ردیف، یک ستون.
------------------------------------------------------

--------------------------------------------------
⚜️ نیاز به ایکس در انواع نمودار مت‌ پلات‌ لیب خالص⚜️

#انواع نمودار matplotlib 
#ایکس در نمودار ها 

❌ = نیاز ندارد
⭕ = اختیاری
✔️ = نیاز دارد
-----------------------
hist
❌ نیاز ندارد
فقط ایگریک می‌گیرد
محور ایکس به‌صورت خودکار بازه‌ها (bins) است
plt.hist(y)
-------------------------------------
boxplot
❌ نیاز ندارد
ایکس  صرفاً جایگاه رسم است
ایکس معنا ندارد و توسط خود نمودار ساخته می‌شود
plt.boxplot(y)
-----------------------------------
plot (line plot)

⭕ اختیاری
حالت‌ها:
1- بدون درج x
ایکس به‌صورت خودکار، ساخته می‌شود
plt.plot(y)

2- وقتی y یک Series است ولی ایندکس معنا ندارد و فقط ترتیب است
در این حالت x از روی y ساخته می‌شود با فرمول زیر 
plt.plot(range(len(y),y)

3- وقتی هر دو محور معنا دارند.

plt.plot(df["col1"], df["col2"])

4- وقتی y سری حاصل از دسته بندی value_countd  است و ایندکس آن معنا دارد 
در این حالت x از ایندکس سری و y از والیوی سری بدست می آید . 
s= df["col_name"].value_counts()

plot.plot(s.index,s.values)
-------------------------------
scatter
✔️ نیاز دارد
هر دو محور باید دادهٔ واقعی باشند
 ⏪ایکس ساختگی معمولاً تحلیل را بی‌معنا می‌کند

plt.scatter(x, y)
پس x میتوان از ستون داتافریم، آرایه یا ایندکس سری بیاید ولی ایکس ساختگی نداریم. 
---------------------------------
bar
✔️ نیاز دارد
ایکس  باید دسته، نام، یا مفهوم قابل مقایسه باشد.
ایگریک،  مقدار هر دسته است
plt.bar(x, y)
پس x میتوان  از ستون داتافریم، آرایه یا ایندکس سری بیاید. ولی x ساختگی نداریم. 
--------------------------------------
value_counts bar
✔️ نیاز دارد

ایکس  از index سری می‌آید (دسته‌ها)
ایگریک از values می‌آید (تعداد)

s = df["col"].value_counts()
plt.bar(s.index, s.values)
مثال :
رسم از متپلات لیب
grouped = df["country"].value_counts()
plt.plot(grouped.index,grouped.values)
plt.xticks(rotation = 45)
plt.show()
رسم از پانداس:
grouped = df["country"].value_counts()
grouped.plot.line()
plt.show
------------------------------
جمع‌بندی نهایی (قانون ذهنی)
🔰نمودارهای توزیعی و خلاصه → x لازم نیست
🔰نمودارهای رابطه‌ای و مقایسه‌ای → x باید معنا داشته باشد
🔰اگر x در داده وجود ندارد → فقط در plot می‌توان آن را مصنوعی و ساختگی  ساخت. 
 -------------------------------------
⚜️چه نوع نموداری را رسم کنیم؟ نمودار ما نیاز به ایکس دارد یا نه ؟ ⚜️
این موضوع در رسم نمودار از متپلات لیب خالص موضوعیت دارد و نه در رسم از پانداس 
چون پانداس خودش اغلب ایندکس را به جای محور ایکس می آورد مگر در اسکتر که نیاز به درج دستی ایکس و ایگریک هست

#تشخیص نوع نمودار درست 
#نوع درست نمودار
#نیاز به x 

الگوریتم تشخیص ایکس (سه سؤال طلایی)
سؤال ۱-🟣این نمودار فقط برای دیدنِ توزیع است؟

مثال
پخش نمره‌ها
پراکندگی داده
میانه و چارک‌ها

اگر بله ⟶
❌ x نساز
❌ x از داده نگیر

نمودار درست:
plt.hist(y)
plt.boxplot(y)
----------------
سؤال ۲) اگر ایکس را حذف کنم، نمودار هنوز معنی دارد؟

اگر بله ⟶
⭕ x معنا ندارد
✔️ می‌توانی x را بسازی

نمودار درست:

plt.plot(y)
# یا
x = range(len(y))
plt.plot(x, y)
------------------------------
سؤال ۳)آیا چیزی در مسئله هست که «روی محور افقی» باید دیده شود؟

مثل:
زمان
نام افراد
دسته‌ها
نمره‌ها
گروه‌ها

اگر بله ⟶
✔️ x باید از داده بیاید
❌ x نساز

نمودار درست:
plt.bar(df["student"], df["math"])
plt.scatter(df["age"], df["score"])
plt.bar(s.index, s.values)
---
نسخهٔ فوق‌فشرده (حفظی)

توزیع؟        → x نمی‌خواهم
روند؟         → x می‌تواند مصنوعی باشد
مقایسه/رابطه؟ → x باید واقعی باشد
---

تست نهایی خودکنترلی
قبل از نوشتن کد، این جمله را کامل کن:
> «محور افقی این نمودار نشان‌دهندهٔ ____ است»
اگر نتوانستی چیزی معنادار بگویی → x نساز
اگر فقط گفتی «ردیف‌ها» → x بساز
اگر گفتی «زمان / شخص / دسته / مقدار واقعی» → x را از داده بگیر
----------------------------------
جمع‌بندی ذهنی نهایی

x یا:

وجود ندارد (توزیع)
ساختگی است (ترتیب)
واقعی است (مفهوم)
------------------------------------
----------------------------------
⚜️رسم نمودار مستقیما از پانداس ⚜️

🔰رسم نمودار از پانداس شبیه رسم از مت پلات لیب است ولی کمی کار را ساده تر میکند و کد نویسی را کمتر.
🔰 در رسم از پانداس،  فقط یک تابع داریم که  plot() است و نوع نمودار به صورت پارامتر kind به این تابع داده میشوند. 
🔰 همچنان نیاز به ایمپورت متپلات لیب داریم چون مراحل نمایش و ذخیره نمودار مثل قبل است و تنها ساخت نمودارها از پانداس است. 

🧠 Pandas Plot – Cheat Sheet
-----------------------------
:در رسم نمودار از پانداس دو شیوه متفاوت داریم
1)>>>>> df.plot() داتافریم پلات 
نکات:

پارامترهای ایگریک و ایکس(اگر لازم باشد) باید از درونِ پلات درج شوند
ایکس و ایگریک نمیتوانند سری یا آرایه باشند
مثلا 
❌ df["col_name"]
✅ df.columns[1]
✅ "math score"
بلکه باید رشته و معادل نام رشته ای یک ستون باشند
-------------------
حتی بدون درج پارامتر لیبل در پلات ، همه نمودارها لیبل دارند
--------------------
2)>>>>>  series.plot(label=df)
مثلا df["col_name"].plot()
نیازی به درج پارامترهای ایکس و ایگریک از درون پلات ندارد
----------------------------
هم در سری پلات و هم داتافریم پلات فیلتر تعداد ردیف ها قبل از پلات باشد
مثلا
df.head(20).plot(y="col1")
df["col1"].tail(20).plot(kind="line")
--------------------------------------------
----------------------------
✅ plot✴️ (Line)
نمودار خطی
کاربرد: نمایش روند و تغییرات داده

df.plot() دی اف پلات خطی از پانداس 

پارامترهای مهم:
kind برای نمودار خطی لازم نیست 
x →  اگرآن را  ندهیم، ایندکس ستون ایگریک جایش را میگیرد (پیش‌فرض: index)
اگر بدهیم، نام یک ستون واحد از داتافریم

y →  اسم ستون یا لیستی از نام ستون‌های داتافریم
color → رنگ
linestyle → '-' '--' ':'
marker → 'o' '*' 'x' '+'
linewidth → ضخامت خط
ضخامت خط : پیشفرض 1 
title → عنوان
grid → True / False default
نمودار را درون یک شبکه رسم میکند
figsize → (w, h)
اندازه نمودار به اینچ ( عرض, ارتفاع)
پیشفرض figsize=(6.4,4.8)

legend → True / False default 

مثال ها :
import matplotlib.pyplot as plt
fig1,axs1=plt.subplots()
df.plot(x="year", y="sales", marker="o", grid=True,ax=axs1)
# df.tail(20).plot(y=df.columns[-2],marker="*",linestyle="--",ax=axs1)
# df.tail(20).plot(y="writing score",marker="*",linestyle="--",ax=axs1)
اگر ایکس را بدهیم و ایگریک را نه، تمام ستون های عددی دی اف به عنوان ایگریک رسم میشوند
ایگریک میتواند اسم یک تک ستون یا لیستی از اسامی ستون های عددی باشد
-------------------------------------
df["col_name"].plot(kind="line") سری پلات نمودار خطی از پانداس

نمونه:
import matplotlib.pyplot as plt
fig1,axs1=plt.subplots()
# df.iloc[:,6].head(20).plot(kind="line",marker="*",linestyle="--",ax=axs1)
# df["writing score"].tail(30).plot(kind="line",marker="*",linestyle="--",ax=axs1)
plt.show()
----------------------------------------
📊 bar✴️ (ستونی)

کاربرد: مقایسه مقادیر گسسته

df.plot(kind="bar") دی اف پلات میله ای 

پارامترهای مهم:

x →  اگرآن را  ندهیم، ایندکس ستون ایگریک جایش را میگیرد (پیش‌فرض: index)
اگر بدهیم، نام یک ستون واحد از داتافریم

y →  اسم ستون یا لیستی از نام ستون‌های داتافریم

color
width
title
grid

نمونه:

import matplotlib.pyplot as plt
fig1,axs1=plt.subplots()
df.plot(x="city", y="population", kind="bar",ax=axs1)
df.tail(20).plot(kind="bar",y="writing score",color="green",edgecolor="black",ax=axs1)
df.tail(20).plot(kind="bar",y=df.columns[-2],color="green",edgecolor="black",ax=axs1)
plt.show()
------------------------------
df["col_name"].plot("kind="bar") سری پلات میله ای از پانداس

مثال ها
df["writing score"].tail(25).plot(kind="bar",color="green",edgecolor="black",ax=axs1)
df[df.columns[6]].tail(25).plot(kind="bar",color="green",edgecolor="black",ax=axs1)
df.iloc[:,6].tail(25).plot(kind="bar",color="green",edgecolor="black",ax=axs1)

---------------------------------------
📉 hist✴️ (هیستوگرام)
------------------------
دی‌اف پلات هیستوگرام (DataFrame.plot hist)
df.plot(kind="hist")

کاربرد: بررسی توزیع داده

پارامترهای مهم:
bins → تعداد بازه‌ها
color
alpha → شفافیت
grid
edgecolor

مثال‌ها:
fig1, axs1 = plt.subplots()
df.plot(y="writing score", kind="hist", bins=20, ax=axs1)
df.tail(50).plot(y=df.columns[-2], kind="hist", bins=15, ax=axs1)
plt.show()
-----------------------------
سری پلات هیستوگرام (Series.plot hist)

df["col_name"].plot(kind="hist")

مثال‌ها:

df["writing score"].plot(kind="hist", bins=20)
df[df.columns[6]].plot(kind="hist", bins=15)
df.iloc[:, 6].plot(kind="hist", bins=10)
-------------------------------
📦 box✴️ (جعبه‌ای)
کاربرد: تشخیص پراکندگی و داده پرت

df.plot(kind="box")
پارامترهای مهم:

vert → True / False
grid
title

نمونه:
df[["height", "weight"]].plot(kind="box")
plt.show()
---------------------------------------
🔘 scatter✴️ (پراکندگی)
کاربرد: بررسی رابطه دو متغیر

دی‌اف پلات اسکتر (DataFrame.plot scatter)

df.plot(kind="scatter", x="col_x", y="col_y")

نکته مهم:
نمودار اسکتر فقط با دی اف رسم می‌شود (سری پلات اسکتر ندارد).
-------------------
پارمترهای مهم:

x
y
ایکس و ایگریک نمیتوانند سری باشند
مثلا 
❌ df["col_name"]
✅ df.columns[1]
✅ "math score"
بلکه باید رشته و معادل نام رشته ای یک ستون باشند

color
s → اندازه نقاط
alpha
----------------
مثال‌ها:

fig1, axs1 = plt.subplots()
df.plot(kind="scatter", x="math score", y="writing score", ax=axs1)
df.tail(50).plot(kind="scatter", x=df.columns[3], y=df.columns[6], ax=axs1)
plt.show()
--------------------------------
🥧 pie✴️ (دایره‌ای)
کاربرد: نمایش نسبت‌ها

df["col"].plot(kind="pie")
پارامترهای مهم:

autopct
startangle
ylabel → '' (برای حذف)

نمونه:
df["gender"].plot(kind="pie", autopct="%1.1f%%", ylabel="")
plt.show()
--------------------------
💾 ذخیره نمودار (همه حالت‌ها)

plt.savefig("plot.png")
plt.show()

بدون نمایش:
plt.savefig("plot.png")
plt.close()
---------------------------------
🧩 الگوی حفظی نهایی (VS Code)

import matplotlib.pyplot as plt

df.plot(...)
plt.title(...)
plt.savefig("file.png")
plt.show()

# ============================================================
# جزوه جامع نامپی برای کامپیوتر ویژن
# ============================================================
import numpy as np
# شیوه درست ایمپورت کردن نامپی
# ============================================================
# ۱-۲. ساخت آرایه (Array)
# ============================================================
# a = np.array(data)
# data = اغلب لیست، گاهی تاپل
# در ساخت ماتریس، لیستی از لیست‌ها

# پارامترها:
# dtype= پیش‌فرض مطابق با لیست اولیه / int, float
# گاهی str یا bool

# ============================================================
# ۱-۳. تبدیل آرایه به لیست
# ============================================================

.tolist()
# برای تبدیل مجدد آرایه (بردار/ماتریس) به لیست
new = a.tolist()

# ============================================================
# ۲. ساخت بردار (Vector)
# ============================================================
نحوه به خاطر سپاری این توابع در توابع سازنده ماتریس بهتر توضیح داده شده

# ۲-۱. روش کلی
v = np.array(data)
# data = لیستی از مقادیر عددی مثبت یا منفی
# تعداد پارامترها بدون محدودیت

# ۲-۲. ساخت بردار با توابع کمکی
np.zeros(n)          # بردار n خانه‌ای پر از صفر
np.ones(n)           # بردار n خانه‌ای پر از یک
np.full(n, value)    # بردار n خانه‌ای پر از عدد دلخواه
np.arange(n)         # بردار با اعداد ترتیبی از صفر تا n-1
np.arange(start, stop, step)  # بردار با بازه و گام مشخص
np.linspace(start, stop, num) # بردار با اعداد در رنج دلخواه و گام مساوی
np.random.rand(n)    # بردار n خانه‌ای با اعداد تصادفی بین 0 و 1
np.random.randint(min, max, size=n)  # بردار با اعداد تصادفی در رنج دلخواه

# ۲-۳. توابع کمکی برای تبدیل شکل بردار
reshape(n, 1)   # تبدیل بردار به ستون برای ضرب ماتریسی

reshape(1, n)   # تبدیل بردار به سطر برای ضرب ماتریسی
برای تبدیل به سطر یا ستون ان مساوی با منفی یک است 
---------------------------------
عدد اول ریشیپ، تعداد سطر است و دومی ستون، هر کدام را منفی یک بگذاریم یعنی خودت حساب کن
عدد دیگر را باید یک بدهیم مگر اینکه از قبل یا به هر روشی تعداد عناصر آرایه را بدانیم چون اعداد نامناسب سبب ارور میشوند
مثلا اگر تعداد عناصر 17 باشد و ما اینطوری ریشیپ کنیم ارور میده
a.reshape(-1,5)  نتیجه ارور

# ============================================================
# ۳. ساخت ماتریس (Matrix)
# ============================================================

# ۳-۱. روش کلی
m = np.array([[list1], [list2], [list3]])
# فرمول ماتریس (آرایه‌ای با لیستی از لیست‌ها)

# ۳-۲. ساخت ماتریس با توابع کمکی
نکته: اگر تابع یک آرایه جدید میسازد تقریبا همیشه بایددرون یک پرانتز جداگانه ابعاد را بدهی
برای بردار صرفا یک عدد کافیست (تعداد پارامتر ها)
np.zeros((m, n))           # ماتریس m×n پر از صفر
np.ones((m, n))            # ماتریس m×n پر از یک
np.full((m, n), value)     # ماتریس m×n پر از عدد دلخواه
np.empty(shape/یا همان(m,n))
# -----------------------------
اگر تابع، خودش از روی یک بازه یا داده، اندازه را تشخیص می دهد پس شیپ نیاز نیست
ولی برای ماتریس باید ریشیپ کنیم 

np.arange(total).reshape(m, n)  # ماتریس با اعداد ترتیبی
np.linspace(start, stop, num).reshape(m, n)  # ماتریس با رنج و گام مشخص
np.array(data)
---------------------------
در ماتریس همانی، صرفا یک عدد برای ساخت ماتریس کافیست
np.eye(n)
np.identity(n) معادل فرمول بالاتر#
----------------------------------
در توابع تصادفی یا همان راندوم، پارامتری برای شیپ یا سایز نداریم و باید ابعاد را مستقیما نوشت
البته راندوم راندیت، پارامتر سایز دارد ولی میتوانیم نام آن را ننویسیم

np.random.rand(m, n)       # ماتریس m×n با اعداد تصادفی بین 0 و 1
np.random.randint(min, max, size=(m, n))  # ماتریس با اعداد تصادفی در رنج دلخواه
# امکان تکراری بودن مقادیر در دو تابع بالا وجود دارد
# ۳-۳. نکات مهم در ساخت ماتریس
# در reshape، حاصلضرب ابعاد باید برابر با تعداد کل عناصر باشد
# در np.linspace، حاصلضرب m * n باید برابر با num باشد
# برای np.random.rand بهتر است از .round() برای گرد کردن اعداد استفاده شود

# ============================================================
# ۴. توابع آماری
# ============================================================

# ۴-۱. توابع آماری روی بردارها
np.size(v)     # تعداد (برابر با len(v))
np.sum(v)      # مجموع
np.mean(v)     # میانگین
np.std(v)      # انحراف معیار
np.var(v)      # واریانس
np.max(v)      # بیشینه
np.min(v)      # کمینه
np.median(v)   # میانه
# توجه: NumPy تابع mode ندارد

# ۴-۲. توابع آماری روی ماتریس
اگر در توابع آماری ماتریس، اکس را تعریف کنیم (چه برای ستون و چه ردیف ) به جای یک عدد واحد یک آرایه (بردار ) بدست می آوریم

np.sum(m)           # مجموع کل ماتریس
np.sum(m, axis=0)   # مجموع ستون‌ها
np.sum(m, axis=1)   # مجموع ردیف‌ها

# میانگین
np.mean(m)          # میانگین کل ماتریس
np.mean(m, axis=0)  # میانگین ستون‌ها
np.mean(m, axis=1)  # میانگین ردیف‌ها

# انحراف معیار
np.std(m)           # انحراف معیار کل ماتریس
np.std(m, axis=0)   # انحراف معیار ستون‌ها
np.std(m, axis=1)   # انحراف معیار ردیف‌ها

# واریانس
np.var(m)           # واریانس کل ماتریس
np.var(m, axis=0)   # واریانس ستون‌ها
np.var(m, axis=1)   # واریانس ردیف‌ها

# بیشینه
np.max(m)           # بیشینه کل ماتریس
np.max(m, axis=0)   # بیشینه ستون‌ها
np.max(m, axis=1)   # بیشینه ردیف‌ها

# کمینه
np.min(m)           # کمینه کل ماتریس
np.min(m, axis=0)   # کمینه ستون‌ها
np.min(m, axis=1)   # کمینه ردیف‌ها

# میانه
np.median(m)        # میانه کل ماتریس
np.median(m, axis=0)  # میانه ستون‌ها
np.median(m, axis=1)  # میانه ردیف‌ها

# ۴-۳. تعداد عناصر ماتریس
m.size          # تعداد کل عناصر
m.shape[0]      # تعداد ردیف‌ها
m.shape[1]      # تعداد ستون‌ها
البته به صورت زیر نیز نوشته میشوند
np.size(m), np.shape(m)
np.size(v), np.shape(v)   برای بردار
# ============================================================
# ۵. توابع ریاضی و مثلثاتی (Element-wise)
# ============================================================

# --- مثلثاتی ---
توابع مثلثاتی در نامپی، روی تک تک عناصر آرایه اعمال شده و یک آرایه جدید و مشابه با مقادیر جدید میسازند یعنی روی بردار، بردار میسازند و روی ماتریس هم ماتریس

np.sin(v)          # سینوس هر عنصر
np.cos(v)          # کسینوس هر عنصر
np.tan(v)          # تانژانت هر عنصر
np.arcsin(v)       # آرک سینوس هر عنصر
np.arccos(v)       # آرک کسینوس هر عنصر
np.arctan(v)       # آرک تانژانت هر عنصر

np.sinh(v)         # سینوس هیپربولیک
np.cosh(v)         # کسینوس هیپربولیک
np.tanh(v)         # تانژانت هیپربولیک

# --- توانی و ریشه ---
np.sqrt(v)         # ریشه دوم (جذر)
np.cbrt(v)         # ریشه سوم
np.exp(v)          # تابع نمایی (e^x)
np.exp2(v)         # تابع نمایی به مبنای 2
np.power(v, 3)     # توان (v^3)
np.power(2, v)     # 2^v
در تابع پاور پارامتر اول (چه عدد باشد و چه یک آرایه) 
پایه ی توان و دومی خود توان است خود توان میتواند یک آرایه باشد
اگر هم توان و هم پایه آن آرایه باشند باید این دو از قوانین برودکاسیتنگ پیروی کنند
اگر توان منفی باشد نامپی ارور میدهد مگر اینکه پایه رو فلوت کنیم

# --- لگاریتم ---
np.log(v)          # لگاریتم طبیعی (ln)
np.log2(v)         # لگاریتم مبنای 2
np.log10(v)        # لگاریتم مبنای 10
np.log1p(v)        # لگاریتم (1 + x)

# --- قدرمطلق و گرد کردن ---
np.abs(v)          # قدر مطلق
np.ceil(v)         # گرد کردن به بالا
np.floor(v)        # گرد کردن به پایین
np.round(v)        # گرد کردن معمولی
np.clip(v, 0, 1)   # محدود کردن بین min و max
np.sign(v)         # علامت هر عنصر (-1, 0, 1)

# ============================================================
# ۶. توابع جبر خطی و ماتریسی
# ============================================================

# ۶-۱. ضرب ماتریسی
# شرط تحقق ضرب ماتریسی
if A.shape[1] == B.shape[0]:
    C = A @ B
    print(C)
else:
    print("ابعاد سازگار نیستند، ضرب انجام نمی‌شود")

# دو روش برای ضرب ماتریسی
c = A @ B
c = np.dot(A, B)
# ------------------------------------
# ۶-۲. سایر توابع جبر خطی
np.linalg.norm(v)        # طول بردار (نرم)
np.linalg.det(m)         # (فقط برای ماتریس های مربعی)دترمینان ماتریس
np.linalg.inv(m)         # معکوس ماتریس (فقط مربعی)

# شرط معکوس داشتن ماتریس (صفر نبودن دترمینان)
if det != 0:
    m_inv = np.linalg.inv(m)
    print("ماتریس معکوس:", m_inv)
else:
    print("ماتریس معکوس ندارد!")

np.linalg.svd(m)         # تجزیه SVD
np.linalg.eig(m)         # مقادیر ویژه (فقط مربعی)
np.linalg.matrix_rank(m) # رتبه ماتریس
np.transpose(m)          # ترانهاده ماتریس
np.trace(m)              # اثر ماتریس (مجموع قطر اصلی)
# ------------------------------------------------
# ۶-۳. حل دستگاه معادلات - چهار ابزار پایه‌ای
# ----------------------------------------------

# 🟢 solve - برای دستگاه‌های مربعی با ماتریس معکوس‌پذیر
x = np.linalg.solve(A, b)

# مثال:
# 2x + y = 5
# x - y = 1
A = np.array([[2, 1], [1, -1]])   #ماتریس ضرایب 
b = np.array([5, 1])      #بردار خروجی(بردار نتایج، یا بردار سمت راست)
x = np.linalg.solve(A, b)   #حل دستگاه(بردار مجهول ها)

نکته : تعداد عناصر بردار خروجی همیشه و در هر نوع ماتریسی 
باید با تعداد ردیف های ماتریس(تعدادمعادلات) برابر باشد وگرنه نامپی ارور میده
--------------
#  یک مثال از حل کردن دستگاه معادلات با سولف
A = np.array([[2, 1], [1, -1]])
b = np.array([5, 1])

# آیا مربعی است
p(A.shape)
# ------
# آیا دترمینان دارد؟ اگر ترو بود، دارد و معکوس پذیر هم هست
p(np.linalg.det(A) !=0) 
# -------
x= np.linalg.solve(A,b)
p(x)
# -----------------------------------------------------
# 🟢 inv - معکوس صریح ماتریس مربعی
A_inv = np.linalg.inv(A)
x = A_inv @ b  #حل دستگاه با معکوس ماتریس و بردار خروجی
این یک روش مکروه برای حل معادلاتی است که سولف حل میشوند
توصیه نمیشود به سبب دقت پایین و اضافه کاری بیشتر
# --------------------------------------------------------
# 🟢 lstsq - برای دستگاه‌های بیش‌تعداد و داده‌های نویزی
x = np.linalg.lstsq(A, b, rcond=None)[0]

منظور از دستگاه بیش تعداد دستگاهی است با تعداد معادلات(تعداد سطر ها) بیشتر از مجهول هاست (ستون ها)
در اینگونه دستگاه های معادلات جواب دقیق نداریم و باید بهترین تقریب را بیابیم
p(A.shape[0] > A.shape[1]) 

پارامتر rcond=None یا منفی یک 
فیلتری برای صفر کردن مقادیر خیلی کوچک وقتی والیوی منفی یک یا ناون بهش میدیم یعنی 
نامپی خودت فیلتر پیشفرض  برای یافتن بهترین تقریب را بگذار خودت را بگذار و از این بابت ارور نده
# -------------
[0] این یعنی تنها اولین جواب این حل رو به ما بده
زیرا که جواب های دیگرِ آی اس تی اس کیو 
چیزهایی مثل رنک ماتریس و ... هستند و فعلا به کار ما نمی آیند
------------------------------------------
مفهوم نویزی بودن داتا
--------------------------------------
نویزی بودن تنها وقتی موضوعیت دارد که داده ها از دنیای واقعی باشند مثلا از تصویر، سنسورها و ...
در مرحله آموزش، ماتریس ضرایب و بردار خروجی اغلب منبع ریاضی و توابع آرایه ساز دارند
پس هیچ وقت نویزی نمیشوند. 
هر گاه solve @ A = b 
باشد داده نویزی نیست ولی اگر نتیجه این ضرب متفاوت با بردار نتایج باشد 
و این تفاوت بزرگ باشد داده نویزی است و باید نتیجه سولف را کنار گذاشته و از 
istsq استفاده کنیم
# ---------------------------------------------------
# 🟢 pinv - شبه‌معکوس مور-پنروز برای دستگاه‌های کم‌تعداد
کم تعداد یعنی تعداد ردیف ها کمتر از ستون ها 
اینگونه دستگاه ها بی نهایت جواب دارند پس باید با شبه معکوس یک جواب معقول و پایدار بیابیم
x = np.linalg.pinv(A) @ b
----------------------------
چند مثال برای حل دستگاه های معادلات خطی 
# ----------------------------------------------------------------
import numpy as np
a1 = [[1,2],[3,5]]
b1=[7,16]

x1= np.linalg.solve(a1,b1)
# p(x1)
# ---------------------------------
a2 =np.array([[2,1],[4,3],[6,5]])
b2 = np.array([15,11,17])
p(a2.shape)
x2 =np.linalg.lstsq(a2,b2,rcond=-1)[0]
p(x2)
# -------------------------------
a3 =np.array([[2,14,20],[3,6,5]])
b3 = np.array([15,11])
x3= np.linalg.lstsq(a3,b3,rcond=-1)[0]
p(x3)
# ----------------------------
a4 = [[2,1,3],[1,0,1],[1,4,2]]
b4 = [10,4,13]
x4=np.linalg.solve(a4,b4)
p(np.linalg.det(a4) !=0)
p(x4)
# ---------------------------------
a5 = [[2],[4],[6],[8]]
b5 = [3,5,7,9]
x5= np.linalg.lstsq(a5,b5,rcond = -1)[0]
p("x5: ",x5)
# -------------------------------------
a6 = [[4,3,2,1],[8,7,6,5]]
b6 = [10,26]
x6= np.linalg.pinv(a6)@b6
p(x6)
# ============================================================
# ۷. توابع تبدیل داده‌ها
# ============================================================

# ۷-۱. astype - تبدیل نوع داده
new_array = array.astype(array_type)
# array_type = int / float / ...
# --------------------------
# ۷-۲. reshape - تغییر شکل آرایه
new_array = array.reshape(n, m)
# تغییر تعداد سطر و ستون بدون تغییر مقدارها
# -----------------------
# ۷-۳. flatten - تبدیل آرایه چندبعدی به یک‌بعدی
new_array = array.flatten()
# مثلاً برای تبدیل ماتریس به بردار
# -----------------------------
# ۷-۴. np.concatenate() - چسباندن آرایه‌ها
new_array = np.concatenate((array1, array2), axis=0/1)
# یک ماتریس ساخته میشود چه بردارها رو به هم بچسبانیم و چه ماتریس ها را
# توجه: آرایه‌ها باید ابعاد یکسانی داشته باشند

# axis=0 یا عدم درج: چسباندن عمودی
ردیف ها با هم جمع میشوند و ستون ها ثابت می مانند

# axis=1: چسباندن افقی
ستون ها با هم جمع میشوند و ردیف ها ثابت می مانند
# -------------
مثال :
import numpy as np
a6 = [[4,3,2,1],[8,7,6,5]]
a8 = [[4,3,2,1],[8,7,6,5]]
new = np.concatenate([a6,a8],axis=0)
p(new.shape ==(4,4)) # آیا ردیف ها با هم جمع شدن؟

new = np.concatenate([a6,a8],axis=1)  # آیا ستون ها با هم جمع شدن؟
p(new.shape ==(8,2))
-----------------------------------------------
# ۷-۵. np.split() - تقسیم آرایه
new_array = np.split(array, parts, axis=0/1)[0]
# axis=0 یا عدم درج: برش عمودی ستون‌ها
# axis=1: برش افقی ردیف به ردیف

parts=  یک عدد می نویسیم و پارامتری به اسم پارت نداریم
# توجه: خروجی به صورت لیستی از آرایه‌هاست
[0] برای تبدیل لیستی از آرایه ها به لیستی از لیست ها (البته فقط پارت اول را به ما میده)
مثال :
a6 = [[4,3,2,1],[8,7,6,5]]
a6_arr = np.array(a6)  تبدیل لیست لیست های بالا به آرایه(ماتریس)
new = np.split(a6_arr, 2,axis=1)[1]
p(new)
--------------------------------
# ۷-۶. np.sort() - مرتب‌سازی داده
new_array = np.sort(array)
# مرتب‌سازی از کوچک به بزرگ
مثال :
import numpy as np
a6 = [[4,3,2,1],[8,7,6,5]]
a6_arr = np.array(a6)
p(a6_arr.sort())

# ============================================================
# ۸. دسترسی به داده‌ها در ماتریس
# ============================================================

# انتخاب یک ردیف واحد
matrix_name[row_index]

# انتخاب رنجی از ردیف‌ها
matrix_name[start:end]

# انتخاب یک ستون
matrix_name[:, col_index]

# انتخاب رنجی از ستون‌ها
matrix_name[:, start:end]

# انتخاب رنجی از ستون‌ها و ردیف‌ها
matrix_name[start:end, start:end]

# انتخاب یک عنصر
matrix_name[row_index, col_index]

# مثال‌ها:
m[1]           # دسترسی به ردیف دوم
m[:, 1]        # دسترسی به ستون دوم
m[:-5, :4]     # دسترسی به پنج ردیف آخر از پنج ستون اول
m[2, 5]        # دسترسی به عنصر ردیف سوم، ستون ششم

# ============================================================
# ۹. میانگین وزندار در NumPy
# ============================================================

np.average()

# شکل رایج:
avg = np.average(m, weights=w)

# پارامترها:
# data = آرایه یا لیست مقادیر عددی
# weights =  آرایه‌ یا لیستی با طول برابر با داتا برای تعیین وزن هر مقدار داتا
# returned =True/False
# پیش‌فرض فولز اگر، ترو شود مجموع وزن‌ها را هم برمی‌گرداند
# axis =0/1 محور محاسبه میانگین برای ماتریس ها
صفر روی  ستون ها 
# ----------------
مثال با داتای برداری
import numpy as np
np.random.seed(2)
a = np.random.randint(-20,40,15)    
avg = np.average(a,weights = [i for i in range(15,30,1)])    #وزن، لیستی با رنج پانزده تایی
p(avg)
-------------
برای داتای برداری : دو حالت داره 
1) یا باید ماتریسی هم اندازه با داتا برای وزن هم بسازیم(اگر هر المنت داتا،فراوانی متفاوتی داشت)
import numpy as np
np.random.seed(2)
a = np.random.rand(15).reshape(3,5).round()
weight = np.random.randint(0,30,15).reshape(3,5)
avg = np.average(a,weights =weight)
p(avg)
# ----------------------------
# 2)  اکس تعریف کنیم و یک لیست یا آرایه هم اندازه با بعد مخالف اکس برای وزن ها بدیم
np.random.seed(2)
a = np.random.rand(15).reshape(3,5).round()
avg = np.average(a,axis=0,weights = [3,5,8])
# اکسِ صفر، مربوط به ستون هاست.
# هر ستون را اگر به صورت مجزا بررسی کنیم به تعداد ردیف های ماتریس المنت دارد
# پس باید به تعداد ردیف ها المنت در لیست وزن تعریف کنیم
# axis = 0  مربوط به ستون ها>> len(weights) = تعداد ردیف ها
# axis = 1   مربوط به ردیف ها >>> len(weights) = تعداد ستون ها 
p(avg)
# ============================================================
# ۱۰. تولید داده‌های تصادفی
# ============================================================

random.choices()
برای ساخت لیست از یک بازه یا لیست مرجع دلخواه 
از این لیست میتوان بعدها در ساخت آرایه استفاده کرد

شکل رایج
random.seed(2)  برای ثابت بودن نتیجه در تمام پرینت ها 
random.choices(population, k=n)

پارامترها:

🔹 population → 
مجموعه‌ای که از آن انتخاب تصادفی انجام می‌شود. می‌تواند لیست، تاپل یا رنج باشد.
مثال:
gender = ["M", "F", None] داتای منبع که توسط راندوم چویز درون لیست جدید تکثیر میشود
range(-20, 40)

🔹 k → تعداد المنت هایِ لیست جدید حاصل از داتای منبع
🔹 weights → وزن احتمال هر عنصر برای انتخاب (اختیاری). مثلا بعضی آیتم‌ها شانس بیشتری برای انتخاب داشته باشند.
--------------------
سایر پارامترها:

🔹 cum_weights → 
وزن تجمعی عناصر، 
جایگزین weights می‌شود (اختیاری).


مثال:
import random
random.seed(2)  # برای ثابت ماندن خروجی
gender = random.choices(population=["M","L",None], weights=[0.8,0.1,0.1], k=80)

مثال بعدی 
import numpy as np
import random
random.seed(1)
L= random.choices(range(-10,50), k= 8)
# لیست ساخته شد
v= np.array(L)
# آرایه ساخته شد

نکات مهم:
- خروجی این تابع همیشه لیست است و ممکن است حاوی مقادیر تکراری باشد؛ یونیک بودن تضمین نمی‌شود.
- این تابع متعلق به کتابخانه‌ی استاندارد راندوم پایتون است.
- نیازی به کتابخانه دیگری مانند نامپی یا پانداس ندارد.
----------------------------------------
random.sample()
مشابه تابع بالا ولی برای ساخت لیست های یونیک

random.sample(population, k=1)

پارامترها:

🔹 population → 
مجموعه‌ای که از آن انتخاب تصادفی انجام می‌شود. می‌تواند لیست، تاپل یا رنج باشد.
مثال:
gender = ["M", "F", None] 
range(-20, 40)
 
راندوم سمپل برخلاف راندوم چویز، داتای منبع را تکثیر نکرده بلکه از درون آن انتخاب میکند
تعداد این انتخاب ها را پارامتر "کا" انتخاب میکند و نباید عدد این پارامتر بزرگتر از تعداد المنت های داتای منبع باشد تا ارور ندهد

🔹 k → تعداد نمونه‌هایی که می‌خواهیم انتخاب کنیم (تعداد ردیف‌ها).
مثلا
k=عدد دلخواه ولی کوچکتر از len(population)
k=len(df) برای تولید لیست به اندازه طول ستون داتافریم

مثال:
import random
random.seed(2)  # برای ثابت ماندن خروجی
L = random.sample(population=range(-10,50) k=30)

نکات مهم :
خروجی این تابع همیشه یونیک است؛ هیچ عدد یا مقدار تکراری نخواهد بود.
- این تابع متعلق به کتابخانه‌ی استاندارد random پایتون است.
- نمی‌توان از weights یا cum_weights استفاده کرد.
- نیازی به کتابخانه دیگری مانند NumPy یا pandas ندارد.
----------------------------
np.random.choice()
تابع مشابه دوتای بالایی این بار از کتابخانه نامپی 
برای تولید آرایه

شکل رایج:
np.random.choice(a, size=, replace=False)
نتیجه تابع به صورت یک آرایه یک بعدی است

پارامترها:
🔹 a (معادل پاپیولیشن در دو تابع بالاتر)→
آرایه تک بعدی یا مجموعه‌ای که از آن انتخاب تصادفی انجام می‌شود. 
می‌تواند آرایه(البته نه ماتریس)، لیست یا رنج باشد.
فقط والیو یا نام داتای منبع را در تابع درج میکنیم 
و پارامتری به نام "ای" ندارد 

مثال:
arr = np.array([10, 20, 30, 40, 50])
range(10000, 100000)

🔹 size → تعداد المنت ها در آرایه جدیدی که تولید میشود
🔹 replace → پیشفرض ترو. اگر فولز شود، نمونه‌ها بدون تکرار انتخاب می‌شوند (یونیک خواهند بود).
🔹 p →پارامتری اختیاری و  معادل ویتز در دو تابع بالا برای وزن دهی به المنت های لیست جدید(وزن ها باید اعشاری و کوچکتر از یک باشند و مجموع همه وزن ها باید برابر با یک باشد)

مثال عملی:
import numpy as np
import pandas as pd
np.random.seed(2)  # برای ثابت ماندن خروجی
id = np.random.choice(range(10000,100000), size=len(df), replace=False)
print(id)
# یک ستون از آیدی 5 رقمی یونیک برای داتافریم پانداس ساختیم

نکات مهم:
- با replace=False → خروجی یونیک است، بدون تکرار.
- با replace=True → خروجی ممکن است تکراری باشد.
- این تابع متعلق به کتابخانه‌ی نامپی است و برای
تولید نمونه‌های تصادفی سریع و حرفه‌ای مناسب است.
# ============================================================
⚜️توزیع های مهم و مورد نیاز در کامپیوتر ویژن ⚜️
# انواع توزیع داده ها در نامپی

۱) توزیع یکنواخت Uniform Distribution🟣
تابعی آرایه ساز با سایزی دلخواه از روی یک رنج عددی 
همه المنت ها ممکن است تکرار شوند ولی با شانسی برابر 

تعریف ساده
در این توزیع، همه‌ی مقادیر در یک بازه شانس برابر دارند.

شکل رایج

data = np.random.uniform(low, high, size)

توضیح پارامترها: 
low = کمترین مقدار ممکن
high = بیشترین مقدار ممکن
size = تعداد داده‌ها

مثال
import numpy as np
np.random.seed(1)
data = np.random.uniform(-20, 40, 100).astype(int)
print(data)

مناسب برای: 
✅مقداردهی اولیه وزن‌ها در شبکه‌های عصبی ساده
✅شبیه‌سازی داده بدون سوگیری
✅تست الگوریتم‌ها

مناسب نیست برای
❌مدل‌سازی داده‌های واقعی طبیعی
❌داده‌هایی که تمرکز حول یک مقدار دارند
----------------------------------
۲) توزیع نرمال Normal (Gaussian) Distribution🟣
تابعی آرایه ساز با اندازه دلخواه از روی میانگین و انحراف معیار دلخواه 

تعریف ساده
اکثر داده‌ها نزدیک میانگین هستند و هرچه دورتر شویم، احتمال کمتر می‌شود.

شکل رایج
data = np.random.normal(mean, std, size)

توضیح پارامترها: 
mean = میانگین
std = انحراف معیار
size = تعداد داده‌ها

مثال
import numpy as np
np.random.seed(1)
data = np.random.normal(0, 1, 100)
print(data)

مناسب برای
✅مدل‌سازی نویز
✅داده‌های طبیعی و انسانی
✅فرض‌های آماری در یادگیری ماشین


مناسب نیست برای:
❌داده‌های کاملاً نامتقارن
❌داده‌های گسسته
-------------------------------------------
رابطه انحراف معیار و میانگین در توزیع نرمال 
در توزیع نرمال مقدار عددی میانگین و انحراف معیار چقدر باشد؟
--------------------------------------
💠رابطه انحراف معیار و میانگین در توزیع نرمال💠
در تولید داده های آزمایشی برای مدل ها بسته به هدفی که داریم باید بدانیم کدامیک از انحراف معیار و میانگین  رو بزرگتر و چقدر بزرگتر انتخاب کنیم. در کل، هر چه انحراف معیار کوچکتر از میانگین باشد ، داده ها پایدارتر و کم نویزرترند و برعکس هر چه انحراف معیار به میانگین نزدیک تر شود یا از آن بزرگتر شود داده ها خشن و ناپایدارتر و نویزی از میشوند. 
یک قاعده تجربی ساده برای سنجش پایداری داده ها وجود دارد که بر مبنای نسبت انحراف معیار بر میانگین است 
cv = scale/loc 

CV < 0.1 → بسیار پایدار
انحراف معیار خیلی کوچک‌تر از میانگین است
تقریباً:
میانگین ≈ ۱۰ برابر یا بیشتر از انحراف معیار
------------------------------------------
0.1 ≤ CV < 0.3 → نوسان معمول
انحراف معیار کوچک‌تر از میانگین است
تقریباً:
میانگین ≈ ۳ تا ۱۰ برابر انحراف معیار
--------------------------------------
0.3 ≤ CV < 1 → نوسان زیاد (مرزی)
انحراف معیار هم‌مرتبه با میانگین است
تقریباً:
میانگین ≈ ۱ تا ۳ برابر انحراف معیار
----------------------------------
CV ≥ 1 → نوسان شدید
انحراف معیار بزرگ‌تر یا مساوی میانگین است
تقریباً:
انحراف معیار ≈ ۱ برابر یا چند برابر میانگین
# ============================================================
# ۱۲. توابع ریاضی و آماری پیشرفته - کاربرد در Computer Vision
این موضوع را در حین برنامه 5 ماهه کامپیوتر ویژن یاد خواهید گرفت
پس الان، لازم به خواندن نیست
# ============================================================
# کاربرد توابع ریاضی 
# کاربرد توابع آماری

# تفاوت اصلی توابع ریاضی و آماری:
# - توابع ریاضی: محاسبات عنصر به عنصر، خروجی هم‌شکل ورودی (مثل np.sqrt)
# - توابع آماری: خلاصه‌سازی توزیع داده، خروجی اسکالر یا کوچک‌تر (مثل np.mean)

# ============================================================
# ۱۲-۱. پیش‌پردازش تصویر
# ============================================================

img = np.random.randint(0, 256, (100, 100, 3), dtype=np.uint8)

# نرمال‌سازی Z-score
img_normalized = (img - np.mean(img)) / np.std(img)

# نرمال‌سازی 0 تا 1
img_01 = (img - np.min(img)) / (np.max(img) - np.min(img))

# جلوگیری از overflow
img_clipped = np.clip(img, 0, 255).astype(np.uint8)

# تبدیل به خاکستری (weighted)
gray = np.dot(img[..., :3], [0.2989, 0.5870, 0.1140])

# ============================================================
# ۱۲-۲. آمار تصویر
# ============================================================

np.mean(img, axis=(0, 1))     # میانگین هر کانال
np.std(img, axis=(0, 1))      # std هر کانال
np.histogram(img, bins=256)   # هیستوگرام
np.percentile(img, 95)        # حذف outlier

# ============================================================
# ۱۲-۳. عملیات ماتریسی - شبکه عصبی
# ============================================================

W = np.random.randn(64, 128)
x = np.random.randn(128)

np.dot(W, x)               # لایه Fully Connected
np.matmul(W, W.T)          # ضرب ماتریس
np.linalg.svd(W)           # کاهش بعد / PCA
np.linalg.eig(np.cov(W))   # مقادیر ویژه برای PCA

# ============================================================
# ۱۲-۴. توابع فعال‌سازی
# ============================================================

x = np.random.randn(10)

sigmoid = 1 / (1 + np.exp(-x))
relu = np.maximum(0, x)
softmax = np.exp(x) / np.sum(np.exp(x))
tanh = np.tanh(x)

# ============================================================
# ۱۲-۵. Loss Functions
# ============================================================

y_pred = np.array([0.9, 0.1])
y_true = np.array([1.0, 0.0])

mse = np.mean((y_pred - y_true) ** 2)
cross_entropy = -np.sum(y_true * np.log(y_pred + 1e-8))
mae = np.mean(np.abs(y_pred - y_true))

# ============================================================
# ۱۲-۶. Convolution و فیلتر
# ============================================================

# Padding
img_padded = np.pad(img, pad_width=1, mode='constant')

# FFT برای فیلتر فرکانسی
freq = np.fft.fft2(gray)
img_back = np.fft.ifft2(freq)
freq_shifted = np.fft.fftshift(freq)

# ============================================================
# ۱۲-۷. تبدیل و هندسه
# ============================================================

np.rot90(img)                    # چرخش 90 درجه
np.flip(img, axis=0)             # آینه عمودی
np.transpose(img, (2, 0, 1))     # HWC به CHW
img.reshape(-1)                  # Flatten
r, g, b = img[...,0], img[...,1], img[...,2]
np.stack([r, g, b], axis=-1)     # ترکیب کانال‌ها

# ============================================================
# ۱۲-۸. آستانه‌گذاری و ماسک
# ============================================================

mask = gray > np.mean(gray)
binary = np.where(gray > 127, 255, 0)
predictions = np.random.rand(10, 5)
np.argmax(predictions, axis=1)   # پیش‌بینی کلاس

# ============================================================
# ۱۲-۹. ارزیابی مدل
# ============================================================

pred_labels = np.array([1, 0, 1, 1])
true_labels = np.array([1, 0, 0, 1])

accuracy = np.mean(pred_labels == true_labels)

v1 = np.random.rand(128)
v2 = np.random.rand(128)

euclidean = np.linalg.norm(v1 - v2)
cosine_sim = np.dot(v1, v2) / (np.linalg.norm(v1) * np.linalg.norm(v2))

# ============================================================
# ۱۲-۱۰. مسیر کلی CV با NumPy:
# تصویر خام
#   -> np.mean, np.std       (نرمال‌سازی)
#   -> np.matmul, np.dot     (لایه‌های شبکه)
#   -> np.exp, np.maximum    (فعال‌سازی)
#   -> np.log, np.mean       (محاسبه Loss)
#   -> np.linalg.norm        (ارزیابی)
# ============================================================
✴️مراحل ضروری تحلیل داده ها قبل از ورود به Computer Vision✴️
این نوشته برای تحلیل داده های پانداس منشا است در حالی که داده های منبع در کامپیوتر ویژن اغلب منشا آرایه ای دارند پس تحلیل داده در کامپیوتر ویژن با آنچه در پایین میبیند متفاوت است ولی همچنان مطلب زیر برای درک موضوع، مفید است
# ============================================================
#تحلیل داده ها 
#درک توزیع داده ها 
🔴درک توزیع زیرمجموعه تحلیل و بخش مهمی از آن  است.
 
🟢 مرحله ۱: شناخت ساختار داده
هدف: فهمیدن اینکه دیتاست چیست و چه چیزهایی در آن وجود دارد.
توابع لازم:

df.shape
df.columns
df.info()
df.head()
باید بفهمی:
چند سطر و ستون داریم
ستون‌ها چه مفهومی دارند
داده ناقص یا مشکوک هست یا نه

🟢 مرحله ۲: بررسی کیفیت داده
هدف: مطمئن شدن از قابل‌استفاده بودن دیتاست.
توابع لازم:

df.isnull().sum()
df.duplicated().sum()
df.drop_duplicates()
باید بفهمی:
داده خالی داریم یا نه
داده تکراری داریم یا نه
---------------
🟢 مرحله ۳: درک عددی اولیه (Sanity Check)
هدف: چک کردن عجیب نبودن داده‌های عددی.
توابع لازم:

df.describe()
df.min()
df.max()
باید بفهمی:
بازه مقادیر منطقی است یا نه
عدد غیرعادی وجود دارد یا نه
--------------
🟢 مرحله ۴: درک توزیع عددی (فهم شهودی)
هدف: فهمیدن اینکه داده‌ها چطور پخش شده‌اند.
توابع لازم:

df["col"].mean()
df["col"].median()
df["col"].skew()
------------------
توضیح skew:
نزدیک ۰ → نسبتاً متقارن
مثبت → دم راست بلند
منفی → دم چپ بلند
---------------
باید بفهمی:
داده متقارن است یا یک‌طرفه
آیا میانگین نماینده خوبی هست یا نه
آیا توزیع نامتوازن است
-------------------
🟢 مرحله ۵: درک توزیع دسته‌ها / کلاس‌ها
هدف: فهمیدن فراوانی گروه‌ها یا برچسب‌ها.
توابع لازم:

df["label"].value_counts()
df["label"].nunique()
باید بفهمی:
آیا یک دسته غالب است
آیا داده منصفانه پخش شده یا نه
-----------------
🟢 مرحله ۶: تصویرسازی حداقلی
هدف: دیدن سریع الگوها، نه گزارش‌سازی.
توابع لازم:

df["col"].hist()
df.plot()
باید بفهمی:
آیا شکل توزیع عجیب است
آیا عدم تعادل واضح دیده می‌شود
🟢 مرحله ۷: نتیجه‌گیری ذهنی
هدف: جمع‌بندی بدون کد.
باید بتوانی جواب بدهی:
دیتاست سالم است یا نه؟
عدم تعادل واضح دارد یا نه؟
چیزی هست که بعداً باید اصلاح شود؟
🟢 مرحله ۸: کنترل نهایی قبل از ادامه مسیر
هدف: اطمینان از آمادگی دیتاست.
توابع لازم:

df.isnull().sum()
df.duplicated().sum()
df.info()
🧭 مفاهیمی که در ادامه مسیر با آن‌ها آشنا خواهی شد (فقط نام‌ها)
توزیع شدت پیکسل‌ها
imbalance در مدل
normalization
data augmentation
validation distribution
اثر توزیع روی یادگیری مدل
✅ جمله‌ی قفل‌کننده
تحلیل داده یعنی فهمیدن رفتار داده؛
درک توزیع یعنی فهمیدن «غلبه»‌ها.


# ============================================================
# مفاهیم کلیدی در مسیر هوش مصنوعی
# ============================================================

# - توزیع شدت پیکسل‌ها
# - Imbalance در مدل
# - Normalization
# - Data augmentation
# - Validation distribution
# - اثر توزیع روی یادگیری مدل

# ============================================================
# جمله کلیدی:
# تحلیل داده یعنی فهمیدن رفتار داده؛
# درک توزیع یعنی فهمیدن «غلبه»‌ها.
# ============================================================
'''